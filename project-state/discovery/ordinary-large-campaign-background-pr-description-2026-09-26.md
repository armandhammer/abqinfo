## Summary

Resolve 500 formerly pending records across 201 completed source/documentary families: 13 substantive approvals, 23 exact aliases and 464 scope exclusions. Preserve all existing human-review and completed families and every unrelated inventory row.

Archive the 13 unchanged CMP originals in the existing operations/data namespace and verify every public download by exact size and SHA-256. R2 grows by 17,230,744 bytes to 1,292 objects / 9,387,544,940 bytes, below the unchanged storage ceiling. All pre-existing keys, sizes and ETags remain unchanged.

Save source/container/render evidence, family dispositions, full-inventory exact-identity audit, final live reconciliation, campaign accounting and the filtered remaining queue. Add deterministic regressions for transitions, scope, archives, storage, protected rows and zero content changes. Preserve earlier campaign contracts against their immutable completion snapshot.

## Visible site changes

None.

## Validation

Complete project validation, including inventory/schema, mission scope, borderline queue, family/archive regressions and Hugo build; exact fresh public GETs for all 13 originals; complete saved/live R2 equality; `git diff --check`; unchanged `content/` tree.
