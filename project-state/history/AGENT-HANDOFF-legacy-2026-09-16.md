# ABQInfo Dual-Agent Work Ledger

## Claude checkpointed consolidation-research lane — active September 13, 2026

Claude's next work is the queue in `project-state/discovery/claude-consolidation-research-queue-2026-09-13.json`. This is a research-only lane supporting Codex's approved site cleanup. Work slices in ascending `priority` order and continue automatically until the queue is complete or a systemic blocker occurs.

Checkpoint after **every document or official-source meeting check**, not merely at the end of a slice. Write only within the queue's unique `research/staging/claude-consolidation-checkpoints/<slice>/items/` directories and the named self-contained slice-result artifacts. Complete each JSON through a temporary sibling and rename it atomically. Before doing work, scan existing checkpoints; a valid result is immutable and must never be repeated. If usage stops, the next Claude session resumes at the first missing document in the lowest-priority unfinished slice.

For every annual committee-minutes slice, exhaustively check the official calendar/archive for missing approved minutes, cancellations, no-quorum meetings, and agendas eligible under the missing-minutes policy. For every program slice, review the complete source family, version relationships, governing records, page order, proposed contents, and whether a compilation would exceed 100-150 pages. Do not edit shared inventory, project checkpoint, site content, R2 state, Git state, or deployment state. Codex alone integrates accepted research.

This ledger keeps Codex and Claude productive at the same time without user relay.

## Coordination rules

- Read this file, `AGENTS.md`, `project-state/checkpoint.json`, and `project-state/master-inventory.json` before claiming work.
- A research lane writes only a dated, self-contained decision artifact under `project-state/discovery/`. It must not rewrite `master-inventory.json`, `checkpoint.json`, site content, or R2 state while the other agent may be writing those shared files.
- The Codex integration lane validates and applies completed research artifacts to shared inventory state. It records the integration result in the checkpoint.
- Once an agent completes a lane, it automatically claims the next listed lane for that agent. Do not ask the user to carry a prompt or wait for another agent's narrative response.
- The user has authorized Codex to upload, merge, and deploy routine archive-ready content batches autonomously. Claude remains research-only unless separately authorized. Codex must still stop for a novel content category, unresolved provenance/version conflict, failed validation, destructive storage action, credential/permission change, or material site-architecture change.
- For any later site-content work, a static document must first have an R2 archive object whose public download, exact size, SHA-256, and authoritative-source provenance are verified. A direct official link alone is not site-ready; live official services such as ArcGIS web maps are the exception.

## Active lanes

| Owner | Lane | Deliverable | State |
| --- | --- | --- | --- |
| Codex | Reconcile verified inventory successions, then integrate the active construction-documents research artifact when available | Deterministic inventory validation and checkpoint update | active |
| Claude | DPM remaining-draft and agenda review | A dated research decision file for the 43 draft/redline chapters and eight agendas; classify only, do not edit shared inventory | complete — `project-state/discovery/dpm-draft-and-agenda-research-2026-09-11.json` |
| Claude | Review the next substantive pending-review source cluster outside the DPM library | A dated research decision file for 15–30 candidates, with authoritative URLs and duplicate/supersession checks | complete — `project-state/discovery/early-go-bond-cluster-research-2026-09-11.json` |
| Claude | Review a further substantive pending-review cluster after the current one | A dated research decision file with authoritative URLs and duplicate/supersession checks | complete — `project-state/discovery/go2009-bond-cluster-research-2026-09-11.json` |
| Claude | Review the `documents.cabq.gov/planning/UDD` cluster (30 pending) | A dated research decision file separating substantive plans, overlay-zone guidelines, and maps from routine application forms | complete — `project-state/discovery/planning-udd-cluster-research-2026-09-11.json` |
| Claude | Review the `cip-documents/2011-go-bond-documents` cluster (46 pending) | A dated research decision file applying the 2009 directory's component-versus-canonical method, using normalized-text and token-coverage comparison rather than hashes alone | complete — `project-state/discovery/go2011-bond-cluster-research-2026-09-11.json` |
| Claude | Review the `documents.cabq.gov/planning/code-enforcement` cluster (27 pending) | A dated research decision file separating adopted enforcement standards and notices from routine forms, applying the UDD form-versus-standard split | complete — `project-state/discovery/code-enforcement-cluster-research-2026-09-11.json` |
| Claude | Review the `www.cabq.gov/municipaldevelopment/documents/construction-documents` cluster (18 pending) | A dated research decision file separating adopted construction standards and specifications from routine forms and bid paperwork | complete — `project-state/discovery/construction-documents-cluster-research-2026-09-11.json` |
| Claude | Review the `documents.cabq.gov/planning/DevelopmentReviewServices` cluster (81 pending) | A dated research decision file for the largest remaining pending cluster, separating adopted standard specifications, standard drawings, and design guidance from submittal forms and checklists | complete — `project-state/discovery/development-review-services-cluster-research-2026-09-11.json` |
| Claude | Review the flat level of `www.cabq.gov/council/documents` (107 pending) | A dated research decision file separating enacted legislation, adopted plans, and Council reports from routine agendas, notices, and administrative forms | complete — `project-state/discovery/council-documents-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/rail-yard` cluster (20 pending) | A dated research decision file separating Rail Yards planning, environmental, and redevelopment records from meeting handouts, extending the placement already established under Rail Yards Planning and Environmental Records | complete — `project-state/discovery/rail-yards-advisory-board-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/police-oversight-task-force-documents` cluster (20 pending) | A dated research decision file separating the task force's findings, recommendations, and adopted ordinance material from meeting handouts, applying the missing-minutes agenda policy as recorded in the Rail Yards artifact | complete — `project-state/discovery/police-oversight-task-force-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/councilor-district-5-documents` cluster (20 pending) | A dated research decision file that must resolve the cross-directory DOJ findings-letter duplicate flagged by the police-oversight artifact, and separate substantive district records from constituent notices | complete — `project-state/discovery/councilor-district-5-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/paseo-del-volcan-documents` cluster (20 pending) | A dated research decision file separating corridor studies, alignment records, and adopted plans from meeting handouts and correspondence | complete — `project-state/discovery/paseo-del-volcan-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/redistricting-documents` cluster (18 pending) | A dated research decision file separating adopted district maps and redistricting committee findings from meeting handouts and public-comment material | complete — `project-state/discovery/redistricting-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/Volcano Heights Sector Development Plan` cluster (20 pending) | A dated research decision file separating the adopted sector development plan and its supporting studies from meeting handouts, checking against the already-validated Volcano Trails records | complete — `project-state/discovery/volcano-heights-sdp-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/pgs` Planned Growth Strategy cluster (19 pending, plus both prior-lane RHR records) | A dated research decision file that discharges the checkpoint's open PGS split-package blocker by reconciling the complete package with the method recorded in the Volcano Heights artifact, and classifies the directory | complete — `project-state/discovery/planned-growth-strategy-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/form-based-code` cluster (20 pending) | A dated research decision file separating adopted form-based code text and regulating plans from drafts and meeting handouts, applying the enactment test and the split-package method | complete — `project-state/discovery/form-based-code-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/nob-hill-highland` cluster (20 pending) | A dated research decision file separating adopted sector plan text and regulating material from drafts and meeting handouts, rendering image-only records and probing the filename sequence against the City listing | complete — `project-state/discovery/nob-hill-highland-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/if` impact-fees cluster (19 pending) | A dated research decision file that checks each candidate against the already-triaged CCIP resolutions and the O-04-9 land use assumptions ordinance, and against R2 objects already held | complete — `project-state/discovery/impact-fees-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/councilor-district-2-documents` cluster (27 pending) | A dated research decision file separating substantive district records from constituent notices and press material, applying the district-5 method: render image-only records, check the enactment number on every legislative file, and check against R2 objects already held | complete — `project-state/discovery/councilor-district-2-cluster-research-2026-09-11.json` |
| Claude | Inventory-wide audit of records sharing one URL, prompted by the district-2 finding | A dated audit artifact quantifying the collision, separating the actionable subsets, and recording the method for future lanes | complete — `project-state/discovery/inventory-url-collision-audit-2026-09-11.json` |
| Claude | Review the `council/documents/amendments-for-posting-on-council-page` cluster (29 pending) | A dated research decision file that TESTS rather than assumes the amendment-sheet category decision recorded in the council-documents artifact, by fetching and reading every file rather than inferring from the directory name | complete — `project-state/discovery/council-amendments-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/meeting-agenda-documents` cluster (20 pending) | A dated research decision file applying the missing-minutes agenda policy and its recorded exhaustive-review method, running the URL group-by first | complete — `project-state/discovery/meeting-agenda-documents-cluster-research-2026-09-11.json` |
| Claude | Review the `council/documents/councilor-district-9-documents` cluster (20 pending) | A dated research decision file separating substantive district records from constituent notices, running the URL group-by first and rendering image-only records | complete — `project-state/discovery/councilor-district-9-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/councilor-district-4-documents` cluster (17 pending, not 16) | A dated research decision file that applies the district-9 lesson before writing any enactment-blocker row: search the inventory for the adopted outcome, not only for the enactment number | complete — `project-state/discovery/councilor-district-4-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/dnasdp-documents` cluster (17 pending, not 16) | A dated research decision file that fetches both URL forms per the district-4 Plone `/view` finding, and tests each candidate against R2 objects already held before recommending anything | complete — `project-state/discovery/dnasdp-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/councilor-district-7-documents` cluster (16 pending, not 15) | A dated research decision file that checks every candidate against the *published* site as well as the inventory, per the dnasdp finding that a published record can be the superseded one | complete — `project-state/discovery/councilor-district-7-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/councilor-district-1-documents` cluster (13 pending, not 12) | A dated research decision file applying the now-standard opening sequence: URL group-by, both URL forms, internal and inventory-wide hashing, then a comparison against the published site and R2 before anything is recommended | complete — `project-state/discovery/councilor-district-1-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/east-central-sector` cluster (9 pending, not 8) | A dated research decision file that runs the standard opening sequence and reconciles any multi-file sector-plan package by printed page ranges, per the PGS and DNASDP method | complete — `project-state/discovery/east-central-sector-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/councilor-district-6-documents` cluster (9 pending, not 8) | A dated research decision file running the standard opening sequence, reading each record's citations closely enough to answer other clusters' open questions the way the district-9 EPC record answered R-07-278 | complete — `project-state/discovery/councilor-district-6-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/12th-and-menaul` cluster (7 pending, not 6) | A dated research decision file running the standard opening sequence, checking each candidate against the already-triaged Menaul MRA records the council-amendments artifact identified | complete — `project-state/discovery/12th-and-menaul-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/completed-reports-studies` cluster (7 pending, not 6) | A dated research decision file running the standard opening sequence; given the directory name, test every candidate against the published site before anything else, since a completed report is the class most likely to be held already | complete — `project-state/discovery/completed-reports-studies-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/4th-street-montano-area-improvement-coalition` cluster (6 pending) | A dated research decision file running the standard opening sequence, checking every candidate against the Fourth Street and Montaño material the site already publishes on `content/development-land-use/area-sector-plans.md` and `content/transportation/roadway-projects/studies.md` | complete — `project-state/discovery/4th-street-montano-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/West Central Metropolitan Redevelopment Area` cluster (6 pending, not 5) | A dated research decision file running the standard opening sequence and testing every candidate against the West Central MRA material already held, before anything is recommended | complete — `project-state/discovery/west-central-mra-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/sawmill-documents` cluster (5 pending, not 4) | A dated research decision file running the standard opening sequence, and applying the new enactment-number method — read later legislation's recitals, not the plans those instruments adopted — to any legislative file it finds | complete — `project-state/discovery/sawmill-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/economic-forum` cluster (5 pending, not 4) | A dated research decision file running the standard opening sequence, testing each candidate against the published site as well as the inventory, and comparing normalized text rather than bytes wherever a version chain appears | complete — `project-state/discovery/economic-forum-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents/councilor-district-8-documents` cluster (5 pending, not 4) | A dated research decision file running the standard opening sequence, verifying every container by leading bytes per the economic-forum content-substitution finding, and testing each candidate against records recommended earlier in this batch as well as against terminal inventory state | complete — `project-state/discovery/councilor-district-8-cluster-research-2026-09-12.json` |
| Claude | Review the `council/documents` topical slice (63 fetched, 22 genuinely uncovered) | A dated research decision file covering the planning and policy material; 41 of the 63 turned out to be already covered by the 2026-09-11 flat-level artifact and are accounted for rather than re-decided | complete — `project-state/discovery/council-documents-topical-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 1: `cip-documents` (42 uncovered) | A dated research decision file claimed against the artifact-coverage set, with the coverage gate asserted in the generator itself | complete — `project-state/discovery/cip-documents-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 2: the bid and procurement series (52 uncovered, not 50) | A dated research decision file testing whether any of these are substantive City records rather than transactional solicitation paperwork, rather than assuming the whole class is excluded | complete — `project-state/discovery/municipaldevelopment-procurement-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 3: the agenda and minutes series (26 uncovered) | A dated research decision file applying the missing-minutes policy, checking for each body whether approved minutes exist before any agenda is considered | complete — `project-state/discovery/municipaldevelopment-agenda-minutes-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 4: the 2013 GO bond department scope and summary set (58 uncovered) | A dated research decision file; the rest of the flat level proved heterogeneous and is sliced separately | complete — `project-state/discovery/bond-2013-department-set-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 5: the remaining flat-level bond material (numbered purpose sheets, lowercase bond sheets, the 2015 G.O. summary) and the standard-form agreements, ~45 uncovered | A dated research decision file; date each group from the face of the documents, and compare the lowercase and CamelCase sheets before assuming either is a copy | complete — `project-state/discovery/flat-bond-and-standard-forms-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 6: the facility environmental compliance set — the fifteen 2021 MSGP stormwater pollution prevention plans, the seven MS4 water-quality monitoring records, and the five site-named HelioScope solar reports (27 uncovered) | A dated research decision file; establish for each SWPPP which City facility and which permit term it belongs to, and decide whether the site-named HelioScope series changes the exclusion recorded for the unattributable sixth report | complete — `project-state/discovery/facility-environmental-compliance-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 7: the project-specific engineering and procurement records — numbered project sheets, pre-submittal presentations and sign-ins, site walkthroughs, and the questions-and-answers files (~30 uncovered) | A dated research decision file; tie each file to its City project number and decide whether procurement process papers for a single project belong in a public archive at all | complete — `project-state/discovery/project-procurement-records-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 8: the permits, forms and applications group — excavation and barricade forms, right-of-way and on-street parking applications, the automated speed enforcement forms, the `mf-` numbered forms and the fiber-optic right-of-way regulations (~30 uncovered) | A dated research decision file; separate adopted regulations from the application forms that implement them, and date every form from its own face rather than from a version number in its filename | complete — `project-state/discovery/permits-forms-regulations-cluster-research-2026-09-12.json` |
| Claude | Review the `municipaldevelopment/documents` tree, slice 9: what remains at the flat level — the moratorium notices and maps, the project and facility sheets, the bike and pedestrian education posters, the presentations, and the remaining miscellany (~65 uncovered) | A dated research decision file; this is the heterogeneous remainder, so group it by what each document is before deciding, and expect filenames to be unreliable — eight in this tree have now contradicted their own documents | complete — `project-state/discovery/municipaldevelopment-flat-remainder-cluster-research-2026-09-13.json` |
| Claude | Review `www.cabq.gov/council/find-your-councilor`, slice 1: the district-3 and district-6 councillor document sets (70 uncovered) — the two largest districts not yet reviewed | A dated research decision file; district lanes are already established for 1, 2, 4, 5, 6, 7, 8 and 9, so check each record against those artifacts before deciding and carry the render-and-hash rule into any map or graphic set | complete — `project-state/discovery/councilor-district-3-and-6-cluster-research-2026-09-13.json` |
| Claude | Review `www.mrcog-nm.gov/DocumentCenter/View` (116 uncovered) — the Mid-Region Council of Governments document set, now the largest block of uncovered candidates that is actually documents | A dated research decision file; these are a partner agency’s records rather than the City’s, so establish for each whether MRCOG is the authoritative publisher and whether the City adopted or merely received it, and apply the third-party rule already recorded for USDA-NRCS, NMAC and PNM | complete — `project-state/discovery/mrcog-documentcenter-cluster-research-2026-09-13.json` (119 records; the tree held 119, not 116) |
| Claude | Review `www.cabq.gov/parksandrecreation/parks` (51) and `/recreation` (46), 97 uncovered | A dated research decision file; run the containment test before recommending any part of a plan, and check every candidate against the archive’s existing parks holdings before recommending addition | complete — `project-state/discovery/parks-recreation-cluster-research-2026-09-13.json` |
| Claude | Review `api.realfile.rtsclients.com/PublicFiles/f260a66b364d453e91ff9b3fedd494dc` (87 uncovered) | A dated research decision file; this is a third-party document-hosting endpoint rather than a City domain, so establish first what agency the files belong to and whether the endpoint is the authoritative source or a vendor mirror | complete — `project-state/discovery/nmdot-file-host-cluster-research-2026-09-13.json` |
| Claude | Review `www.cabq.gov/council/projects` (38) and `documents.cabq.gov/planning/online-forms` (31), 69 uncovered | A dated research decision file; check the inventory for the archive’s existing decisions about each source **before** framing the lane — two briefs in a row have started from a wrong premise | complete — `project-state/discovery/council-projects-planning-forms-cluster-research-2026-09-13.json` |
| Claude | Review `www.cabq.gov/neighborhood-traffic-management-program/documents` (29) and `www.cabq.gov/planning/documents` (28), 57 uncovered | A dated research decision file; check the archive’s existing decisions at both paths first, and **run the cross-inventory checksum sweep** — it has found three already-archived records in two consecutive lanes | complete — `project-state/discovery/ntmp-planning-documents-cluster-research-2026-09-13.json` (61 records) |
| Claude | Review `www.riometro.org/DocumentCenter/View` (21) and `www.cabq.gov/sustainability/documents` (20) and `www.cabq.gov/economicdevelopment/documents` (20), ~61 uncovered | A dated research decision file; check each source’s existing dispositions first and run the cross-inventory checksum sweep before writing | complete — `project-state/discovery/riometro-sustainability-econdev-cluster-research-2026-09-13.json` (67 records) |
| Claude | Review the four next-largest document-bearing sources together: `www.cabq.gov/transit/documents` (16), `www.cabq.gov/parksandrecreation/open-space` (15), `www.dot.nm.gov/planning-research-multimodal-and-safety/modal` (15) and `www.cabq.gov/sustainability/transportation` (12), 58 uncovered | A dated research decision file; check existing dispositions at each source first and run the cross-inventory checksum sweep before writing | complete — `project-state/discovery/transit-openspace-nmdot-sustainability-cluster-research-2026-09-13.json` |
| Claude | Review the Council clusters: `www.cabq.gov/council/administration-questions-answers` (15), `www.cabq.gov/council/albuquerque-bernalillo-county-government-commission` (14) and `www.cabq.gov/council/news` (11), ~40 uncovered | A dated research decision file; check existing dispositions first and run the checksum sweep **against both the archived inventory records and every row of every saved artifact** | complete — `project-state/discovery/council-qa-lgcc-news-cluster-research-2026-09-13.json` |
| Claude | Review **all remaining uncovered candidates except the `find-your-councilor` tree** — 416 records spread across ~100 directories, none larger than 9, so one sweep lane rather than many | A dated research decision file; fetch and measure every one, classify by container, and run the two-set checksum sweep. This exhausts the document-bearing pool; the 169 `find-your-councilor` records remain as the bulk HTML-confirmation job already recommended to Codex | complete — `project-state/discovery/final-sweep-cluster-research-2026-09-13.json` |
| Claude | Close out the pool: the **169 remaining `council/find-your-councilor` records**, the last uncovered block in the inventory | A dated research decision file. `councilor-district-3-and-6-cluster-research-2026-09-13.json` established by fetching 100 of that tree’s 239 records that not one is a static document; **confirm that by measurement across all 169 rather than by inference**, run the two-set checksum sweep, and record any record that is *not* a web page as a finding | complete — `project-state/discovery/find-your-councilor-cluster-research-2026-09-14.json` |
| Claude | **The inventory pool is exhausted; the remaining research work is what the run found but the inventory does not hold.** Resolve the undiscovered documents reported across these lanes — the **37 MS4 stormwater documents**, the **4 files found on councillor pages** (including the April 2018 Cruising Task Force report), and the **Sandia High School study’s final report of July 2023** | A dated research decision file recording, for each, the authoritative URL, measured size, SHA-256 and container, and the two-set checksum sweep against both the archived inventory records and every row of every saved artifact. **These are candidate *additions* to the inventory, so the artifact must say plainly that no inventory id exists for them yet** | complete — `project-state/discovery/undiscovered-documents-research-2026-09-14.json` |
| Claude | **Apply this run’s most productive method to the pages it was never applied to.** Two lanes harvested document links from City pages they were about to discard and found **41 files the crawl had missed**. This run excluded **roughly 900 HTML pages** across its lanes and harvested almost none of them. Parse every excluded page for links to pdf, doc, docx, xls, xlsx, ppt, pptx and csv files and reconcile the results against the inventory | A dated research decision file. The HTML for ~535 of those pages is already on disk from the final-sweep and find-your-councilor lanes, so most of it needs no refetch. Fetch and measure every undiscovered link found, and sweep **by checksum against every checksummed inventory record and by URL against every inventory URL** — not only the archived ones, because these would be additions | complete for the `www.cabq.gov` slice — `project-state/discovery/harvested-city-documents-research-2026-09-14.json` |
| Claude | Continue the harvest: the **77 `documents.cabq.gov` planning files** — 23 master development plans, 20 framework plans, the development review board set, building safety and design review forms, the Mesa del Sol plan series and four large R-23 Council bills | A dated research decision file in the same *proposed additions* form (`inventory_id: null`, `recommendation` not `recommended_status`). **Most of the master development plans have no text layer at all and must be rendered, not guessed from their filenames.** Two of them are already recorded as `do not add` in the `www.cabq.gov` slice and must be decided here | complete — `project-state/discovery/harvested-planning-documents-research-2026-09-14.json` |
| Claude | Then finish the harvest: the **173 real property flyers** from `dmdgis.cabq.gov` as **one class decision**, and the **36 files on third-party hosts** (NMDOT via `api.realfile.rtsclients.com` and `dot.nm.gov`, MRCOG, three federal agencies, the City transit map host and the file-share host) | A dated research decision file. The flyers are auto-generated listing sheets off a live GIS, carry no date of issue, and say AVAILABLE — **whether the archive keeps a snapshot of a live dataset is a scope question for a person, so put them to one as a single package** | complete — `project-state/discovery/harvested-remainder-research-2026-09-14.json` |
| Claude | **Extend the harvest to the pages whose scratch files are gone.** Only 535 of the excluded HTML pages were still on disk to parse; **220 more excluded HTML rows across eleven earlier artifacts have never been link-harvested**. Re-fetch those 220 pages and parse them for document links | A dated research decision file. Reconcile every link **against every inventory URL and against every row of every saved artifact, including the three harvest lanes**, then fetch and measure whatever is left. On the first harvest’s yield — 535 pages → 355 undiscovered documents — this is the highest-value work remaining | complete for the Council slice — `project-state/discovery/second-harvest-council-research-2026-09-14.json` |
| Claude | Finish the second harvest: the **50 City/County Behavioral Health Task Force files** (2014–2015) — agendas, meeting notes, working-group notes, recommendations, funding proposals, resource guides and consultant presentations | A dated research decision file in the *proposed additions* form. **This body published meeting notes as well as agendas**, so the missing-minutes exception is not needed; check each file for whether it is a City record, a County record or a third party’s, and watch for material that names private individuals — one file opens with a personal account from a family member | complete — `project-state/discovery/behavioral-health-task-force-research-2026-09-14.json` |
| Claude | Measure and decide the two traffic studies the repository owner retrieved by hand from `sfftp.cabq.gov`, which no automated fetch can reach | A dated research decision file recording both, the weaker provenance chain, and what it resolves in three earlier artifacts | complete — `project-state/discovery/file-share-retrieval-research-2026-09-14.json` |
| Claude | Sweep the City site for `sfftp.cabq.gov` links — the file-transfer host no automated fetch can read — and decide whatever the repository owner retrieves from them | A dated research decision file recording every link found, where it is linked from, and the owner’s determination on each | complete — `project-state/discovery/file-share-sweep-research-2026-09-14.json` |
| Claude | **The harvest still has a much larger seam.** Both harvests parsed only pages this run had *excluded*. The inventory holds **2,586 page-shaped records that have never been parsed for document links** — 882 excluded, 751 pending review, 508 validated, and the rest duplicate, superseded or human-review. **1,401 of them are on `www.cabq.gov`.** Harvest them in slices, starting there | A dated research decision file per slice. Same method: fetch the page, parse for pdf/doc/docx/xls/xlsx/ppt/pptx/csv links, reconcile **by URL against every inventory URL and against every row of every saved artifact**, then fetch and measure what is left. **Yield so far: 754 pages → 461 undiscovered documents.** | first slice complete — `project-state/discovery/urban-design-planning-research-2026-09-14.json` |
| Claude | Continue the third harvest: the remaining **272 new documents** — 81 `fire/documents`, 50 `council/documents`, 38 `planning/DevelopmentReviewServices`, 35 `acs/documents`, 18 `planning/IDO`, 8 `planning/agis` large-format maps and the rest | A dated research decision file per slice, same method. **The fire and ACS departments have not been touched by any lane in this run.** Watch for CSV data extracts — seven are in the batch and a data file is not the same kind of record as a report | fire slice complete — `project-state/discovery/fire-monthly-reports-research-2026-09-15.json` |
| Claude | Continue the third harvest: the remaining **191 new documents** — 50 `council/documents`, 38 `planning/DevelopmentReviewServices`, 35 `acs/documents`, 18 `planning/IDO`, 8 `planning/agis` and the rest | A dated research decision file per slice, same method | active |


## Automatic next lanes

| Owner | Next lane | Deliverable |
| --- | --- | --- |
| Codex | Integrate the saved construction-documents, DevelopmentReviewServices and council-documents research artifacts, then the rail-yard, police-oversight-task-force, councilor-district-5, paseo-del-volcan and redistricting artifacts when available, then the volcano-heights, planned-growth-strategy, form-based-code, nob-hill-highland, impact-fees, councilor-district-2, the inventory URL-collision audit, the council-amendments artifact the meeting-agenda-documents artifact the councilor-district-9 artifact, the councilor-district-4 artifact, the dnasdp artifact, the councilor-district-7 artifact, the councilor-district-1 artifact, the east-central-sector artifact, the councilor-district-6 artifact, the 12th-and-menaul artifact, the completed-reports-studies artifact, the 4th-street-montano artifact, the West Central MRA artifact, the sawmill artifact, the economic-forum artifact, the councilor-district-8 artifact the council-documents topical artifact the cip-documents artifact the municipaldevelopment-procurement artifact the municipaldevelopment-agenda-minutes artifact and the bond-2013-department-set artifact, then any further Claude cluster artifacts as they are saved, then any further Claude cluster artifacts as they are saved | Deterministic inventory application, validation, and checkpoint update |
| Claude | Review a further substantive pending-review cluster after the current one | A dated research decision file with authoritative URLs and duplicate/supersession checks |

## Current shared facts

- 18 DPM Executive Committee minutes are already `approved for addition`; their completed research artifact is `project-state/discovery/dpm-executive-committee-minutes-decisions-2026-09-11.json`.
- The DPM library is now fully triaged. `project-state/discovery/dpm-draft-and-agenda-research-2026-09-11.json` recommends 46 `superseded`, 5 `excluded`, and 6 `requires human review` across the last 57 pending DPM candidates. It writes no shared state and awaits Codex integration.
- The DPM research artifact has been integrated into the master inventory; `Test-MasterInventory.ps1` passes. The six missing-minutes or adoption-uncertain records remain intentionally requires-human-review.
- Anchors used for DPM supersession: `src-0b9dc46fbf32cfb7` (signed consolidated manual, June 2, 2020) and `src-d6f0d94a12b31a2d` (adopted Article 4-3 ABCWUA amendment, May 4, 2026).
- The 2003 and 2004 general obligation bond directories are triaged. `project-state/discovery/early-go-bond-cluster-research-2026-09-11.json` recommends 19 `approved for addition`, 4 `excluded`, and 2 `requires human review` across 25 candidates, with all 25 SHA-256 values distinct from every checksum in the inventory. It writes no shared state and awaits Codex integration.
- The 2003/2004 GO-bond research artifact has been integrated into the master inventory; `Test-MasterInventory.ps1` passes. The 19 approved records remain inventory-only and await explicit authorization for any site placement.
- Precedent applied in that directory: detailed project-scope and allocation tables are retained (`src-e28f489ec0fba72f`, `src-0782d8521e42a068`); high-level percentage summaries are excluded (`src-7405cfc41e5500c4`).
- The 2009 general obligation bond directory is triaged. `project-state/discovery/go2009-bond-cluster-research-2026-09-11.json` recommends 13 `approved for addition`, 16 `duplicate`, 4 `requires human review`, and 2 `excluded` across 35 candidates. It writes no shared state and awaits Codex integration.
- The 2009 GO-bond research artifact has been integrated and its three flagged legacy duplicate relationships were re-adjudicated. `Test-MasterInventory.ps1` passes. The 13 approved records remain inventory-only and await explicit authorization for any site placement.
- **Correctness flag raised by that artifact, for the Codex integration lane.** Three already-terminal records — `src-4204206743b66c6c`, `src-5c9206c81408ce50`, and `src-3ef781d4a038852a` — are duplicates whose recorded canonical is `src-b3d0cd2df96d0bc6` (2009 Community Facilities summary) on the stated ground that the Public Safety, Police, and Family and Community Services schedules are contained within it. Measurement does not support that: the Community Facilities summary contains zero occurrences of "Fire" and zero of "Police", and its three "Public Safety" mentions are an EDACS and microwave communications sub-block. Claude did not modify these terminal records; they need re-adjudication.
- Hash-only duplicate checks are insufficient in the 2009 directory: three pairs are identical in normalized text while differing by two or three bytes of PDF encoding. Use normalized-text and token-coverage comparison as well.
- The Planning UDD library is triaged. `project-state/discovery/planning-udd-cluster-research-2026-09-11.json` recommends 15 `approved for addition`, 6 `superseded`, and 9 `excluded` across 30 candidates. It writes no shared state and awaits Codex integration.
- **Dating rule for historic overlay material.** The Integrated Development Ordinance adopted 2017-11-13 renamed the H-1 Old Town zone and the Historic Overlay Zones to Historic Protection Overlay zones. The City reissued the whole zone-map series on 2018-05-25 and the Old Town standards effective 2018-05-17, so every January 2017 sheet in `planning/UDD` has a direct post-ordinance successor. File names do not reveal this; the printed title and date do.
- Neither text nor hash comparison identifies versions of overlay-zone maps, because their extracted text is only unordered parcel-address labels. All six UDD supersession pairs were established by rendering and reading the sheets.
- **Consistency flag for the Codex integration lane.** `src-011ccfd869be15b6` is validated and already archived to R2 as `maps/cabq-huning-highland-edo-historic-overlay-zone-map-2017.pdf`, but it is the January 2017 Huning Highland/EDo sheet, superseded by the May 2018 map `src-75755583aa85c00c` recommended for addition. Decide the treatment of the 2017 sheets site-wide and the five other recommended supersessions together. Claude did not modify that terminal record.
- The 2011 general obligation bond directory is triaged. `project-state/discovery/go2011-bond-cluster-research-2026-09-11.json` recommends 26 `approved for addition`, 16 `duplicate`, 4 `requires human review`, and 1 `excluded` across 47 candidates. It writes no shared state and awaits Codex integration.
- **Version rule for the 2011 directory.** It already validates both `family_summary.pdf` ("2011–2019 Family and Community Services General Obligation Bond Schedule") and `1family_summary.pdf` (the same schedule "(Initial Version)"). A leading `1` therefore marks an earlier version, not automatically a duplicate: eight `1`-prefixed files matched their counterpart's normalized text exactly and are duplicates, while five differ materially and are recommended as retained initial versions. Title any published initial version the way the validated `1family_summary.pdf` record already is.
- Hashing found only 2 of the 16 duplicate relationships in the 2011 directory. The other 14 — including eight pending files text-identical to already-validated department schedules — were visible only to normalized-text comparison.
- Four abbreviated-filename 2011 variants (`parks_and_rec_summary`, `cultural_summary`, `parks_and_rec_scope`, `cultural_scope`) are materially different from their full-name counterparts but carry no version signal, so they are held for human adjudication as two related pairs.
- The Planning code-enforcement library is triaged. `project-state/discovery/code-enforcement-cluster-research-2026-09-11.json` recommends 5 `approved for addition`, 2 `duplicate`, 1 `requires human review`, and 21 `excluded` across 29 candidates. It writes no shared state and awaits Codex integration.
- **Currency flag for the Codex integration lane.** `src-6e1166ed8ae8f0e5` is validated and published on the public-safety data page as the "Completed Problem Properties Snapshot, March 2024", but the pending `src-07ce09fd200d2d69` is the same cumulative series covering 2019 through a June 2026 entry, so the published snapshot is over two years stale. The directory already set this precedent by superseding the December 2023 snapshot with the March 2024 one. The newer file carries no date in its filename, so any title must state the June 2026 coverage explicitly. Claude did not modify the terminal validated record.
- Candidate URLs in City Plone libraries can serve HTML rather than the expected PDF. Check the leading bytes of each fetch: one 2011 candidate and two code-enforcement candidates are directory listing pages, and the two code-enforcement ones are byte-identical to each other.
- `Development_Application.pdf` exists byte-identically in both `planning/UDD` (`src-7df872bf2933c807`) and `planning/code-enforcement` (`src-77fc6b8921fe85bb`), SHA-256 `136a1bcce639179a6e1a58ad92b17a99b2a56d3984caa263f79e53f16927feca`. Cross-directory duplicates are only visible when both folders are fetched, so keep hashing fetched bytes across clusters.
- The Municipal Development construction-documents library is triaged. `project-state/discovery/construction-documents-cluster-research-2026-09-11.json` recommends 2 `approved for addition`, 6 `duplicate`, 1 `superseded`, 2 `requires human review`, and 8 `excluded` across all 19 pending candidates. Completing it leaves that directory with no non-terminal records. It writes no shared state and awaits Codex integration.
- That directory contains no adopted standards or specifications. It is the permit layer sitting on top of the already-validated City Standard Specifications and Standard Drawings under `documents.cabq.gov/planning/DevelopmentReviewServices/DRC`. The two retained records are the contractor right-of-way permit-requirements packet (`src-1f9234403de1f16f`) and its homeowner counterpart (`src-380887c0fbcc9111`).
- **Hashing found zero of the seven relationships in that directory.** All 19 SHA-256 values are distinct from each other and from all 1,612 checksummed inventory records. Three relationships are PDF/Word format twins, four are whole-file containment inside the retained packet, and one is a genuine version pair.
- **Version-pair signature to reuse.** `src-fb5fde5246712f61` (2001, $5,000, R.O. 1994) and `src-aeb5714315a45302` (2005, $10,000, R.O. 2005) are the same Contractor's Excavation Bond and share 0.9709 of their tokens but only 0.4426 of their normalized text. Re-versioned legal boilerplate keeps its vocabulary and changes the numbers, so token coverage alone calls it a duplicate and sequence similarity alone calls it unrelated. Use both measures. The City publishes both versions on the same page with no version label in either filename.
- **Extension lies, not just content-type lies.** Extend the existing leading-bytes check from "HTML served as PDF" to "does the container match the extension at all". `src-497bd154d2db10c6` is RTF served as `.doc`, which antiword reads as empty; `src-ba39d49eb178e6af` is legacy OLE2 served as `.docx`, which any ZIP-based reader fails on.
- **Filename dates in `municipaldevelopment/documents` are unreliable.** `balloon_fiesta_moratorium_8-20-09.pdf` and `state_fair_moratorium_8-21-09.pdf` both print 2015 periods on the sheet itself; the City replaced the contents in place and kept the 2009 URLs. This is the Municipal Development counterpart of the Planning UDD dating rule: the printed title and date govern, not the file name.
- **Series flag for the Codex integration lane.** `src-09153929967d729f` is terminal-excluded as an undated State Fair moratorium map "with no stated legal authority, period, project list, or implementation record", but `src-b7d34f9ee6422783` and `src-cebad24115e1b02d` are the same two City series and do print their periods. Five more sheets are pending in the sibling directory (`src-3b8d91e803255804`, `src-db6729572f9b4184`, `src-2266ca3b89ee09f0`, `src-d271ba5b038d8770`, `src-7c96ca757c40af55`) and the current state of the series is already on the site via the validated live page `src-9a35226d6d5d0bfc`. Decide the series once, not sheet by sheet. Claude did not modify the terminal excluded record.
- The City library's own collection landing page is the authority for official display titles, which can differ materially from filenames: `barricade-companies.pdf` is "Albuquerque Traffic Control Companies", `Dumpsterpermit.pdf` is "Dumpster_Storage Pod Application", and `ContractorsBond.pdf` is "Contractors Sidewalk Bond.pdf". Parse the landing page before titling anything from a filename.
- The Development Review Services library is triaged. `project-state/discovery/development-review-services-cluster-research-2026-09-11.json` recommends 19 `approved for addition`, 11 `superseded`, 10 `duplicate`, and 41 `excluded` across all 81 pending candidates, with no record left for human adjudication. It writes no shared state and awaits Codex integration. The ledger had recorded 74 pending for this directory; the current inventory holds 81 and all 81 are covered.
- **Five PDFs in that directory have no text layer, and four of them are the most substantive records in it.** Rendering them through pdf.js in a headless browser identified two enacted ordinances and two enacted resolutions that exist nowhere else in the inventory: `src-28372ac121eeb0ba` (O-2018-020, drainage ordinance stormwater-quality amendment), `src-e5b815c2a314724a` (O-2014-024, wireless telecommunications zoning amendment), `src-db74fbcfb6e04b9e` (R-2012-100, CCIP 2012-2022), and `src-f0d6744711099a88` (R-2013-115, CCIP amendment). None of the four enactment numbers appears in any filename. Any cluster triaged only on extracted text will leave records like these pending forever.
- **The Development Review Services inventory records carry no measurements at all.** All 81 have `size_bytes` null and `checksum_sha256` null, and seven also have `direct_file_url` null and are addressable only by `source_url`. The artifact supplies first measurements for every one; Codex should write them into the inventory alongside the status. Do not assume other clusters' records carry sizes either — check before claiming a fetch was size-verified.
- **The adopted Development Process Manual incorporates a Development Review Services file by reference.** Article 6-1 of the signed June 2, 2020 manual states that drainage work must follow the ordinances listed in the "Drainage, Flood Control and Erosion Control Governing Regulations Summary, found on the City website". That is `src-53f3375a9829dc10`, which is why it is retained rather than excluded as a link list.
- **Gap flag for the Codex integration lane.** The site publishes the March 2021 Procedure A and Procedure B Infrastructure Improvements Agreements from the City's `DRC/IIA/` directory. That directory, listed 2026-09-11, contains no Procedure C file, yet the adopted 2020 manual still defines Procedure C at Section 2-3(C)(3) and requires an IIA Procedure C. `src-567244356665a527` is the only published copy and is recommended for addition beside the A and B entries.
- **Filename collision to watch.** `IIA B.pdf` exists twice with different content: the flat `DevelopmentReviewServices/IIA%20B.pdf` is a pre-IDO Procedure B agreement (superseded), and `DevelopmentReviewServices/DRC/IIA/IIA%20B.pdf` is the validated March 2021 edition. Keep full paths in provenance; a basename-keyed check would merge them.
- **Filename dates are wrong again, in a second City library.** Both impact-fee files are named `...-2016.pdf` and are actually 2012 and 2013 enactments. And `ESC plans and Permit in DRC process.pdf` is Revision June 2018 while `ESC plans and Permit in DRC process July2018.pdf` is Revision July 2018, so the plain-looking name is the older file with the wrong disturbance threshold. Treat filename dates across `documents.cabq.gov` as unverified.
- **Storage flag.** `src-d74108ddea65eb7d` is the 586-page joint City/NMDOT/AMAFCA NPDES Manual, Revision 2, August 2012, at 37,140,337 bytes. It is 87 per cent of the 42,485,572-byte footprint of that batch's 19 approved records and should not be folded into a routine upload batch.
- `src-150a847f768d749c` ("FeeSchedule links.docx") is an internal staff worklist headed "Documents to Delete from Server" that the City published to its public library by mistake. Excluded; do not surface it.
- The flat level of the Council document library is triaged. `project-state/discovery/council-documents-cluster-research-2026-09-11.json` recommends 12 `approved for addition`, 6 `duplicate`, 14 `requires human review`, and 75 `excluded` across all 107 flat-level pending candidates. It writes no shared state and awaits Codex integration.
- **Scope correction.** The ledger recorded 52 pending for `www.cabq.gov/council/documents`. The tree actually holds 525 pending across 872 records in 48 subdirectories. The artifact covers the 107 at the flat level only. Remaining subdirectories by size: `21st-century-transportation-task-force` (57, now claimed), `councilor-district-2-documents` (50), `amendments-for-posting-on-council-page` (34), `north-fourth-street-plan` (23), then a long tail of 20-record directories.
- **Every piece of legislation at the flat level carries a blank enactment number.** Seven substitute bill texts and twenty-three one-page amendment sheets, and not one enacted ordinance or resolution. The seven substitutes are held at `requires human review` under the two existing checkpoint blockers (resolve the authoritative enacted package; never present a non-enacted version as enacted). The twenty-three amendment sheets are recommended excluded as non-self-contained fragments — each says only "on page 2, line 12, strike X" without the bill it amends.
- **Category flag for the Codex integration lane.** Those amendment sheets are a deliberate Council publication series, not stray files: 23 here plus 34 more in `amendments-for-posting-on-council-page`, at least 57 in total. Each records the vote with councilors named for and against, which the enacted text does not. Treat the exclusion as one category decision open to revisiting, not 23 independent judgements.
- **The missing-minutes agenda policy has a decisive in-inventory precedent.** `src-0a6bdb5c898342da`, the June 2, 2026 African American Advisory Board agenda, is already terminal-excluded while the minutes of that same meeting were still pending. The May and July 2026 agendas are recommended excluded on the same ground and their three sets of minutes are recommended for addition.
- **Six Trails Public Improvement District agendas and one LGCC agenda are held at `requires human review`, not excluded.** The recorded review covered all 872 council/documents inventory records, the cabq.gov site search, and the `meeting-agenda-documents` directory listing, which holds agendas for the Trails, Mesa del Sol, Saltillo, Winrock and Hunt district boards and minutes for none of them. Legistar and the district administrator's records were not reached, and the agendas prove minutes exist (the May 29, 2013 agenda lists "Review of minutes of August 21, 2012 and February 15, 2013"). Nothing in an agenda shows whether the meeting was cancelled or lacked a quorum, so that half of the policy cannot be applied from these files at all.
- **Architecture flag.** ABQInfo has no page for City board and commission records. Three African American Advisory Board minutes and the April 2026 Council Services organizational chart are approved on the merits but have no good home; a new `content/about/boards-commissions.md` would fit and is a material site-architecture change reserved for user direction. Keep those four inventory-only.
- Hashing did most of the duplicate work in this cluster for the first time in this run: four pending files are byte-identical to already-validated records, including the 8.9 MB Volcano Trails Sector Development Plan and the 11.8 MB Central Avenue Complete Street Plan draft. The two relationships it missed are the Old Town task force colour and greyscale editions of the same ranking results, published as an accessibility pair.
- Lead worth chasing in a later lane: the excluded flyer `src-6db0397455652054` announces a Rainbow Boulevard Traffic Calming and Pedestrian Safety Study between Paseo del Norte and Volcano Vista High School. The study itself is nowhere in the inventory.
- The Rail Yards Advisory Board directory is triaged and is now fully terminal. `project-state/discovery/rail-yards-advisory-board-cluster-research-2026-09-11.json` recommends 17 `approved for addition` and 3 `excluded` across all 20 pending candidates, with no duplicate, supersession, or human-review row. It writes no shared state and awaits Codex integration.
- **That directory is a minute book, not a handout pile.** It is the record of how the Rail Yards master developer was chosen between 2009 and 2012. The single most consequential file is the one-page October 19, 2010 selection committee minutes (`src-280c30eafa8548d7`), which carry the unanimous vote to interview the Samitaur and City View/Paradigm teams and the 9-1 vote rejecting the Albuquerque Station Consortium as non-responsive. The site already archives the master plan those decisions produced in three editions; what was missing was how the developer was picked.
- **The board's own labels govern record kind.** Four 2010 records are headed `FACILITATED MEETING NOTES` on their face and every other meeting record is headed `MINUTES`. The four notes records are retained on the same footing but must never be titled minutes: `src-629bcb5615c018f0`, `src-912c7bc4e5bd5a67`, `src-1c09de804a8080ee`, `src-a6e79304cfd046df`.
- **First full application of the missing-minutes agenda policy with a recorded exhaustive review.** Three agendas whose minutes exist are recommended excluded. Two orphan agendas (May 4 and June 20, 2011) are recommended for preservation, titled "(approved minutes not located)". The review is recorded in the artifact under `exhaustive_minutes_review`: the City's own directory listing returns exactly 20 links matched one-for-one against the inventory, an inventory-wide RYAB search returns only those 20 plus the ULI report, the next meeting's minutes carry no minutes-approval item for either date, and the board demonstrably did approve minutes as a matter of practice (3-14-12 approved 11-14-11; 10-8-12 approved 3-14-12). The artifact also states plainly what the review cannot establish: whether minutes were produced but unpublished, and whether either meeting was held at all, since no agenda shows cancellation or quorum. If that is held to a stricter standard those two records move to requires human review and nothing else in the batch changes.
- **Integrate the Rail Yards and council-documents artifacts together.** The 2008 Urban Land Institute Rail Yards panel report (`src-9f5ac56df8c6cbb8`) is approved in the council-documents artifact from a different directory, and the April 12, 2010 facilitated meeting notes record the board debating how to use that very report in the developer solicitation. They belong on the same page in the same batch.
- Placement note: `content/development-land-use/projects.md` has a "Rail Yards Planning and Environmental Records" section currently holding two items. Seventeen meeting records is a large addition; consider a distinct "Rail Yards Advisory Board Records" subsection rather than appending to the existing list. That is a page-structure choice, not a site-architecture change.
- The Police Oversight Task Force directory is triaged and is now fully terminal. `project-state/discovery/police-oversight-task-force-cluster-research-2026-09-11.json` recommends 8 `approved for addition`, 5 `superseded`, 3 `duplicate`, 3 `excluded`, and 1 `requires human review` across all 20 pending candidates. It writes no shared state and awaits Codex integration.
- **Hashing found zero of the nine relationships in that directory and could not have.** The 238-page final report (`src-f4204196f921104e`) is the canonical package and nine other files are extracts from it or drafts of it. Token coverage inside the report: statement of principles 1.0000, final recommendations 1.0000, report draft 1.0000, January 15 draft recommendations 0.9776, Goal 3 substitute 0.9663, cover memorandum 0.9557, cover-letter handout 0.9528, items-for-discussion handout 0.8949. Two of those needed the sequence ratio as well as coverage to classify correctly, because the text was rewritten rather than copied.
- **Two records in that directory are not task force product and are the most consequential things in it:** `src-5961fb8a4db0fb4e`, the US DOJ Civil Rights Division findings letter on APD of April 10, 2014, and `src-8fa6fb11a3c5125b`, the City/APOA collective bargaining agreement effective July 16, 2014 to July 16, 2015 — the document that determines what civilian oversight can actually reach. The CBA has no text layer and was identified by rendering; its filename says only `NewUnionContract.pdf`, and its term expired in 2015, so any title must state the exact term.
- **Federal-document line now has two worked examples.** A federal publication with no Albuquerque-specific content is excluded (the FCC antenna-safety guide in the Development Review Services artifact); a federal finding about an Albuquerque department is retained (the DOJ letter). Use that distinction rather than a blanket rule about publisher.
- **Cross-directory duplicate for the next lane to resolve.** The same DOJ letter is published twice: `src-5961fb8a4db0fb4e` (born-digital, 46 pages, 242,627 bytes, full text layer) and `src-bdf2981b4899255b` in `councilor-district-5-documents` (scanned image, 48 pages, 4,283,835 bytes, no text layer, first page rendered and confirmed as the same letter to Mayor Berry). Different bytes, different hashes, and the district-5 record carries no checksum, so nothing automated connects them. Make the district-5 copy the duplicate; keep the born-digital one. Claude did not modify that out-of-cluster record.
- **Fourth filename trap of the run.** `POTF Final Recommendations_updated.pdf` is the EARLIER document and `POTF Final Recommendations.pdf` is the later one: the "updated" file lacks the Statement of Principles the other carries as section A, and 0.9944 of its tokens sit inside the other against 0.8750 the other way. Never order two versions by filename in this project.
- Second worked application of the missing-minutes agenda policy: two agendas whose minutes exist are excluded, and the August 6, 2014 agenda is preserved as "(approved minutes not located)" with the review recorded. The March 7, 2014 City Council study session agenda filed in that directory is held at `requires human review` instead, because its own item (a) approves prior study session minutes and so proves Council keeps that series — the policy's precondition that no approved minutes exist is not met, and they are almost certainly in Legistar.
- The District 5 councilor directory is triaged and is now fully terminal. `project-state/discovery/councilor-district-5-cluster-research-2026-09-11.json` recommends 3 `approved for addition`, 2 `duplicate`, 5 `requires human review`, and 10 `excluded` across all 20 pending candidates. It writes no shared state and awaits Codex integration.
- **Enacted legislation is rare in this tree and this directory has two of it.** Across the Council library triaged in this run, thirty-five legislative files carry a blank enactment number and only three carry a real one: `src-fc250f15df2ab028` (Ordinance O-2014-005, Eclipse Aerospace LEDA project), `src-77ded92dfd05478c` (Resolution R-2010-056, Rainbow and Universe Boulevard traffic and pedestrian safety project), and R-2018-081, which survives only because it was bound as Appendix 1 inside a Council staff report. Both of the first two were found only by rendering: neither has a text layer and neither filename mentions an enactment number.
- **Lead closed.** The council-documents artifact flagged that a flyer announced a Rainbow Boulevard Traffic Calming and Pedestrian Safety Study the archive did not hold. R-2010-056 is that project's legislative origin, naming Tierra Antigua Elementary, Tony Hillerman Middle School and Volcano Vista High School as the traffic generators and appropriating Transportation Infrastructure Tax Fund 340 money. The study document itself is still missing and is still worth a targeted search; the resolution supplies the project name, funding source and date to search on.
- **Eleven of that directory's twenty records are scanned paper with no text layer, 55 per cent, the highest share of any cluster in this run, and every record recommended for addition is one of them.** Any triage that runs on extracted text would have called the directory almost empty. Render image-only records before concluding a directory holds nothing.
- **The cross-directory DOJ duplicate is confirmed and is invisible to every automated method this project has.** `src-bdf2981b4899255b` (scan, 48 pages, 4,283,835 bytes, no text) and `src-5961fb8a4db0fb4e` (born-digital, 46 pages, 242,627 bytes) share no bytes and the scan yields no text, so hashing and token coverage both fail. It was settled by rendering page 1. Keep the born-digital copy. A claim that a directory holds no duplicates is only meaningful if its image-only records were rendered.
- **Filename convention does not travel between City libraries.** A leading `1` marks a byte-identical duplicate upload here (`1lewis_additional_info.pdf`), where in the 2011 GO-bond directory the same convention marked a materially different earlier version.
- Five district-5 records are pre-enactment legislation or its exhibit, held under the two standing checkpoint blockers, and three of them are one instrument in two presentations plus its supporting wage-cut scale. Resolve `src-1288d4b8b7490606` (R-09-1, Unser Boulevard extension) first: the roadway projects page already has an Unser Boulevard and Paseo del Norte section, so an enacted version has an immediate home.
- Councilor press releases have a terminal exclusion precedent (`src-0b20b31b78ea3865`) and five more are recommended excluded here. Two excluded records carry durable facts worth keeping as editorial context without an inventory record: a City park was named for Hailey Ratliff, a Tony Hillerman Middle School student killed riding her bicycle to school, and the 2013 GO bond cycle funded a fully signalized intersection at Irving and Universe Boulevards.
- The Paseo del Volcan directory is triaged and is now fully terminal. `project-state/discovery/paseo-del-volcan-cluster-research-2026-09-11.json` recommends 11 `approved for addition`, 1 `duplicate`, and 8 `excluded` across all 20 pending candidates. It writes no shared state and awaits Codex integration.
- **Coverage measurement over-calls duplication in directories of slide presentations, and this artifact records the counter-rule.** Three files sat above 0.83 token coverage inside the retained 86-page corridor study while sharing almost no sequence with it: the November 2014 summary presentation (0.9055 coverage, 0.106 sequence ratio), the project brochure (0.9091, 0.087) and the private landholder presentation (0.8396, 0.047). A slide deck's extracted text is only its headings, so a 500-token corridor vocabulary sits inside a 3,000-token one about the same corridor without the documents being related. **Never call a duplicate from coverage alone when the covered file's token count is an order of magnitude smaller than the container's.** This is the mirror image of the 2011 GO-bond finding: coverage and sequence similarity fail in opposite directions and both are needed.
- Paseo del Volcan currently appears in site content exactly once, inside a description of an MRCOG appendix. The batch would be the corridor's first substantive presence and contains the only corridor economic study, the only right-of-way status record, and the only Double Eagle II Airport development planning record in the inventory.
- **Most of that directory is not City-authored.** The steering committee was convened and minuted by the Mid-Region Council of Governments as the metropolitan planning organisation, and the right-of-way map was prepared for NMDOT by Parsons Brinckerhoff. Attribute each record to its actual author; the City page is the source location, not the provenance.
- **Fifth and sixth date traps of the run, with a named cause.** `src-05705d25113d3f2e` is filed as 11/12/2013 by both its filename and its inventory title; the sheet says November 20, 2013. The right-of-way map's two copies are filenamed 011514 and 04042014; the map's printed date is DECEMBER 2013. In this directory the cause is specific: **handout filenames record the meeting, not the document.**
- Interest-disclosure note: the excluded landholder presentation was made by Western Albuquerque Land Holding and Ranch Joint Venture, and the retained right-of-way map names those same companies as land dedicators at 315 and 178 acres. Keep the map, which records the interest; leave out the advocacy, which asserts a position.
- Third worked application of the missing-minutes policy: three agendas whose meeting records exist are excluded, and the November 20, 2013 first-meeting agenda is preserved as "(approved minutes not located)". The review's stated limit is real — the committee was MRCOG's, so its authoritative minute book is MRCOG's rather than cabq.gov's, and the meeting numbering shows at least one further meeting record is unpublished.
- New lead: the excluded RFP findings memorandum cites a Santolina Fiscal Impact Analysis of May 14, 2013 with a projected net taxable value of $3,213,295,892 at full build-out. Santolina is a major Bernalillo County master plan and that analysis is not in the inventory.
- The 2010 Redistricting Committee directory is triaged and is now fully terminal. `project-state/discovery/redistricting-cluster-research-2026-09-11.json` recommends 10 `approved for addition`, 1 `duplicate`, and 7 `excluded` across all 18 pending candidates. It writes no shared state and awaits Codex integration.
- **ABQInfo has no redistricting content of any kind** — no page mentions it and the inventory holds no redistricting record outside that directory. The batch includes enacted Resolution R-2010-103 creating the committee, two sets of summary minutes, the population estimation methodology, the districting principles, a three-page legal briefing on one-person-one-vote and Voting Rights Act constraints, a council district map, and the adopted-2001 district demographic table.
- **Two records were needed to date one instrument.** R-2010-103's own sheet carries no passage date; the retained interoffice memorandum supplies it ("On August 16, 2010, the City Council passed R-10-109"). Check sibling records before recording a legislative date as unknown.
- **Description constraint on `src-e3deb128206a8c5c`.** The council district map names plan variant G-2-1d Mod 5 Amended but says nothing about adoption, and no adoption record exists in the inventory. Its text layer is only district numbers and precinct labels — the same limitation the Planning UDD lane recorded for overlay-zone maps. Title it as the plan variant; do not call it the adopted districts. The 2001 demographic table by contrast states its adoption date on its face and can be described as adopted.
- **Series-incomplete flag.** That directory stops in December 2010, while R-2010-103 itself recites that census results would not arrive until the first quarter of 2011 and that the municipal election was October 2011. The committee's recommendations, the adopted plan, and any meeting after the third are all absent — and Albuquerque's 2020-cycle redistricting records are absent from the inventory entirely. Both are discovery leads.
- Fourth worked application of the missing-minutes policy, and the strongest evidence yet: the December 2, 2010 agenda is preserved because two of that meeting's handouts survive in the same directory and are retained, which independently proves the meeting was held.
- **Enacted-instrument tally for the Council library after seven cluster artifacts:** thirty-five legislative files carry a blank enactment number; four carry a real one — O-2014-005 (Eclipse Aerospace), R-2010-056 (Rainbow/Universe safety project), R-2010-103 (redistricting committee), and R-2018-081, which survives only because it was bound inside a Council staff report. All four were found by rendering image-only PDFs; none has an enactment number in its filename.
- The Volcano Heights Sector Development Plan directory is triaged and is now fully terminal. `project-state/discovery/volcano-heights-sdp-cluster-research-2026-09-11.json` recommends 19 `approved for addition` and 1 `requires human review` across all 20 pending candidates, with no duplicate, supersession, or exclusion. It writes no shared state and awaits Codex integration.
- **The split-package reconciliation the checkpoint asks for has now been done once, as a worked model.** The checkpoint blocker reads "src-0e133db868401e77 is PGS Part 2 Chapter 10; reconcile the complete split PGS package before any archival or visible use." The Volcano Heights artifact performs exactly that reconciliation for its own plan and records the method: read the printed page range from each part's own running header, never from the filename, then check the ranges join end to end. Result for Volcano Heights: **COMPLETE AND CONTIGUOUS**, pages 1 through 182 with zero gaps and zero overlaps plus front matter, sixteen parts, verified manifest with per-part page ranges and checksums in `split_package_reconciliation`. Apply the same method to the PGS package.
- **Biggest find of the run: the Albuquerque/Bernalillo County Comprehensive Plan.** `src-2f5e70d5a655369e` is a 277-page copy of the Rank I plan as adopted August 1988 and amended through the January 2002 County resolutions, filed in the Volcano Heights directory as a reference. The archive cites this plan throughout and does not hold it: the inventory carries only twelve lineage stubs for it (`lin-13291f6314d11937`, `lin-56d8cb0454ac470d`, `lin-5bde064f99c9f470`, `lin-719dd1a871998539`, `lin-a015dff846665c02`, `lin-a5ac6e5320f82ddb`, `lin-a848983617f571aa`, `lin-b514d37381919aea`, `lin-b6984a912acaa6b9`, `lin-c0f031ceb3a62c28`, `lin-fbae987aedf73234`, `lin-fbc11f5ed0807d12`), all terminal, none with a URL or bytes. Title it with the 1988-to-2002 span: it was superseded by the 2017 Comprehensive Plan adopted alongside the IDO, which is also absent and worth a targeted search.
- **Largest storage decision of the run, and it needs an explicit answer before any upload.** The sixteen plan parts alone total 129,213,922 bytes (about 123 MiB) for one 182-page document; the whole approved batch is 143,148,895 bytes. That is more than twice the 67,526,043 bytes already sitting in the checkpoint's open R2 upload blocker.
- **Provenance rule for split packages.** The City publishes this plan only as sixteen files and no single-file original exists anywhere. Archive the sixteen originals as they are, each with its own verified checksum, and present them as one plan with sixteen parts. Do not recombine and archive the result as the original: a recombination's bytes and SHA-256 match nothing the City ever published, and the AGENTS.md archival standard requires verification against the authoritative source. A combined reading copy, if ever wanted, must be labelled a derived convenience copy kept separate from the archived originals. Either all sixteen parts are archived or none is.
- Placement is already prepared: `content/development-land-use/area-sector-plans.md` has a Southwest Mesa and Volcano Area section holding the Volcano Trails plan and its EPC notice of decision, and a 2014 transportation-amendment entry whose own description names the Volcano Heights plan among those it amends. That plan has been missing from the site.
- `R-06-86` (`src-e9b93dd7ac244a12`), the bill adopting the plan as a Rank 3 Sector Development Plan, carries a blank enactment number and is held under the standing blockers — but unlike every other such case in this run the adoption is independently evidenced (the plan is marked FINAL October 2006, the EPC staff report recommends approval based upon conditions, and a formal response to protests exists). Find the enactment number, not the fact, and do not hold the plan parts waiting for it.
- The Planned Growth Strategy directory is triaged. `project-state/discovery/planned-growth-strategy-cluster-research-2026-09-11.json` recommends 9 `approved for addition`, 6 `duplicate`, 3 `requires human review`, and 1 `excluded` across all 19 pending candidates. It writes no shared state and awaits Codex integration.
- **The checkpoint's open PGS split-package blocker is discharged as a factual question.** The blocker read "src-0e133db868401e77 is PGS Part 2 Chapter 10; reconcile the complete split PGS package before any archival or visible use." The reconciliation is in `blocker_resolution` and `split_package_reconciliation`, and the answer is not what the blocker anticipated:
  - **Part 1 is COMPLETE and a combined original exists.** `Part1.pdf` (`src-9aeb5f621800da58`, 286 pages) is the exact concatenation of the seven chapter files: their PDF page counts sum to exactly 286 and each chapter's token coverage inside it is 1.0000. There is nothing to assemble — retain the combined original and record the seven chapters as duplicates.
  - **Part 2 is INCOMPLETE and has no combined original.** Its table of contents lists eleven chapters; **Chapter 3.0 "Preferred Alternative Summary", printed pages 131–156, returns HTTP 404** (`{"error_type": "NotFound"}`) and exists nowhere in the inventory. It is the summary chapter — the one a general reader would most want.
- **Dispositions for both prior-lane RHR records in that directory (Claude did not modify either).** `src-0bd664780961f148` (Part1-6) → resolve to `duplicate`, canonical `src-9aeb5f621800da58`, coverage 1.0000 identical to its six siblings. `src-0e133db868401e77` (Part2-10) → resolve to `approved for addition` as Chapter 10.0 alongside the eight pending Part 2 chapters; the reconciliation the blocker required is now done and nothing about the record is uncertain.
- **Three files exist on the City server and are absent from the inventory entirely**, all HTTP 200, none in the City's own collection listing: `Part2-4.pdf` (306,971 B, Chapter 4.0, pp.157–168), `Part2-6.pdf` (241,037 B, Chapter 6.0, pp.211–218), and `Part2.pdf` (5,469,413 B). Add the first two as candidates **before any Part 2 archival** — without them the package is missing three chapters, not one. This artifact creates no inventory records.
- **`Part2.pdf` is a filename trap and is NOT the combined Part 2.** Its table of contents reads "PART 2A Legal Parameters of Unification / 2B City and County Financing / 2C Functions, Services and Operations" — it is Part 2 of a City/County *governmental unification* study. Token coverage of Part 2 chapters inside it is 0.56–0.61 against 1.0000 for every chapter inside `Part1.pdf`. The same naming convention means two different things in one directory: never infer a combined-volume relationship from a filename, measure it.
- **First proof in this run that a City collection listing is incomplete against its own server.** The `pgs` landing page lists none of the three undiscovered files; they were found by probing the chapter filename sequence directly. Where a directory's files are numbered, probe the sequence rather than trusting the listing — this is a crawler finding, not a classification one.
- **Presentation rule for a permanently incomplete package.** Volcano Heights set the rule that a partial archive must not present an incomplete document as complete. Here completeness is unachievable, so the answer cannot be to withhold: publish the ten obtainable chapters with an explicit visible note naming Chapter 3.0 and its page range as unavailable from the City. Do not withhold ten chapters because the eleventh is lost.
- The three PGS enabling ordinances all carry blank enactment numbers and are held as one group: `src-f7c7bd5b273def22` (F/S O-02-39(2), adopting elements of the Planned Growth Strategy), `src-68582bc4fe41fb4f` (O-03-132, Infrastructure and Growth Plan), `src-fcbe6a7ebcf916a1` (O-04-9, Land Use Assumptions under the State Development Fees Act). The last connects to already-triaged material: the land use assumptions it adopts are what CCIP resolutions R-2012-100 and R-2013-115 later amend.
- Storage: the nine approved PGS records total 102,272,070 bytes (~98 MiB) and that figure rises once the two undiscovered chapters and Part2-10 are added. With Volcano Heights the two artifacts now propose roughly 240 MB of archival, against the 67,526,043 bytes already awaiting approval in the checkpoint's existing blocker.
- The Form Based Code directory is triaged. `project-state/discovery/form-based-code-cluster-research-2026-09-11.json` recommends 2 `approved for addition`, 10 `superseded`, 1 `duplicate`, 3 `requires human review`, and 4 `excluded` across all 20 pending candidates. It writes no shared state and awaits Codex integration.
- **Four generations of one code, and the generation is readable only from the section number printed in every page header.** October 30, 2007 drafted it as standalone Chapter 14 Article 20; June 6, 2008 folded it into the Zoning Code at §14-16-3-20; February 27, 2009 renumbered it to §14-16-3-22 Form Based Zones; April 2, 2009 produced the final. Filenames do not show that a 2008 file and a 2009 file regulate under different code sections. Ten files are superseded by a later generation.
- **Second consecutive directory where the City's own collection listing is incomplete against its own server.** `fbz_part_a_04_02_09.final.pdf` (1,276,278 B, 59pp) and `fbz_part_c_04_02_09.final.pdf` (718,729 B, 28pp) return HTTP 200 and are absent from both the listing and the inventory. Only Part B of the final generation was ever discovered (`src-05176d26d35effd3`, a prior-lane RHR record). **Until those two are added the archive cannot represent the final form of this code at all.** Seven probes run; five 404s bound the generations at three parts each after G1.
- **Two superseded rows carry `canonical_id: null` because their successors are those undiscovered files** — do not apply `src-4ce0f7387bab68d1` or `src-a94103538515dadb` until the named files are inventory candidates, or the supersession chain terminates outside the inventory. A third null-canonical row, `src-24e65271e6797b78` (the 2007 table of contents), has no file-level successor at all: the June 2008 restructure published no contents page (probe 404). Each row states its `null_canonical_cause`.
- **Whether Albuquerque ever adopted a form-based code is unresolved and is the cluster's most consequential open question.** All three ordinances carry blank enactment numbers and tell a story on their face: `src-26cb2e52a14f606e` (O-07-116, adopt a form based code outright), `src-7bf6d7801a352612` (F/S O-08-58, instead amend the SU-1 Special Use zone to admit Form Based Zones), `src-d6595748f0d531d4` (O-08-58, filenamed a *reintroduction*, implying an earlier attempt did not carry). Note §14-16-3-22 no longer exists — the IDO replaced Chapter 14 Article 16 in 2017 — which bounds the question without answering it.
- **Two records in that directory have nothing to do with form-based code and are the only ones worth retaining.** `src-eb96077b20226b87` is the complete **La Cueva Sector Development Plan, June 2000** (127pp, 13 MB) filed under the bare name `lacueva.pdf`; nothing in the filename, inventory title, or directory says so and it was identified by rendering the cover. `src-20999f89704896c2` is an extract of the **Downtown 2010 Plan** development-and-building-process chapter beginning at printed page 27; the complete Downtown 2010 Plan is nowhere in the inventory, though the site publishes Downtown 2050. **A bare-neighbourhood-name filename in a topic directory may be a whole plan: render before excluding.**
- **The Comprehensive Plan is byte-identical across directories and hashing does find it.** `src-ca30482593edc4c9` (form-based-code) is the same 11,872,609 bytes and SHA-256 as `src-2f5e70d5a655369e` (Volcano Heights), which the volcano-heights artifact recommends as the archive's only copy. Expect further byte-identical copies in other plan directories still to be triaged; they should all resolve to that single canonical.
- **Measurement direction differs by cluster and both measures are always needed.** Here sequence ratios run as low as 0.0379 between files token coverage shows are 0.86 the same material, because each generation was re-typeset — the opposite failure mode from the Paseo del Volcan cluster, where high coverage with low sequence similarity was a false positive. What settled it here was the printed page headers, not either measure alone.
- The Nob Hill Highland directory is triaged. `project-state/discovery/nob-hill-highland-cluster-research-2026-09-11.json` recommends 2 `approved for addition`, 7 `duplicate`, 2 `requires human review`, and 9 `excluded` across all 20 pending candidates. It writes no shared state and awaits Codex integration.
- **Checkpoint blocker premise corrected.** The blocker calls `src-0f4496e4073a5e06` an "isolated legislative amendment/substitute file". It is not isolated: it is one of **nine** floor amendment sheets to R-07-185 sitting in that directory, and the resolution they amend (`src-3b98e92fa018bda2`) is in the same directory. The sheet names its own target on its face — "The Nob Hill Highland Sector Development Plan attached to and incorporated by R-07-185 is amended as follows", August 6, 2007, inserting a solar-access section for R-1 properties west of Washington Street. **Recommended: resolve it to `excluded` with its eight siblings on the non-self-contained-fragment ground; an excluded record needs no enacted package, which closes this half of the blocker. Narrow the blocker to `src-0feec7ebf6650e27` alone.** Claude did not modify the record. What is actually missing is not the amendment's context but plan page 96, which it operates on and which no inventory record holds.
- **A validated R2 object already contained a third of the directory, and its inventory title hides that.** `src-6916bbd03258654b` is titled "Nob Hill Highland Sector Development Plan — Transportation Section" but is 42 pages spanning printed pages 3, 28–53 and 129–140: the whole Movement Systems chapter plus Appendix D. **All eight plan figures in the directory are inside it**, each on its own printed page (13→29, 14→32, 15→33, 16→34, 17→35, 18→36, 19→43, D6→135). Seven are pending and are recommended `duplicate`, keeping 5,305,484 bytes of already-archived content out of the upload queue. **Consider retitling that record**: a lane reasoning from its current title will underestimate what the archive holds — the error this lane nearly made.
- **A third distinct duplicate-detection method.** Hashing finds nothing here (a loose one-page figure shares no bytes with the 42-page object containing it) and text comparison fails too (six of seven figure sheets have no text layer). The relationships were found by fetching the R2 object read-only and locating each figure by its printed page. For image-only extracts of a document the archive already holds, that is the only method that works.
- **Fourth consecutive directory with an incomplete City listing — and the corrective.** `Figure17.pdf` (634,013 B) returns HTTP 200 and is absent from both the listing and the inventory. **It needs no action**: Figure 17 is already inside the validated R2 archive at printed page 35. Unlike the pgs and form-based-code cases, adding it would create a record whose only disposition is duplicate. Rule: probe the sequence, then check the archive before queuing an undiscovered file.
- **The plan is held in four disjoint page ranges from two different generations.** Recommended here: `src-eee3ae1f6b6df008` (printed i–23) and `src-da5e395c4284e4b8` (printed 99–117), both marked **September 2006 Draft** on every page. Already archived: printed 3, 28–53 and 129–140 of the **adopted** text as amended through August 2014. Printed pages 24–27 and 54–98 are in no record anywhere. Any page presenting these together must state that two are draft and two are adopted; neither new record may be titled as the adopted plan.
- The plan's own lifecycle is now documented: adopted 2007 by R-07-185, amended through August 2014, **repealed by R-17-213** (stated on the first page of the R2 object). Both resolutions in the directory are held at `requires human review`: `src-3b98e92fa018bda2` (R-07-185) is an unsigned working copy with COUNCIL BILL NO., ENACTMENT NO. *and* SPONSORED BY all blank — worse than the usual case — and `src-99b19f7854667dca` (R-04-189, sponsored by Martin Heinrich) started the process R-07-185 finished.
- The impact-fees directory is triaged. `project-state/discovery/impact-fees-cluster-research-2026-09-11.json` recommends 8 `approved for addition`, 9 `requires human review`, 1 `duplicate`, and 1 `excluded` across all 19 pending candidates. It writes no shared state and awaits Codex integration.
- **This is the most tractable of the enactment-blocker groups raised in this run — resolve it first.** Nine records carry blank enactment numbers (four `F/S(2) O-04-69/70/71/72` fee ordinances, their four fee-schedule appendices, and `F/S R-04-159`), but unlike the form-based code or PGS groups **the adoption is not in doubt, only the numbers are.** Corroboration already sits in the inventory from earlier lanes: the DRS artifact recommends enacted R-2012-100 (CCIP 2012–2022) and R-2013-115 (CCIP amendment) plus the Feb 2020 Credit Holder Summary showing live balances up to $1,972,877; the PGS artifact holds O-04-9 adopting land use assumptions under the State Development Fees Act, the statutory precondition for these fees. The question is which enactment numbers to cite, and there is a known-good outcome to confirm against.
- **Each ordinance and its appendix is one instrument and must be applied together.** The appendix carries the dollar fee schedule; the ordinance carries the legal text. Each row carries `appendix_record`/`parent_record`. **Never publish an appendix alone** — a fee schedule detached from an instrument whose enactment is unconfirmed is the most misleading output that directory could produce.
- **One genuine gap: the roadway facilities impact fee study is missing** — the largest of the four systems at eight service areas. The Impact Fee Committee's 8-page comments on it survive (`src-e483853343a12296`), which proves it existed; two filename probes 404 and the City listing shows no such study. Three of four fee justifications would be held. The retained methodology summary `src-9aaa5d2d38dc619f` is the only inventory document covering the roadway calculation at all.
- The strongest single record is `src-f1d84e049627a759`, the **Impact Fees Service Areas composite map printed March 15, 2005** — one sheet showing all four service-area systems (Public Safety 2, Drainage 5, Parks 7, Roadway 8), a composite overlay, worked fee examples from $1,332 to $8,751 for a 2,000 sq ft residence by area, and the complete reductions and waivers policy including the MRA exemption and the full affordable-housing waiver in Planned Village and Infill Development Zones. Title it with the March 2005 date: the geography and policy are durable, the dollar figures are historic.
- **Size is not evidence of difference in this archive.** `o132fin.pdf` here (126,466 B) and `o-132fin.pdf` in the PGS directory (25,029 B) are the same five-page Council Bill O-03-132 at different scan settings — coverage 1.0000/0.9916, sequence ratio 0.6964. A fivefold size gap between two copies of one bill. Recommended `duplicate`; its canonical is the PGS copy, itself held at RHR, so **integrate the impact-fees and planned-growth-strategy artifacts together** and answer O-03-132's enactment question once.
- Dispositions for both prior-lane RHR records in that directory (Claude did not modify either): `src-0e84cb9afce66afc` (IFC comments on the drainage study, 10/05/04) → `approved for addition`, completing the set of committee comments for all four systems; `src-0d81ec5c9bdeb726` (IFC memorandum to the EPC on the Component CIP, 3.1.05) → `approved for addition`, giving that plan a continuous record alongside the 2012 and 2013 amendments the DRS artifact recommends.
- Dating caution on `src-afc895972cb92c6b`: filename says 10.18.04, page header says 10/05/04. Date it to October 2004 and no more precisely.
- The District 2 directory is triaged. `project-state/discovery/councilor-district-2-cluster-research-2026-09-11.json` recommends 5 `approved for addition`, 1 `superseded`, 7 `duplicate`, and 14 `excluded` across all 27 pending candidates, with no human-review row. It writes no shared state and awaits Codex integration.
- **Largest single document triaged anywhere in this run:** `src-d31769eae697e92b`, the 302-page, 31,565,710-byte **Downtown Albuquerque Walkability Analysis and Recommendations** by Jeff Speck. Its title page carries no author — the only thing identifying him is the lecture-announcement flyer in the same directory (`src-aa08d71a1dd69370`), which is recommended excluded. **The attribution is recorded in both rows so it does not die with the flyer.** It is 80 per cent of that batch's footprint and needs a deliberate upload decision.
- Also retained from that directory: the Central Avenue Complete Street **performance monitoring summary** (post-implementation measurement is rare in this archive — the inventory holds the ordinance, packet and presentation but nothing on how the street performed); the **Comprehensive Plan Central and Established Urban Areas map**, which pairs with the 1988–2002 Comprehensive Plan recommended in the volcano-heights artifact; the **DNA Traffic Study public meeting presentation of June 5, 2014**, completing a record whose technical half is already validated and implemented; and the **Highland Park Renovation record drawing**, City Project 644294.
- **A fourth duplicate-detection method, and the cheapest: group records by URL.** Seven pending District 2 records were the *same URL* as an already-excluded record — one discovery pass titled files from the writing organisation, another from the link text on the Council page, and both created records. No fetching, hashing or text extraction needed. **Run the URL group-by first in every future cluster, before fetching anything.**
- **That prompted an inventory-wide audit: `project-state/discovery/inventory-url-collision-audit-2026-09-11.json`.** Across all 7,074 candidates, 454 URLs carry more than one record (929 records); 344 of those are document URLs (701 records). Excess beyond one record per URL: **68 overall, 40 on document URLs.**
- **A first pass of that audit reported 296 excess records and was wrong by roughly a factor of four.** One URL — the MRMPO TIP Viewer, a live web application — has 229 distinct transportation projects catalogued against it. That is the live-service category behaving normally, not a collision. **Exclude live-service URLs before reporting any URL-collision figure.**
- **Three actionable subsets, separated because they need different work.** (1) **79 document URLs carry a pending record with a terminal twin** — disposition already settled, inherits, no research needed; twin statuses are 38 excluded, 31 duplicate, 13 validated, 2 superseded. (2) **13 URLs carry two pending records and no terminal one** — not a cheap win: triage the document once and collapse the pair rather than letting two lanes measure the same file. (3) **135 document URLs carry more than one validated or implemented record** — terminal, so outside Claude's lane; **decide first what a validated-and-implemented pair means**, since that determines how many of the 135 are real errors.
- **Effect on a figure the project reports:** the validated count of 1,321 includes 16 records that are second records for a document another validated record already covers — about 1.2 per cent. On a one-record-per-document basis it is ~1,305. Small, but it is the number reported as archive size.
- **Two genuine checksum conflicts**, where one URL served different bytes at different capture times: the 2024 Bikeway and Trail Facilities Plan (two records at 68,233,939 B sharing a checksum, one at 19,715,991 B already at RHR) and the South Yale Boulevard Study Area sheet. Currency questions, not hygiene.
- The Council amendments directory is triaged. `project-state/discovery/council-amendments-cluster-research-2026-09-11.json` recommends 4 `requires human review` and 25 `excluded` across all 29 pending candidates. Nothing is approved, so it proposes no archival and no site change. It writes no shared state and awaits Codex integration.
- **CORRECTION to `council-documents-cluster-research-2026-09-11.json`.** That artifact recommended excluding Council amendment sheets as a category and said the category covers "at least 57 files across this tree" — counting all 29 records in this directory **from the directory name alone, without fetching any of them**. Four of the 29 are not amendment sheets: they are complete substitute bill texts. **Read the figure as at least 48 amendment sheets (23 flat-level + 25 here).** The four substitutes join the seven flat-level ones already held, making **eleven 2024-session substitute bill texts to decide as one group**.
- The four mis-swept substitutes are consequential and three set current City policy if enacted: `src-29b20d60b30299cd` (F/S O-24-10, **63 pages**, authorising up to **$22,500,000** in gross receipts tax improvement revenue bonds), `src-d7c28482bfc9b3c0` (C/S O-24-21, amending the **Albuquerque Minimum Wage Ordinance**), `src-cba160dcbb55f5a1` (F/S R-24-34, repealing Article 9 of Chapter 3 and replacing it with the **Sustainability Resolution**), `src-56f776ae07a23774` (C/S R-24-45, amending the **Immigrant Friendly Policy**).
- **A sharper rule for amendment packets, read off this directory's own terminal records.** Whether a packet is `superseded` or `excluded` turns on one question: **is the enacted instrument identified and held?** Two records here are superseded with validation_status "superseded by signed enacted R-2025-014"; three are excluded as "Isolated 2024 City Council amendment packet without parent-legislation context". Same document type, opposite dispositions. The council-documents artifact excluded on the fragment ground alone and missed this. Adopt the sharper rule going forward — an amendment packet is not inherently worthless, it becomes a superseded predecessor once its parent is held.
- **Verified: zero enacted 2024 instruments exist anywhere in the inventory** (searched all 7,074 candidates for O/P/R-2024-nnn, and per-bill for all 24 bills). So the flip condition is absent and the 25 exclusions stand. Each excluded row carries `parent_enacted_instrument_held: false` so the condition is machine-readable. **If a Legistar attachment audit later captures the 2024 enactments — as the one that produced the R-25-117 records in this directory did — revisit all 25 as supersessions.**
- **Method warnings recorded.** (1) A directory named for a category is not evidence its contents are that category — second occurrence this run, after `lacueva.pdf` turned out to be a complete sector development plan. Classify legislative files by **masthead**, not filename or directory. (2) A loose per-bill match that truncated `O-24-25` to `o-25` and `R-24-22` to `r-22` produced five false "parent" matches (the Menaul MRA enacted amendment, the 2025 IDO, the Menaul MRA plan, R-22-38 records, an Edgewood fact sheet). **Match full bill tokens only.**
- The URL group-by was run first per the audit artifact's rule and found **zero collisions** here. Worth recording: the collision pattern is not uniform across the Council tree, so the check is cheap insurance rather than a reliable yield.
- C/S R-24-45 is the instructive case: the only bill in the directory represented by **both** a substitute (held) and an amendment sheet (excluded), sitting side by side and receiving opposite dispositions. The split is structural, not arbitrary.
- The meeting-agenda-documents directory is triaged. `project-state/discovery/meeting-agenda-documents-cluster-research-2026-09-11.json` recommends 1 `approved for addition`, 1 `duplicate`, and 18 `excluded` across all 20 pending candidates, with no human-review row. It writes no shared state and awaits Codex integration.
- **The lane brief pointed at the wrong policy, and the directory's own terminal record supplied the right one.** `src-00f9dae00dbfbbc4` (Winrock TIDD agenda, already excluded) carries the reason "Meeting agenda lists consideration of two Winrock TID resolutions but contains neither final resolution nor supporting substantive plan" and the status "excluded: agenda only; does not contain the referenced adopted resolutions or substantive planning record". **The substance test — does the agenda contain what it names, or only name it? — is prior to the missing-minutes question and answers these files without reaching it.**
- **This is not in conflict with the AGENTS.md missing-minutes policy.** That policy is permissive ("ABQInfo *may* preserve"), never mandatory. The rail-yards and redistricting artifacts preserved orphan agendas that carried substantive business of their own; these carry none. Same rule, different facts, different outcome.
- **REVISION recommended to `council-documents-cluster-research-2026-09-11.json`.** That artifact held six Trails PID agendas plus one LGCC agenda at `requires human review` pending a series decision, and said its review could not be called exhaustive. It did not have this precedent — the Winrock record is in this directory, not the flat level it triaged. The Trails agendas here are structurally identical to the ones held there (listing Resolutions 2014-01 to 2014-03, containing none). **Resolve the six Trails PID agendas to `excluded` on the substance test citing `src-00f9dae00dbfbbc4`; check the LGCC agenda against the same test first, since this lane did not fetch it.** That closes six RHR rows and the series decision that artifact requested. Claude modified none of those records.
- **The one retainable file in the directory is not an agenda.** `src-1f47a1d06392eecc` has no text layer and an inventory title that is just its filename, but it carries the **full adopted text of the Committee on Guidelines for Negotiations' Open Meetings Act notice resolution for 2014** (NMSA 1978 §10-15-1(B) and (D)), behind a one-page agenda. Found only by rendering — the fourth record this run whose identity was recoverable only that way. That committee sets the City's collective-bargaining guidelines, the process behind the APOA agreement approved in the police-oversight lane.
- **Filename trap.** `bhfsdocs-1209862-v2-hunt_board_agenda.pdf`, inventoried as "HuntBoardAgenda", has a **Quorum at ABQ Uptown Tax Increment Development District** masthead. `BHFSDOCS` is an outside law firm's document-management stamp and the client name in the filename is not the body that met. Where a filename carries a third-party DMS prefix, treat the rest as that firm's internal labelling.
- Third cross-directory text-identical pair in the Council tree, and hashing missed it by **177 bytes**: the March 7 2014 Council study session agenda appears here and in the police-oversight directory. Recommended `duplicate`; canonical is the police-oversight copy, itself at RHR. The near-miss sizes now span 177 bytes, a fivefold gap (O-03-132) and complete divergence (the DOJ scan) — size tells you nothing about identity in this archive.
- Nine distinct public bodies across eighteen excluded agendas; six are tax increment or public improvement district boards seated by Council. Their substance lives in resolutions and reports the directory does not hold.
- **Corrected pending counts for the council/documents subdirectories.** The figures first written here were directory totals, not pending counts. `21st-century-transportation-task-force` is already fully triaged (0 pending of 57). Verified pending counts across the 41 subdirectories that still hold pending records, 417 in total: `amendments-for-posting-on-council-page` (complete), `councilor-district-2-documents` (complete), then `paseo-del-volcan-documents`, `councilor-district-5-documents` (complete), `paseo-del-volcan-documents` (complete), `Volcano Heights Sector Development Plan` (complete), `rail-yard` (complete) and `police-oversight-task-force-documents` (complete) at 20 each, `meeting-agenda-documents`, `councilor-district-9-documents`, `nob-hill-highland` (complete), `form-based-code` (complete), `pgs` (complete) and `if` (complete), `redistricting-documents` 18 (complete), `councilor-district-4-documents` 16.
- Do not take `amendments-for-posting-on-council-page` as a routine next lane: its 29 records are the same amendment-sheet category already decided once in the council-documents artifact, and that decision is flagged there as a single category question rather than per-record work.
- Other Claude cluster candidates outside the council tree: `documents.cabq.gov/planning/online-forms` (31).
- `Test-MasterInventory.ps1` currently passes with zero errors.
- Keep the existing uncommitted work intact.
- **Date rollover.** Every artifact before `councilor-district-9-cluster-research-2026-09-12.json` is dated 2026-09-11 and was produced in one continuous run; this lane and everything after it are dated 2026-09-12. Mixed dates in the discovery directory are expected, not a naming error. Artifacts cross-reference each other by filename for this reason.
- The District 9 councilor directory is triaged and fully terminal. `project-state/discovery/councilor-district-9-cluster-research-2026-09-12.json` recommends 4 `approved for addition`, 1 `duplicate`, 4 `superseded`, 2 `requires human review`, and 9 `excluded` across all 20 pending candidates. All 20 fetched HTTP 200; the inventory held neither a size nor a checksum for any of them, so both are first measurements. It writes no shared state.
- **A district-5 decision in this run was wrong and the correction is recommended, not applied.** `councilor-district-5-cluster-research-2026-09-11.json` held `src-338b0d72594a0aad`, `src-a9d18b1ea8963cad` and `src-a4e93912f58bcb5d` — Councillor Lewis's floor substitute to R-10-58 for the FY2011 operating budget, in two presentations, plus its wage-cut scale exhibit — at `requires human review` under the enactment blockers. The adopted outcome is already held: `src-03b66a5d8f836d44`, "City of Albuquerque Approved Budget, Fiscal Year 2011", is validated, R2-archived and published at `content/city-data/budget-spending.md:109`. District 9 holds the rival Harris/Sanchez substitute to the same bill and it is already terminal — `src-0a2535af77a1ecdc` is excluded with exactly that reasoning recorded. Codex should resolve all three district-5 records to `superseded` with canonical `src-03b66a5d8f836d44`, closing three of the five RHR rows that artifact raised. Claude did not modify them.
- **Standing lesson, applies to every future lane.** Before writing an enactment-blocker row, search the inventory for the *adopted outcome*, not only for the enactment number. A budget, plan or map that was adopted may already be archived under a completely different title on a completely different page, which makes the proposal superseded rather than pending. Two of this lane's four `superseded` rows were established from a terminal record's recorded prose, not from any measurement — the inventory's own reasoning carries findings no comparison will reproduce.
- **Second cluster where the saving is against an object already in R2.** `src-2ea5c8b1ed06eea9` (Tijeras Arroyo Biological Zone Open Space Resource Management Plan, 128 pages, 5,988,865 bytes) is text-identical — ratio 1.0000, full token coverage both ways, same 128 pages — to `src-9696b9222ba27231`, already validated, R2-archived and published at `content/public-works/parks-recreation.md:139`. Different bytes (21,463 fewer archived), different directories, different filenames: nothing automated connects them. Recommended `duplicate`; keeps a 6 MB re-upload out of the queue. What survived the same check is `src-ed3a4d379473def8`, the 88-page EPC record of how that plan was adopted, which the archive does not hold.
- The two District 9 records held at `requires human review` are `src-80dbed6afbf096c3` (R-14-82, 684 acres including the Tijeras Arroyo Bio-Zone) and `src-e90039aa580fda19` (R-14-113, Rancho de Carnué site at Singing Arrow), both blank enactment blocks. R-14-82 is the tractable one: the EPC approval record is in this batch and the plan it adopts is already published as City policy, so only the enactment number is missing. Note that `src-7bf7341bebf83fe6` in the same directory *does* carry its enactment number (R-2014-012) — the Council page posts enacted versions inconsistently rather than never, so a blank block is not evidence the instrument failed.
- Discovery lead: the enacted R-2014-012 amends Resolution R-13-189, Enactment R-2013-054, of 17 June 2013, which the archive does not hold.
- The District 4 councilor directory is triaged and fully terminal. `project-state/discovery/councilor-district-4-cluster-research-2026-09-12.json` recommends 1 `approved for addition`, 1 `requires human review`, and 15 `excluded` across all 17 pending candidates. The handoff row said 16; the directory holds 17 records and all 17 were pending. It writes no shared state.
- **Plone `/view` URL form: fetch both, archive from the raw one.** Every file record in `councilor-district-4-documents` is inventoried as `.../file.pdf/view`, which serves an HTML wrapper, not the file. Both forms return HTTP 200. Fetching only the inventoried URL would have measured the wrapper's size and checksum and called all sixteen records HTML on a leading-bytes check. Each row in that artifact carries `raw_file_url` alongside `authoritative_url`; Codex should archive from the raw form and cite the `/view` form. This is a Plone convention, not a district-4 one — future lanes should strip `/view` before fetching and say so.
- **The district-9 lesson has a limit, now recorded.** Searching for the adopted outcome resolves an enactment-blocker row only when the outcome is itself a document. Memorial M-14-5 (`src-227ada9c59279d76`, Winter and Benton, calling for a City/County/State mental health task force) has a blank enactment block, and the outcome *was* found twice — the joint press release of 27 May 2014 in the same directory (`src-25ffefb6dcf6a735`) announcing the task force was being formed, and the City's live Task Force on Behavioral Health page (`src-531fa27913dddde6`). But an event is not an archivable canonical, so the record stays `requires human review`. Resolve it with the two district-9 resolutions: all three are Twenty-First Council instruments whose adoption is evidenced and whose enacted text is unlocated.
- **`council/documents/task-force-on-mental-health-documents` is empty and needs no lane.** Its listing was fetched (HTTP 200) and the collection body reads "There are currently no items in this folder." `src-c0433b3c564a14ca` is a landing page for nothing. This is the mirror of the nob-hill-highland finding that a City listing can under-report what its own server holds: listings can also correctly report nothing, and only fetching tells the two apart.
- **Proclamations are a settled excluded category, with one title trap.** The district-5 artifact established it on the Donate Life proclamation; the district-4 artifact applies it to twelve more, each rendered and read rather than inferred from its filename (eight are image-only scans). Fifteen inventory records match "proc" in the title: twelve here, the district-5 one, a still-pending Small Business Saturday proclamation on the economic development server (`src-14be4b3e253f0140`), and one false positive — `src-567244356665a527`, "IIA Proc C Standard.pdf", is Infrastructure Improvements Agreement *Procedure* C and is approved for addition. Any title sweep for proclamations must exclude it.
- Substantive find in district 4: `src-60a000a59cfbbaaf`, the **North Domingo Baca Park Master Development Plan, 3 February 2005**, 44 pages, 2,991,248 bytes, born-digital with a full text layer. It is regulatory, not descriptive — Section 8 sends the Multi-Generational Center, Library and Pool Complex to the Environmental Planning Commission for approval and all other park features straight to the Design Review Committee, and it fixes design standards and a conceptual grading and drainage report against Kinney Dam. The archive holds nothing for this park: three published bond records fund it (`content/city-data/capital-spending.md` lines 314, 466, 615 and `content/public-works/parks-recreation.md` line 109) without holding the plan those projects build out.
- Discovery lead from an excluded record: the March 2015 Councilor's Corner reports the Council's adoption of the **Neighborhood Traffic Management Plan**, with an amendment requiring a Department of Municipal Development report-back on the criteria within two years. Neither is held. The newsletter itself is excluded, so this lead exists only in the district-4 artifact.
- **Corrected untriaged remainder under `council/documents`.** After district-4, 39 subdirectories still hold pending records, 129 in total: `dnasdp-documents` 16, `councilor-district-7-documents` 15, `councilor-district-1-documents` 12, `councilor-district-6-documents` 8, `east-central-sector` 8, `12th-and-menaul` 6, `completed-reports-studies` 6, `4th-street-montano-area-improvement-coalition` 6, `West Central Metropolitan Redevelopment Area` 5, `councilor-district-8-documents` 4, `economic-forum` 4, `sawmill-documents` 4, then a tail of 27 subdirectories at 3 or fewer.
- The DNASDP directory is triaged and fully terminal. `project-state/discovery/dnasdp-cluster-research-2026-09-12.json` recommends 6 `approved for addition`, 1 `superseded`, and 10 `excluded` across all 17 pending candidates (the handoff row said 16). It writes no shared state.
- **ABQInfo publishes the superseded draft of the Downtown Neighborhood Area Sector Development Plan.** `content/development-land-use/area-sector-plans.md` line 13 carries the 17 September 2010 working draft (`src-1c078f889adede14`, validated, with Parts 2–4 `src-0a1c11a9bce94067`, `src-1794e14599b5ed91`, `src-86c15e73ab9895c6` consolidated under it). The **EPC Draft of 28 October 2010** — the version actually submitted to the Environmental Planning Commission — sits pending in the same City directory under filenames prefixed with a 1: `src-9f4896d16e30c694`, `src-7231803ce1ad2ff6`, `src-52dc90346e567ac1`, `src-380ac9326e0001af`. Evidence: covers read `DRAFT 9.17.2010` versus `epc DRAFT 10.28.2010`; every page footer differs the same way; the October Part 1's own Section 3 says "The draft Sector Plan was submitted to the Environmental Planning Commission (EPC) on October 28, 2010"; identical structure (66/70/1/28 pages, 165 either way) with normalized-text ratios 0.9828/0.9875/0.9934/0.9895; and the October Part 4 opens on a different section, so the section order changed. Codex should resolve the four September records to `superseded` and repoint the page. Claude modified nothing.
- **The check against what is already held can cut either way.** Every prior cluster used it to keep a duplicate out of the queue; this one used it to find that the held record is the older one. Future lanes should compare a candidate against the published site entry, not only against the inventory and R2.
- The DNA plan's **decision record was in the same folder, unconnected**: `src-7472d7fda76be8bb`, the EPC Official Notice of Decision of 7 April 2011 (Project #1008570 / 10EPC-40063), 18 pages, recommending Council adoption. Its numbered conditions are page-specific edits to the plan text — condition 10 changes a figure on p.101 from 50% to 30%, condition 8 sets usable open space at 360 sq ft per dwelling unit (500 without alley access). Reading the draft without it yields superseded numbers, so the two must be published together. Pages 15–18 are the notification list and carry residents' names and street addresses as published by the City.
- Discovery lead: no **adopted** Downtown Neighborhood Area sector plan is held anywhere. The Notice of Decision recommends adoption to the Council; the Council's adopting instrument for Project 1008570 is missing. It is the only thing that would let the site drop the draft caveat.
- Also flagged: `src-1c078f889adede14` carries no `r2_url` and its own validation note defers archival, so the published sector-plan entry links the live City file only and is **not site-ready** under the standing rule. That holds whichever package ends up published.
- **Two conventions for multi-file packages are in use and should be reconciled.** A prior pass marked DNA Parts 2–4 `duplicate` + "consolidated under" Part 1; `planned-growth-strategy-cluster-research-2026-09-11.json` approved each component on its own row with a `multipart` block naming the canonical visible record. The dnasdp artifact follows the PGS convention, because this cluster now contains real duplicate and supersession findings that a consolidation marker would be confused with.
- The District 7 councilor directory is triaged and fully terminal. `project-state/discovery/councilor-district-7-cluster-research-2026-09-12.json` recommends 1 `approved for addition`, 2 `superseded`, 3 `duplicate`, and 10 `excluded` across all 16 pending candidates (the handoff row said 15). It writes no shared state.
- **Both of that directory's major studies were already covered by published archive objects, and neither was reachable except by fetching them.** `src-4d101bf92cb3992f` (66 pp, 6,852,988 B) is the **FINAL DRAFT** of the Uptown Pedestrian Study whose **FINAL** state is validated, R2-archived and published at `content/transportation/roadway-projects/studies.md:321` as `src-967112fbf63ba98f`: same 27 October 2014 cover date, same 66 pages, token coverage 1.0000 both ways, and 67 of the 149 diff lines are the running footer reading FINAL versus FINAL DRAFT. `src-2251877ee3dc73ba` (58 pp, **45,323,369 B**) is a UNM Community and Regional Planning studio road-diet concept written for the Fair Heights Neighborhood Association, superseded by the City's own `src-a42856962290497a` San Pedro Drive Bike Facility Assessment (validated, archived, published at `content/transportation/bicycling/projects/_index.md:115`), whose one-mile Marble-to-Indian-School project area contains the studio's Lomas-to-Constitution segment. Together **53,453,667 bytes kept out of the upload queue**, against 242,712 added.
- Substantive find in district 7: `src-c8b06367d22b8e24`, **Observations and Recommendations: The Built Environment** (Walkable and Livable Communities Institute for the City of Albuquerque, June 2014, 10 pp, marked DRAFT), prepared for the Complete Streets Leadership Team led by the Healthier Weights Council. An inventory-wide search for walkable, livable or WALC returns exactly one record — this one — and the archive holds nothing from that institute. Canonical page `content/transportation/design-references.md`, cross-listed to corridor studies. It is self-limiting (a short-visit review), so label it consultant observations, never City policy.
- **Condensed-presentation exclusions now rest on a recorded inventory precedent, not a fresh judgment.** `src-03f5d07ef881a5bb` is already terminal with the note that its content "is substantively contained in" the canonical 2014 San Pedro assessment. The district-7 artifact applies that to the 26 August 2014 San Pedro deck and the 12 November 2014 Uptown deck. The Uptown deck was rendered before being excluded: its substantive slides are Parametrix exhibits stamped CONCEPTUAL DRAFT ONLY for locations the report covers in Section IV, and the report carries the counts, crash data and Appendices A–C the deck does not.
- **First four-way exact-hash group in the Council library**, and the one relationship plain hashing did find: `src-4f112d9ff2a63a58`, `src-8279121801007f16`, `src-b1d8491309abf640` and `src-8cb66c7833f15fd7` are one 425,770-byte file under Plone `copy_of_` / `copy2_of_` / `copy3_of_` prefixes. The file is a $150 third-party Transition Albuquerque workshop flyer, so nothing is archived either way; three resolve to `duplicate`, the canonical to `excluded`.
- The single URL collision in district 7 was **already actioned** — `src-842f1c8cdfa8fc1f` is `duplicate` against the validated archived assessment. Recorded because it shows the 79 `actionable_now` groups in `inventory-url-collision-audit-2026-09-11.json` are being drawn down rather than accumulating.
- The District 1 councilor directory is triaged and fully terminal. `project-state/discovery/councilor-district-1-cluster-research-2026-09-12.json` recommends 1 `approved for addition`, 3 `requires human review`, 1 `superseded`, and 8 `excluded` across all 13 pending candidates (the handoff row said 12). Nothing in it duplicates anything already archived — the first cluster in four where the already-archived check came back empty. It writes no shared state.
- **A fourth Council instrument now sits at `requires human review` with its outcome evidenced but its enacted text unlocated:** `src-23a38383e9fff089`, Council Bill **C/S R-08-182** (Eighteenth Council, Ken Sanchez) approving the 2009–2018 Decade Plan for Capital Improvements including the 2009 Two-Year Capital Budget and appropriating from Transportation Infrastructure Tax Fund 340. Enactment block blank. The programme it approves is published in depth on `content/city-data/capital-spending.md`, and line 141 of that page already publishes the **enacted** criteria resolution R-07-12 for the same bond programme — so enacted instruments from this programme are obtainable. Resolve it together with district 9's R-14-82 and R-14-113 and district 4's M-14-5.
- Two floor-substitute working tables travel with it (`src-f8453bfbac0e5e6f` by department, `src-e84ecf5f9ebbee36` by Council district), also `requires human review`. The district-by-district table is the **only place in the archive** where the 2009 capital programme is broken out by Council district with before-and-after figures; the published departmental summaries aggregate by department instead. The amendment-packet rule (no enacted parent held ⇒ excluded) was **not** applied to them: that rule was written for amendment sheets that cannot be read without the bill, and these tables are self-contained.
- Substantive find in district 1: `src-1740488e9a78e215`, **Atrisco Drive Lane Modifications, Central Avenue to Iliff Road**, 13 November 2014 public meeting presentation, 21 pp, 9,492,057 B, prepared by Parametrix. Speeds, crash-cause breakdown, 22 side-street intersections and 100+ driveways, volumes against four-lane and two-lane LOS D capacity, signal-cycle impacts, and a twelve-month pilot restriping proposal. Nothing else for Atrisco Drive NW exists in the inventory — every other Atrisco record is Atrisco Vista Boulevard, a different County road. The district-7 condensed-presentation exclusion does not reach it, because that rule turns on the full study being archived and here there is none; the site already publishes a comparable deck at `content/transportation/bicycling/projects/_index.md:125`.
- **Status convention for version pairs where neither member is retained**, now applied three clusters running and stated once so the artifacts read consistently: byte-identical copies → `duplicate` (a mechanical fact, true whether or not anything is archived); content-differing copies with neither retained → both `excluded`, relationship recorded in prose; `superseded` reserved for cases where the canonical is itself retained or held for review.
- **Worked example of two clusters checking each other.** Two copies of one District 1 press release differ in two lines: `src-a208f1c6551241b1` prints the bill number **E-14-31**, `src-2bf84750e66b6baf` prints **R-14-31**. The Plone filename says the second is the copy; it is the corrected one. The proof is in another directory — `src-7bf7341bebf83fe6`, the enacted Resolution R-2014-012, Council Bill **R-14-31**, Harris and Sanchez, recommended for addition from district 9. Both releases stay excluded; the enacted resolution is the archivable record.
- Three further discovery leads, each named only in an excluded district-1 record: a **2010 Traffic Mitigation Study** for the Atrisco Drive area proposing a roundabout at Hanover and Atrisco that was never built for want of funding (and a current volume of ~10,800 vehicles per weekday); **Council Bill R-14-22** reserving $1.8 million in FY14 for fire and EMS pay increases; and the **interim rules of decorum** adopted for the 8 May 2014 special Council meeting.
- The `east-central-sector` directory is triaged and fully terminal. `project-state/discovery/east-central-sector-cluster-research-2026-09-12.json` recommends 8 `requires human review` and 1 `excluded` across all 9 pending candidates (the handoff row said 8). **Nothing is approved, and that is not a judgment about importance**: the directory is not a plan library but a single East Gateway legislative package — six Seventeenth Council resolutions sponsored by Don Harris plus two exhibit maps — every one carrying a blank `ENACTMENT NO.` block, which is exactly the class the checkpoint holds back. It has no ceremonial material, no press releases, no handouts and no duplicates.
- **The most tractable enactment question found anywhere in this run: R-07-278** (`src-1a09b0a2a4e8fcbd`, C/S R07278, requiring a master plan for a Bio-Zone Preserve in the Tijeras Arroyo). Two independent archive records already treat it as law: `src-ed3a4d379473def8`, the EPC record recommended for addition from district 9, cites "Resolution R-07-278 of 2007" as the mandating instrument; and the plan it required is finished and published — `src-9696b9222ba27231`, the Tijeras Arroyo Biological Zone Open Space Resource Management Plan, validated, R2-archived, live at `content/public-works/parks-recreation.md:139`. Resolve this first; it would let the parks page state the authority behind a plan it already publishes.
- **A live, validated record is missing an enactment number too.** `src-b8dad566165e2a17` is Council Bill R-07-275 designating the East Gateway Metropolitan Redevelopment Area. Its outcome is held and published (`src-42700a168ad3eb38`, East Gateway MRA Plan 2014, `content/development-land-use/redevelopment-plans.md:99`) — and that archived plan reproduces the designating resolution in its own appendix as "COUNCIL BILL NO. F/S R-07-275 ENACTMENT NO. ________", also blank. **Do not treat the pending bill as a duplicate of the plan**: token coverage is only 0.6976 and the tokens outside the overlap are street numbers — the standalone resolution carries the parcel-level boundary description in Sections 2–4 that the published plan does not reproduce in text.
- The other six East Gateway files describe an **interim control regime that expired by its own terms**: F/S(2) R-06-18's Interim Development Management Area ran "until June 1st, 2006", C/S R-07-278's moratorium "until October 1st, 2008", R-07-276 "until October 31, 2008 or until the final consideration by the City" Council of the sector plan. These are historical-record questions, not live-regulation questions, and should sit below the two above.
- **Three documents named in the archive but held nowhere**, all from this cluster: the **2010 East Gateway Sector Development Plan** (named by `src-ed3a4d379473def8`; the adopted successor that closed the interim regime, and the single most useful thing this cluster points at); **Council Bill F/S(2) R-06-67** (C/S R-06-172 names an area "established in" it, so the package as filed is incomplete); and the **staff report that is Exhibit A to R-07-275**, on which its blight findings rest.
- Four Council instruments now sit at `requires human review` with adoption evidenced or their programme published, and only the enacted text unlocated: district 9's R-14-82 and R-14-113, district 4's M-14-5, district 1's C/S R-08-182 — plus the two East Gateway resolutions above, which are better-evidenced than any of them.
- If the East Gateway package ever resolves and only one boundary map is archived, take `src-cd4af1b973f2c583` (5,796,235 B, Attachment 1 to F/S R-06-172, AGIS, dated 15 June 2007, legend distinguishing the East Gateway area from the Near Heights MRA) over `src-1a52f943f87c3eb4` (17,569,920 B, less well labelled, overlapping content).
- The District 6 councilor directory is triaged and fully terminal, and it is the **first cluster in this run to produce nothing at all for either queue**: `project-state/discovery/councilor-district-6-cluster-research-2026-09-12.json` recommends 4 `duplicate` and 5 `excluded` across all 9 pending candidates (the handoff row said 8), with zero approved and zero requiring human review. The whole directory is Zuni Road corridor material derived from a study the archive already publishes in both parts. That zero is a finding, not an incomplete lane.
- **Closes a pair the inventory left half-done.** `src-41dd0953c79a9816`, the Council copy of Zuni Road Study Part II, was already terminal as a duplicate of validated `src-257a9d7a9cd2784a`; the Council copy of **Part I** (`src-b303d3b21a520dc0`) sat pending. It is the same relationship with the same shape of evidence — 133 pages each, ratio 1.0000, coverage 1.0000 both ways, **zero differing lines** in a full line-level diff, 1,485 bytes apart (Part II differs by 1,230). Canonical `src-e7442b03ed708de4`, published at `content/transportation/roadway-projects/studies.md:329`.
- **Read the file before choosing the comparison.** The three Zuni public-meeting notes sheets state their own redundancy: "A flyer and other materials from this meeting are included in Appendix A of the Zuni Road Study report." That sentence prompted the containment test, which came back at token coverage 1.0000 with literal phrase matches present in Part I and absent from Part II. A lane classifying them from their titles would have called them public-meeting records and either archived three redundant sheets or excluded them for the wrong reason.
- **Sparse-text coverage still over-calls, and rendering still settles it.** The three Zuni cross-section sheets scored 0.9268–0.9750 token coverage on 33–41 tokens, which proves nothing. Rendering showed they are the study's own typical-section figures with a street photo added — one captioned "ALTERNATIVE 1B – ROAD DIET BY RECONSTRUCTING CURB AND SIDEWALK", a phrase the study uses thirty times. Excluded as public-information handouts.
- Seven relationships in that directory and **hashing found none of them**; all seven came from comparing candidates against R2 objects already held. 9,057,916 bytes kept out of the upload queue, nothing added.
- Discovery lead: the 19 August 2014 meeting packet (`src-32a617f4e28735ba`, excluded) identifies a **distinct later project** — Zuni Road Improvements, Washington Street SE to Central Avenue NE, ~2.9 miles, **Project #592691, CN A300655**, City with NMDOT and FHWA — and reproduces Figure 1 of a Marron and Associates document for it. Nothing for CN A300655 is held. The archive has the 2011 planning study and not the project that followed it.
- Second consecutive councillor directory whose single URL collision was **already actioned** (`ZuniGeometricAnalysis.pdf`, implemented + validated `src-66553f9534aa579f`, published at `studies.md:341`), after district 7's. The URL-collision audit's actionable set keeps drawing down.
- The `12th-and-menaul` directory is triaged and fully terminal. `project-state/discovery/12th-and-menaul-cluster-research-2026-09-12.json` recommends 1 `approved for addition`, 1 `duplicate`, 4 `requires human review`, and 1 `excluded` across all 7 pending candidates (the handoff row said 6). It writes no shared state.
- **Largest single find of the run, and the check that licensed it was a negative.** `src-a3d3af52e6832110` is the **12th & Menaul Study, October 2005**, by HOK Planning Group and Hall Planning & Engineering, prepared for the City — 63 pp, **59,048,492 B**, planning, design and transportation recommendations for the area around 12th Street NW and Menaul Boulevard including the former Indian School site. The obvious risk was the archive's Menaul holdings; fetching the archived 2023 Menaul MRA plan showed it does not contain the string "12th" anywhere in its 7,538,874 bytes, and token overlap is 0.5558 — vocabulary only. Two efforts, one street name, sixteen years apart, no overlap. Canonical `content/development-land-use/area-sector-plans.md`, cross-listed to redevelopment plans and corridor studies. **Plan this upload rather than batching it**; there is no smaller edition.
- **An ordinance that does not name itself.** `src-8a1f90911b6038b3` has blank COUNCIL BILL NO., ENACTMENT NO. *and* SPONSORED BY lines. It is identifiable as F/S O-05-98 only from the headings of its own exhibits ("Bill No. F/S O0598 — Exhibit C") and from the City's filenames. Any automated title sweep over that directory will mis-name it; record the bill number as derived, not stated.
- **Five enactment packages now sit at `requires human review` across this run, and they should be worked in this order** — each of the first four has a published outcome to work back from, the fifth has none: (1) East Gateway **R-07-278** (EPC record cites it as law; the plan it mandated is published); (2) East Gateway **F/S R-07-275** (the MRA plan it directed is published, and reproduces the bill with a blank block of its own); (3) district 9 **R-14-82** (the plan it adopts is published) with **R-14-113**; (4) district 1 **C/S R-08-182** (its bond programme is published, and the enacted criteria resolution R-07-12 for the same programme already is too) and district 4 **M-14-5** (its task force was formed); (5) **F/S O-05-98** at 12th and Menaul — the adopted outcome would be the zoning itself, and the inventory records nothing about this site's zoning, so there is nothing to work back from. Resolve it last.
- Exhibit B1 to O-05-98 (`src-3c28a0575f8c4b78`, 23 pp) is 23 pages of the HOK study — coverage 0.9774 with three literal phrase matches — so it is `duplicate` of the study. **Its legal role is not captured by that status**: as an ordinance exhibit these pages would have been the operative design standards rather than advisory. If O-05-98 ever resolves as enacted, note that on the study; do not archive the excerpt separately.
- The other three O-05-98 exhibits are unheld elsewhere and carry real regulatory content: Exhibit C (`src-a1731c6529388425`) is the C-2 permissive-use exclusion schedule, which the design-led study does not contain; Exhibits A1 and A2 (`src-31ba73e0c3ce56f8`, `src-13ccd9b9108bee02`) are DCSW sheets SP-01 and SP-02 of 11 August 2005 — site plan (Commercial Tract 15.69 ac, Office Tract 31.69 ac, total 47.38 ac) and building height and setback plan (44 ft / 56 ft / 84 ft maxima, measured to the roof deck). Note the sheets cover 47.38 acres while the ordinance rezones 19; they are not coextensive.
- The `completed-reports-studies` directory is triaged and fully terminal, and it is **the densest directory of unheld substantive material found in this run**. `project-state/discovery/completed-reports-studies-cluster-research-2026-09-12.json` recommends 6 `approved for addition` and 1 `excluded` across all 7 pending candidates (the handoff row said 6). Six City Council-commissioned reports from 2001–2002, **699 pages, 18,383,354 bytes**, every one born-digital with a full text layer: Parking System Review (Carl Walker, Oct 2002, 161 pp); Analysis of Albuquerque's Industrial Revenue Bond Program (Prager Company with NatCity and Teresa Córdova, Apr 2002, 56 pp); IRB Program: Community Stakeholder Perspectives (Córdova, May 2002, 46 pp); Police Oversight Project (Richard Jerome P.C. / Police Assessment Resource Center, May 2002, 116 pp); APD Total Compensation and Overtime Analysis (Watson Wyatt, Mar 2001, 44 pp); and Structuring A New Urban Government: unification of Bernalillo County and the City (David Rusk + Unification Exploratory Group, 12 Nov 2002, 276 pp).
- **The brief expected duplication and found none.** Searches for parking system review, Carl Walker, industrial revenue bond, Prager, police oversight project, Police Assessment Resource Center, Richard Jerome, Watson Wyatt, total compensation, unification, urban government and David Rusk — run over every inventory field and over all of `content/` — return one unrelated news item between them. Zero checksum collisions. The likely reason nothing found these before is the filenames: `parking.pdf`, `irbrpt.pdf`, `irbrpt2.pdf`, `pocstudy.pdf`, `UEGreport.pdf`, `APDcompensationstudy.pdf`.
- **Resolves an open item from the PGS artifact.** `planned-growth-strategy-cluster-research-2026-09-11.json` found `pgs/Part2.pdf` — absent from both the City listing and the inventory — established it was not the combined Planned Growth Strategy Part 2 but "Part 2 of a City and County governmental unification study", and recommended adding it as a new candidate under its true identity. It is 144 of the 276 pages of `src-a5475f489b373f20`, the complete unification report in `completed-reports-studies`: token coverage 1.0000 inside it, 0.7239 the other way, and the same three part titles (2A Legal Parameters, 2B City and County Financing, 2C Functions, Services and Operations). **Do not create a separate candidate for `pgs/Part2.pdf`** — archiving the complete report covers it. The recommendation to add `Part2-4.pdf` and `Part2-6.pdf` as genuine PGS chapters is unaffected.
- **Why that match was possible:** the PGS artifact recorded *what the unidentified file's table of contents said*, not merely that it was unidentified. Two clusters later that content matched a cover. Recording the substance of an unknown file is what made the identification possible — worth repeating whenever a lane finds something it cannot place.
- **Coverage gap closed: industrial revenue bonds.** IRBs are the City's largest business-incentive instrument and the archive explains them nowhere; the only IRB record in the inventory is a pending news item (`src-d3176451f1bc5245`). The two 2002 studies were commissioned together — one quantitative, one stakeholder-interview based, sharing an author — and should be published as a pair, labelled 2002 analyses rather than current programme rules.
- **Date cautions carried on three of the six.** The 2002 Police Oversight Project is the earliest independent assessment of Albuquerque police oversight anywhere in the inventory and long predates the 2014 DOJ findings — publish it as a baseline, never as current oversight. The 2001 APD compensation figures are historical only. The unification report explores a merger that never happened.
- **Placement gap flagged, not decided quietly:** the site has no page for City government structure. The 276-page unification report is proposed for `content/city-data/budget-spending.md` because Part 2B is a revenue and financing analysis, but a governance page would be a better home if one is ever created.
- The `4th-street-montano-area-improvement-coalition` directory is triaged and fully terminal, and it is **the second directory in this run to come back empty for both queues** (after district 6). `project-state/discovery/4th-street-montano-cluster-research-2026-09-12.json` recommends 2 `duplicate` and 4 `excluded` across all 6 pending candidates. The corridor is already covered by seven archived or held records — the 2004 visioning report, the Rank III corridor plan, its background and resource materials, existing-zoning and form-based overlay maps, the North Fourth Street MRA plan, and enacted R-17-160 at `requires human review`.
- **Two `actionable_now` URL twins closed in one six-record directory**, the densest concentration seen, and both confirmed byte-identical rather than inferred from the URL match: `src-4d604937d6910980` = validated `src-173ce250cdac7c01` (1,246,032 B, sha 6a50f38b…); `src-f90f45e9692ffc1a` = already-superseded `src-48a9681a27727130` (34,703,589 B, sha 558ce8f2…). 35,949,621 bytes kept out of the queue. Three consecutive directories have now each contributed at least one resolved group — the audit's 79-group set is being worked down, not accumulating.
- **Second instance of the `pgs/Part2.pdf` naming trap.** A 34.7 MB draft of the *North Fourth Street Rank III Corridor Plan* is filed as `4th_street_and_montano_area_improvements_draft_plan.pdf` inside a neighbourhood coalition directory — neither the filename nor the location identifies it. Its terminal twin already records this. Identity comes from the file, never from where it sits.
- **Coverage alone has still never settled a case in this run.** The four 4th Street process files scored 0.6349–0.8750 against the archived corridor plan and background materials on token sets of 126–298 — the sparse-text over-call band — and no literal phrase from any of them appears in either object. They were excluded as public-involvement working material, not called duplicates on a number that would not support it. Third consecutive cluster where rendering or phrase-matching, not coverage, decided the question.
- Recorded because the file is excluded: `src-a7e8a65f2a77cbaa` (survey results) is **the only quantified statement in the archive of what residents said about the Fourth Street corridor** — charted concerns about pedestrian conditions, commercial development, transients, unsafe turns, neighborhood character, labor finder and tattoo businesses, and rural character, with responses up to about 45. The categories are preserved in the artifact row so the content survives the exclusion.
- The `West Central Metropolitan Redevelopment Area` directory is triaged and fully terminal. `project-state/discovery/west-central-mra-cluster-research-2026-09-12.json` recommends 1 `superseded`, 3 `requires human review`, and 2 `excluded` across all 6 pending candidates (the handoff row said 5). Nothing approved.
- **METHOD CHANGE, applies to every open enactment question: enactment numbers live in the recitals and title lines of *later* legislation, not in the plans those instruments adopted.** Two numbers were recovered this way in one small directory: `src-1564eaa66bce3d8c` states in its own title "AMENDING F/S R-04-56, **ENACTMENT 66-2004**", and F/S R-04-56 (`src-be4df966deebed35`) states in its recitals "Council Resolution No. F/S R-2-16, **Enactment 82-2001**". Search Council legislation that amends or cites an instrument, or the City Clerk's enacted series — not the plan it produced.
- **Why the plan appendices are the wrong place, now verified twice inside records that are already validated, R2-archived and live.** `src-9696b9222ba27231` (Tijeras Arroyo Biological Zone Open Space RMP, published at `content/public-works/parks-recreation.md:139`) carries "Appendix A — City Council Resolution" reproducing **C/S R-07-278 with its ENACTMENT NO. line blank**; `src-42700a168ad3eb38` (East Gateway MRA Plan, published at `content/development-land-use/redevelopment-plans.md:99`) reproduces **F/S R-07-275** the same way. The City appends the *pre-enactment bill text* to its adopted plans. Do not treat a plan appendix as evidence of enactment.
- A sweep of **every text fetched across this entire run** for stated enactment numbers found more than twenty, but **none attaches to the five open packages** (R-07-278, F/S R-07-275, R-14-82, R-14-113, M-14-5, C/S R-08-182, F/S O-05-98). R-14-82 does independently confirm the *fact* of adoption for R-07-278 — "WHEREAS, the Albuquerque City Council approved Resolution R-07-278, mandating the City of Albuquerque to develop a Tijeras Arroyo Bio-Zone Preserve" — without giving the number.
- **`src-be4df966deebed35` (F/S R-04-56) is now the best-evidenced enactment question in the run and should go to the front of the queue**, ahead of the five whose numbers are unknown: the plan it adopts is validated, archived and published (`src-56485de565b52365`), *and* its enactment number 66-2004 is stated outright. It adopted the West Central MRA Plan, included the entire MRA for tax increment financing, and appropriated $200,000 each from the Metropolitan Redevelopment Fund and the Community and Economic Development Reserve Fund.
- New discovery lead with its enactment number already attached: **Council Resolution F/S R-2-16, Enactment 82-2001**, the instrument that designated the West Central MRA on slum and blight findings. Held nowhere in the inventory — the rare case where a missing document can be requested by its enacted number.
- **Size is not a recency signal**, now seen twice: the superseded January 2004 *final draft* of the West Central MRA Plan (`src-84d8d553f41dc893`, 93 pp, **21,139,298 B**) is 6 MB *larger* than the adopted June 2004 final already published (103 pp, 15,035,247 B); the same inversion appeared in district 7, where the superseded Uptown draft was larger than the published final. The two West Central editions are told apart only by page footers and the embedded source path — "Final Draft January 2004" over `W CENTRAL MRArpt6-03.doc` versus "June 2004" over `Final Report\W CENTRAL MRArpt-final6-04.doc` — and the revisions are substantive: the draft's "Mexican specialty businesses" and "Mexican and other ethnic specialty retail items" became "ethnic specialty businesses" and "ethnic specialty retail items" before adoption. Anyone quoting the draft would attribute revised wording to the City.
- Governance record worth publishing if its package resolves: `src-1564eaa66bce3d8c` (FS-R-04-188 (4)) puts City redevelopment funding under the management of a named neighbourhood association and accepts a program of association between the Alamosa Neighborhood Association and the West Central Community Development Group. The archive records no such delegation anywhere.
- The `sawmill-documents` directory is triaged and fully terminal. `project-state/discovery/sawmill-cluster-research-2026-09-12.json` recommends 1 `requires human review`, 3 `superseded`, and 1 `excluded` across all 5 pending candidates (the handoff row said 4). The whole directory is **one document in four releases** — Exhibit A to the Sawmill/Wells Park Sector Plan Amendment, the Land Use and Transportation Section with the Master Circulation Plan — dated February 15, October 27 (two releases) and November 10, 2008.
- **A blank adoption line is the same blocker as a blank enactment block, reached through an exhibit instead of a bill.** The canonical `src-96e192e3bb793470` reads "Master Circulation Plan (Adopted ___________)" and states which subsections of an adopted sector plan it overrides — Section 2 subsections 2.b, 2.c, 2.d and Section 3 subsection 3.d. Publishing it would present unadopted street classification and design regulation as if it governed. Held at `requires human review`; it is worth publishing if adoption is established, since 42 pages of connectivity and street-design regulation for Sawmill and Wells Park exist nowhere else.
- The enactment-number method had **nothing to work with** there: the directory contains no legislation at all, so there are no later recitals to read. The Sawmill/Wells Park **Sector Development Plan** is not held in any form, nor is the instrument that adopted the amendment. The archive's only Sawmill planning record is a different instrument under a different statute — `src-6d2bd024ade8862e`, the 2005 Sawmill/Wells Park Community MRA Plan, published at `content/development-land-use/redevelopment-plans.md:183`.
- **Size is not a recency signal — third confirmation, and the sharpest.** The canonical November 10 final is the *smallest* file in the directory at 8,448,729 B; the superseded October release is 10,433,789 B and its re-encoded twin 14,796,082 B.
- **Two files with exactly equal extracted text and no byte overlap**, 4,362,293 bytes apart: `src-4de7fbdd147c8c66` and `src-e54536e42de9edcc`. Hashing found nothing; normalized-text comparison found they are one release re-encoded. The clearest demonstration in the run that byte comparison and content comparison answer different questions — and the reason neither was called a `duplicate`, since that status is reserved for byte-identical copies.
- **The City's leading-"1" filename prefix is not a recency signal.** It marked the *newer* EPC package in `dnasdp-cluster-research-2026-09-12.json` and an *older* release's fatter re-encoding here. Compare the files; do not read the prefix.
- The last release was not a cosmetic pass: besides fixing garbled typography ("2.aEstern Gateway" → "2. Eastern Gateway", "rFontage" → "Frontage"), the November final **removed two map annotations** present in October — an "Interim Connection Bellamah Ave NW between Aspen Ave and Arbolera de Vida" and a "Temporary connection to connect Sawmill Land Trust property and 12th Street Corridor". Anyone citing an October release would describe road connections the City dropped.
- Related pending record for whoever revisits District 2: `src-385036989b8d1925`, "EPC Notice for Sawmill Wells Park Hearing" — a hearing notice is the kind of record that would date this amendment's adoption.
- The `economic-forum` directory is triaged and fully terminal. `project-state/discovery/economic-forum-cluster-research-2026-09-12.json` recommends 2 `duplicate` and 3 `excluded` across all 5 pending candidates (the handoff row said 4). Nothing approved.
- **NEW FAILURE MODE — content substitution, and an HTTP 200 does not rule it out.** Three City URLs — `Sirolli.pdf`, `Central_New_Mexico_Competitiveness_Summit.pdf` and `Florida_Summary.pdf` — return **HTTP 200 at their original addresses with zero redirects**, serving the same 4,926 bytes of HTML: the homepage of *Next Generation Economy, Inc.*, a private organisation. All three carry SHA-256 `12983112…`, and their leading bytes are `3c21444f` (an HTML doctype) where a `.pdf` URL promises `25504446`. A control request for `ZZZnotreal.pdf` in the same directory correctly returns **404**, so the server does distinguish missing objects: these exist and their stored content is wrong. **A status-code link check would call all three healthy; the leading-bytes container check caught them on the first fetch.** Keep verifying containers by leading bytes, never by extension.
- **Corrects a terminal record's stated reason.** `src-0b6ab4d6ffe9f903` is excluded as "Florida Summary (official City endpoint unavailable)". The endpoint *is* reachable and returns 200 — it serves the substituted third-party page. The exclusion is right, the reason is not, and it matters: a future availability re-check will see 200 and may try to recover a document that is not there. Codex should restate the reason as content substitution. Claude did not modify the record. Record the substitute's checksum on any such row so repeat fetches are recognised rather than re-investigated.
- **A byte-collision group is not always a duplicate group.** The three substituted pages collide with each other perfectly. Recording that as a duplicate set of documents would be exactly backwards — it is evidence that three *different* documents are missing behind one replacement page.
- The two 2002 Industrial Revenue Bond studies are published **twice**, in `economic-forum` and `completed-reports-studies`, byte-identical under the same filenames `irbrpt.pdf` / `irbrpt2.pdf` (1,082,660 B sha dec4d683…; 448,341 B sha 40b90b2f…). Catching this needed comparison **against the batch in progress**, not only against terminal inventory state — the canonicals are still `pending review` in `master-inventory.json` because this run writes no shared state. **Codex: apply `completed-reports-studies-cluster-research-2026-09-12.json` first, or both together**, so the canonicals are not left pending behind their duplicates.
- **Third directory whose City listing under-reports its own server**, after nob-hill-highland and PGS: the economic-forum index lists four items and omits `Sirolli.pdf`, which exists as an object and returns 200. The twist is that the unlisted file is one of the substituted ones — completeness of discovery and integrity of content are separate problems, and filename-sequence probing needs to be paired with a container check.
- The District 8 councilor directory is triaged and fully terminal. `project-state/discovery/councilor-district-8-cluster-research-2026-09-12.json` recommends 1 `approved for addition` and 4 `excluded` across all 5 pending candidates (the handoff row said 4). All containers checked out — four genuine PDFs and one genuine collection page, no substitution.
- Substantive find: `src-1b34668e04121332`, the **Academy Hills Park** improvement plan — one large-format sheet (6,742,283 B, image-only, rendered) carrying the whole project: scope, **$900,000 CSA** funding, schedule (design 2013, construction early 2014, complete Fall 2014), a legend keying a 1.25-mile perimeter path and 0.5-mile loop with benches, picnic table, mutt mitt dispensers, new trees and plantings, and engineering callouts including a note that changing existing blue grass on slope to a native turf blend conserves water. The archive holds **nothing** for this park. Canonical `content/public-works/parks-recreation.md`, cross-listed to capital projects. It has no text layer, so the description and evidence fields carry the content OCR would otherwise be needed for.
- **The missing-minutes policy used to rule an agenda out rather than in.** The Northeast Area Community Policing Council agenda (`src-6cf78aff0597bce8`, 16 March 2015) defeats its own case: item two is "Approval of February Meeting Minutes", so that body takes and approves minutes and the agenda is not a substitute. Discovery lead: the CPC's **approved minutes and its operating procedures** are held nowhere, and the agenda names both.
- Recorded because the file is excluded: the Academy Hills press release carries counts the plan sheet does not print — eleven benches including **five publicly sponsored 'Tribute Benches' with commemorative plaques**, two picnic tables, twenty-seven trash cans, six mutt mitt dispensers. Intent, not outcome; nothing in the directory records what was built.
- Checked so an exclusion was not made on a false assumption: the archive's bear-named holdings (Bear Canyon Arroyo Resource Management Plan, Arroyo del Oso and Bear Canyon trail guides) are **place names on the Bear Canyon arroyo**, unrelated to the excluded BearWatch wildlife flyer.
- **Remaining `council/documents` work is one lane, not twenty-seven.** After district 8, 35 pending records remain across 27 paths, none holding more than three — and several of those "paths" are single files sitting directly at the flat level (`R211csfinal.pdf`, `natffinalrpt2.pdf`, six Trails PID agendas, and so on). A separate artifact per path would be disproportionate, so the tail is claimed as a single sweep with per-path grouping preserved inside the artifact.
- **CORRECTION to the tail count I recorded one lane earlier.** The `council/documents` remainder is **105 pending records, not 35**. The earlier figure came from a regex that required a trailing slash after the directory segment, which silently dropped every file sitting directly at the flat level — and those are the majority. Any future count over this tree must match flat-level files as well as subdirectories.
- **The tail splits into three natural lanes rather than one sweep**, and is claimed that way: (A) **63** topical records — the small named directories (Huning Highland 4, four-hills speed humps 4, complete-streets 3, south-yale-sector-plan 3, plus two-record directories for South Yale Blvd, Uptown, big-box regulations, district 3, the Neighborhood Traffic Management Plan, the balloon landing site purchase, tax cuts, high-performance buildings and red light cameras) together with substantive flat-level files including the Volcano Trails SDP, its EPC Notice of Decision and resolution, the ULI Rail Yards advisory panel report, the Neighborhood Task Force final report and the 2013 Central Complete Street Plan draft; (B) **30** records of a **2023–2026 legislative amendment series** (`o-*`, `r-*`, `m-*`, `p-*` approved committee substitutes, committee amendments, floor amendments and floor substitutes); (C) **12** advisory-board agendas and minutes (Trails PID, AAAB, LGCC).
- Lane B is **not** a foregone conclusion. The amendment-sheet exclusion was established on a 2004–2014 series; this one is newer, its filenames advertise *approved committee substitutes and floor substitutes*, and `council-documents-cluster-research-2026-09-11.json` already had to be corrected once because 4 of 29 files in an amendments directory turned out to be complete substitute bill texts. The lane brief says to test the category, not assume it.
- Lane C is the first directory group in the run where **both agendas and minutes are present** for the same bodies (AAAB has May, June and July 2026 minutes alongside May and July agendas). That is the configuration the missing-minutes policy was written for, and it should resolve cleanly rather than by exception.
- The `council/documents` topical slice is triaged and fully terminal. `project-state/discovery/council-documents-topical-cluster-research-2026-09-12.json` recommends 10 `approved for addition`, 7 `duplicate`, 1 `superseded`, 6 `requires human review`, and 39 `excluded` across all 63 pending candidates. **37,709,066 bytes kept out of the queue; 17,911,791 bytes / 327 pages added.**
- **Third terminal-or-published decision in this batch that needed revisiting, and the sharpest.** `src-049105295d2745af` is excluded as a "Metadata/view wrapper alias for the canonical Uptown Sector Development Plan record `src-4ccef0c6ec25aac8`, already archived and validated." That canonical is **not the plan** — it is a **13-page** extract titled *Uptown Sector Development Plan — Transportation and Connectivity*, 3,293,254 B, from the DPM cross-sections server, published on `content/transportation/transportation-plans.md`. The adopted plan is **115 pages**, 4,133,982 B. Coverage of the extract inside the plan is **0.9797**; of the plan inside the extract, **0.2335**. A roadway cross-section appendix was recorded as the canonical for an adopted sector plan, so **the full adopted Uptown SDP is held nowhere**. It is recommended for addition (`src-2aa87993b25de330`); Codex should correct the terminal record's reason. Claude did not modify it.
- **Seven candidates were byte-identical to validated R2 records and hashing found every one** — two Central Avenue Complete Streets drafts (Oct 2013, Mar 2014), all three Volcano Trails records (SDP, adoption resolution, EPC Notice of Decision), and two South Yale sheets. Confirmation came from the inventory's *own recorded checksums and sizes*, so none needed an R2 fetch. An eighth (the split Central Part 2) matches a checksumless terminal record by size and is recommended `superseded` rather than `duplicate` for exactly that reason.
- **The enactment-number method worked a second time, in an unrelated directory.** Council Bill **R-05-383** recites its predecessor's number three times — "BILL NO. F/S R04155 (ENACTMENT NO. **R2005033**), WHICH AMENDED THE HUNING HIGHLAND SECTOR DEVELOPMENT PLAN TO ESTABLISH THE SU-2/CRZ". So `src-5a39a69a5a13eb31` (F/S R-04-155) joins F/S R-04-56 as **one of only two rows in the whole run whose enactment number is stated rather than inferred**. Both should sit ahead of the packages whose numbers are unknown. A sweep of every text fetched in this run found no number for C/S2 O-06-53, O-07-73, O-07-84 or R-08-97.
- Other substantive finds: the **ULI Advisory Services Panel report on the Albuquerque Rail Yards** (Feb 2008, 46 pp) — the outside expert assessment that preceded the City's own work, and the only ULI record in the inventory; the **Huning Highland Railroad development and building process plan** (41 pp), flagged because the related East Downtown SDP is marked REPEALED on the City's own server; the **2007 Neighborhood Task Force Final Report** (cover date printed "September XX, 2007" — final in substance, unfinalised in form); the **2019 Small Business Resource Fair report**; the three-part **July 2007 Four Hills Speed Hump Evaluation Study** (report 58 pp + technical appendix 30 pp + executive summary 6 pp, all image-only — the archive's Four Hills study is from 2023, sixteen years later); the **South Yale MRA/SDP boundaries map**; and the **City Attorney 360-degree evaluation balanced scorecard**, the archive's only record of how a City official is formally evaluated.
- Discovery leads from excluded records: the **Redflex red light camera contract** and the administration's response (a contested piece of City history with *no* record in the archive), and the **Streets and Traffic Enhancement Program** document that a 2013 comment-resolution matrix responds to.
- Two flat-level files are **unattributable** and excluded as such: `appendices.pdf` (20 pp) and `exhibitbpage1.pdf` (1 p), both image-only with no identifying header and no parent in the directory. Their checksums are recorded so they can be matched if a parent ever turns up.
- **CORRECTION and a method failure worth not repeating.** Two facts I recorded earlier today were wrong. (1) The `council/documents` remainder was never 35 or 105 uncovered records: most of it was **already decided**. (2) The topical lane was drafted over 63 records of which **41 were already rows** in `council-documents-cluster-research-2026-09-11.json`, which had covered every file at the flat level. The artifact now carries only the **22 genuinely uncovered** records (6 `approved`, 3 `duplicate`, 1 `superseded`, 6 `requires human review`, 6 `excluded`).
- **Why it happened, and the rule that prevents it.** The remainder was computed by excluding a list of *directory names* already worked and then selecting records still marked `pending review`. Neither test catches an already-decided record, **because this run writes no shared state** — every record a lane classifies stays `pending review` in `master-inventory.json` until Codex applies the artifacts. A flat-level file has no directory name to exclude, so it was invisible to the first test and looked untouched to the second. **Before claiming any lane, build the set of ids already appearing as a row in some file under `project-state/discovery/` and subtract it.** The topical lane's validator now enforces this, and future lanes should carry the same guard.
- **The nine conflicting dispositions resolve to the earlier artifact, not to mine.** Where the two lanes disagreed, `council-documents-cluster-research-2026-09-11.json` had read the files properly and the topical lane had not — it rendered page 1 of each image-only file and drew conclusions a fuller reading contradicts. Worked examples: `appendices.pdf` is not unattributable, it is the appendix volume to the Small Business Resource Fair Report opening on **Enactment No. R-2018-081**; the three Old Town topics have **different samples** (33, 12 and 7 participants), not one; and the City Attorney balanced scorecard is the **measuring instrument, not the result**, so the earlier exclusion is right. No integration action — those ids appear only in the earlier artifact.
- The overlap did contribute one thing: independent corroboration that four already-decided `duplicate` rows (the Volcano Trails SDP, its adoption resolution, its EPC Notice of Decision, and the October 2013 Central Avenue draft) are **byte-identical** to their validated canonicals, confirmed against the inventory's own recorded checksums.
- **The `council/documents` tree is now fully covered.** Every pending record under it is a row in some saved artifact. The legislative-amendment and advisory-board lanes I claimed earlier today do not exist as work — those records were already decided by the 2026-09-11 flat-level lane — and their rows have been removed from the ledger.
- **Remaining work, counted correctly:** 2,255 records are `pending review` in the inventory; 624 are already rows in a saved artifact; **1,631 are genuinely uncovered**. The largest bodies are `www.cabq.gov/municipaldevelopment/documents` (378), `www.cabq.gov/council/find-your-councilor` (239), `www.mrcog-nm.gov/DocumentCenter` (116), `api.realfile.rtsclients.com` (87), `www.cabq.gov/parksandrecreation/parks` (51) and `/recreation` (46), `www.cabq.gov/council/projects` (38), `documents.cabq.gov/planning/online-forms` (31) and `www.cabq.gov/neighborhood-traffic-management-program/documents` (29).
- **`municipaldevelopment/documents` lane, in progress — measurements taken, no artifact written yet.** The tree holds 378 uncovered pending records, by kind: ~222 miscellaneous, 50 bid/procurement, 41 CIP, 25 agenda/minutes, 16 forms, 9 project notices, 7 plans/reports, 5 posters/flyers, 3 presentations. Three URL collisions exist inside it (`bike-lane-ed-poster-final-eng.pdf`, `bike-lane-ed-poster-final-esp.pdf`, `2026-08-10-gaatc-agenda.pdf`). The first slice claimed is `cip-documents` (42 uncovered): all 42 fetched HTTP 200, 36 PDFs and 6 collection pages by leading bytes, no content substitution.
- Findings already measured in that slice, to be written up when the artifact is produced: **two internal byte-duplicate groups** — `bond-election-questions.pdf` appears three times (37,724 B: `src-b55b2e3b16fbce5b`, `src-69e0f8d7f13b52c5`, `src-8d49558bccd018a6`) and `summary-planning-process.pdf` twice (43,920 B: `src-959009fa3fe01cd9`, `src-c35e132701113c4d`) — and **four candidates byte-identical to already-validated, R2-archived records**: `src-c8891cd2a4655f74` → `src-888427a6abc5edf0` (2003 Library GO Bond scopes), `src-573991b6788eb8cb` → `src-6fc996f849849028` (2003 Street GO Bond scopes), `src-8198e95c6a898c26` → `src-b17fb11a9e63c180` (2011 Streets GO Bond scope), `src-2b2fc5cb64d08910` → `src-230753126ba3bbf7` (Consultant Compensation Rules and Regulations).
- **Folder year is not evidence of document year.** The City serves byte-identical bond-scope files from both `cip-documents/2003-bond-doc/` and `cip-documents/2007-election-documents/` — `LibraryBonds.pdf`/`library-bonds.pdf` and `StreetBonds.pdf`/`street-bonds.pdf` are the same bytes in both places. Anyone dating these from the directory name will get it wrong for one of the two copies. (A tempting discriminator was checked and rejected: the street scope funds a "West Central MRA Project", but the West Central MRA was designated by F/S R-2-16, **Enactment 82-2001**, so a 2003 bond could legitimately fund it. The year question is genuinely open and the archived 2003 attribution is left standing.)
- The `cip-documents` slice of `municipaldevelopment/documents` is triaged and fully terminal. `project-state/discovery/cip-documents-cluster-research-2026-09-12.json` recommends 25 `approved for addition`, 7 `duplicate` and 10 `excluded` across all 42 uncovered candidates. **The generator now asserts the coverage gate itself** — it fails if any row is already a row in another artifact — so the failure mode from the topical lane cannot recur silently.
- **Two complete GO bond programme packages the archive does not hold.** The first is the **2007 bond election package**: ballot questions ("Shall the City of Albuquerque issue $12,184,000…"), the bond issue taxable/non-taxable balance table, the dollar-and-percentage summary chart, the planning-process schedule, the voter FAQ, the CIP process summary, **Revised Ordinances Article 12 (Capital Improvements)** and seven purpose sheets. The archive publishes bond scope tables for 2003, 2009, 2011 and 2017 and a 2007 decade plan — but **no ballot questions for any programme and no capital improvements ordinance**. Those two fill real category gaps.
- **An undated set that must be dated before publication.** Ten purpose sheets plus a FAQ sit at the flat root of `cip-documents` under underscore filenames with **no programme year printed on nine of them**. Ruled out: 2009 (same filenames, but three R2 fetches gave ratios 0.82–0.95 with coverage 0.53–0.71 — similar subject, different documents), and 2003/2007/2011/2017 (no checksum matches; the 2017 sheets are 110–160 KB against this set's 33–59 KB). Points to **2013**: the museum sheet references 2013, the street sheet cites federal sign-replacement deadlines of 12/31/2014 and 12/31/2017, the archive already holds a validated **2013** bond sheet in this same flat directory (`src-3cc1912d7ac89cf9`), and no 2013 set is held. **Not asserted** — publishing a bond table under the wrong programme year would misstate what voters authorised. Each row carries the caution.
- **A dating conflict inside the 2007 folder, flagged not resolved.** `2007-election-documents/library-bonds.pdf` and `street-bonds.pdf` are **byte-identical** to `src-888427a6abc5edf0` and `src-6fc996f849849028`, which the archive has validated, R2-archived and published as **2003** bond scopes captured from `2003-bond-doc/`. Their sibling sheets in the 2007 folder reference 2006–2009 (the zoo sheet says aquarium construction is "expected to begin in 2009"). Either the City reused two 2003 sheets or the 2003 attribution is wrong. Both candidates are recommended `duplicate` — the bytes are identical either way — and each row carries the canonical's recorded title and date so the conflict is visible at application time. **Codex should confirm the year on those two archived records.**
- Discovery lead: the 2007 planning schedule names **Criteria Resolution R-06-21**, passed by the City Council and held nowhere. The archive already publishes the equivalent enacted criteria resolution **R-07-12** for the 2009 programme on the capital spending page — so it has one and the schedule that names the other.
- **Text comparison earned its place the other way round here.** Hashing found all six real relationships in this slice, because the directory posts the same file at several paths. What text comparison did was *rule out* three same-filename pairs against the 2009 set — a job hashing could not do, since unrelated files and different editions both fail a hash check identically.
- The procurement slice of `municipaldevelopment/documents` is triaged and fully terminal. `project-state/discovery/municipaldevelopment-procurement-cluster-research-2026-09-12.json` recommends 2 `approved for addition` and 50 `excluded` across all 52 uncovered candidates (the handoff row said 50).
- **The brief asked for the category to be tested rather than assumed, and testing found two.** Fifty of fifty-two are transactional solicitation paperwork. The exceptions are the City's **standard-form professional services agreements** for citywide on-call engineering (`src-b3d8dcb56e000437`, 16 pp, Rev. 10-04-17) and on-call landscape architectural services (`src-4952ea05cd055792`, 17 pp) — fourteen articles each, citing Section 61-23-21B NMSA 1978 and the Engineering and Surveying Practice Act, with the parties block left as placeholders ("Your Firm Name Here, Inc.", "Jane Engineer, PE"). They are standards, not solicitations. **The archive already publishes their companion** — `src-230753126ba3bbf7`, the Rules and Regulations Governing Compensation for Consulting Engineers, Architects and Landscape Architects, live on `content/development-land-use/development-process.md`. It has the rule saying how consultants are paid and nothing saying what they sign.
- They are two instruments, not two copies: ratio 0.9597, coverage 0.9767 and 0.9691 — shared boilerplate, different professional-practice provisions. Both approved; both labelled blank templates, not executed contracts.
- **The most format-mixed slice in the run, and a reason to keep checking leading bytes.** Six of the fifty-two are not PDFs: two PowerPoint decks and a Word document in OOXML zip containers, two legacy OLE2 `.doc` files, and one RTF. **`pdftotext` returns nothing for all six** — four readers were needed (pdftotext, a ZIP reader over `word/document.xml` and `ppt/slides/slideN.xml`, an RTF control-word stripper, antiword), plus rendering for one image-only PDF. Any future pass over this directory that assumes PDF will silently read nothing from those six.
- **A size coincidence that is not a relationship:** `src-4a2800924218bb64` and `src-503b2193335553d0` are both exactly **46,080 bytes** and both OLE2, with different checksums and different content. OLE2 files are block-padded, so equal sizes are common and mean nothing — only the hash settles it.
- Discovery lead: the Central Purchasing outreach deck names the **Public Purchases Ordinance, 5-5-1 et seq.**, as the statute the City procures under. Held nowhere. That pairs with the **Revised Ordinances Article 12 (Capital Improvements)** recommended from the cip-documents slice — the run has now found both of the ordinances that govern this department's work, and the archive held neither.
- One fetch (`src-1ecbfbd102bb54df`) failed with a connection reset and was **retried to a clean HTTP 200 before any measurement was recorded**, so no size or checksum in that artifact came from a partial download.
- The agenda and minutes slice of `municipaldevelopment/documents` is triaged and fully terminal. `project-state/discovery/municipaldevelopment-agenda-minutes-cluster-research-2026-09-12.json` recommends 18 `approved for addition`, 1 `duplicate` and 7 `excluded` across all 26 uncovered candidates.
- **The missing-minutes policy resolved the whole slice, and it cut both ways inside one directory.** For the **Greater Albuquerque Active Transportation Committee** the minutes are held and the series is maintained, so its agendas are excluded — including the 10 August 2026 agenda, whose minutes are validated and live as `src-99529b2a4c8db5aa`. For the **Albuquerque Energy Council** a *recorded exhaustive official-source review* found the body has **no presence on cabq.gov at all**: three candidate page URLs return 404 and the City's own site search for the exact phrase "Albuquerque Energy Council" returns **"0 items matching your search terms"**. For the ten meetings with no surviving minutes, the agenda is the only official trace and is preserved under the policy's exception, each labelled **"Agenda (approved minutes not located)"** and each checked for cancellation/no-quorum language (all ten clean).
- Substantive finds: **8 sets of minutes** — six Albuquerque Energy Council meetings 2019–2021, a January 2015 AEC agenda-and-minutes packet, and a **July 10 2023 GAATC meeting packet** whose pages 3 onward are that meeting's minutes, six months earlier than the archive's GAATC series begins (earliest held: 8 January 2024).
- **A filename wrong twice, invisible without rendering.** `src-5a55fed4c4811d02` is filed as `albuquerque-energy-committee-meeting-minutes-dec-17-2014.pdf`. The body is the Energy **Council**, not Committee, and the meeting is **21 January 2015**, not 17 December 2014. The file is image-only (two bytes of extractable text) and the inventory title just repeats the filename, so any pass trusting either would have misdated the archive's earliest Energy Council record by more than a month and attributed it to a body that does not exist. **Title it from the document.**
- **NOT SITE-READY: the entire published GAATC minutes series.** All **25** validated GAATC minutes records (8 Jan 2024 – 10 Aug 2026) are published on `content/transportation/bicycling/_index.md` and **none carries an `r2_url`** — their `validation_status` reads "passed: authoritative source HTTP 200" and their `direct_file_url` points at `onbase.cabq.gov/publicaccess`. The standing rule is explicit that an official live link is **not** a substitute for a verified archive object. Found incidentally, by checking that the August 2026 minutes really were held before excluding the matching agenda. Codex should queue the whole series for R2 archival and download verification. Claude modified nothing.
- Relationships here came from **date mapping, not hashing**: six agenda-to-minutes pairs were matched by extracting the meeting date from the face of each file — filenames are unreliable in this directory, and a hash cannot relate two different documents about one meeting. Hashing found only the one byte-identical URL twin (334,892 B), which is from the **13-group pending-pending subset** the URL-collision audit set aside as still needing triage, not the 79 `actionable_now` groups.
- A distinction worth keeping: the GAATC/GARTC task force agenda of 18 August 2026 is excluded because its minutes are **not yet produced**, not because they are lost. The policy exists for records that are permanently missing; a meeting three weeks old with a maintained minutes series is not that. Flagged to revisit.
- The 2013 GO bond department set is triaged and fully terminal. `project-state/discovery/bond-2013-department-set-cluster-research-2026-09-12.json` recommends 29 `approved for addition` and 29 `requires human review` across all 58 uncovered candidates, with **no duplicates**.
- **A complete GO bond programme the archive does not hold.** Fifteen departments, one scope sheet and one summary sheet each, dated from the face of the documents: scopes headed "Project Title **2013 Scope**", summaries headed "G.O. Bond Summary" over the columns **2013 2015 2017 2019 2021 Totals**. The archive publishes department sets for 2003, 2009, 2011 and 2017 and a 2007 decade plan — **nothing for 2013**. Zero checksum matches against the inventory, which is what establishes it as unheld.
- **This disproved a hypothesis from an earlier slice.** `cip-documents-cluster-research-2026-09-12.json` found an undated set of bond purpose sheets and recorded that the evidence pointed to 2013 *without establishing it*. Compared department by department against these dated 2013 sheets, the cip-documents set comes back at token coverage **0.55–0.80** — similar subject, different documents. **Not the same set.** That artifact's caution stands and its year is still unestablished.
- **MEASUREMENT CORRECTION — this nearly went out wrong.** The lane first paired the two editions with `difflib.quick_ratio`, which reported **1.0000** for five pairs (ABQ RIDE scope and summary, DMD CIP Facilities and Parking summary, Environmental Health scope and summary), and those five were about to be recommended as `duplicate`. **`quick_ratio` is an upper bound computed from character multisets** — it answers whether two strings *could* be identical, not whether they are. Recomputed with the real `difflib.ratio`, those five come out at 0.9871, 0.9830, 0.8742, 0.9849 and 0.9853, and **direct string equality is false for every pair in the set**. The five moved to `requires human review`; no record here is a duplicate of any other.
- **The sharper lesson, and it generalises to every lane:** token coverage of 1.0000 does **not** mean the documents are the same. The Animal Welfare scope pair has **full token coverage in both directions and a real sequence ratio of 0.2739** — same vocabulary, differently arranged and differently valued. Coverage measures whether one document's words appear in the other; it says nothing about order, structure, or the figures attached to them. **Use `quick_ratio` and coverage as cheap screens only; any claim that two documents are the same needs a byte comparison, or a real sequence ratio plus string equality.**
- **Twenty-eight edition pairs, none of them copies.** The City publishes every sheet twice, plain and `copy_of_`, with sequence ratios from **0.0103 to 0.9930**. The `copy_of` prefix names a CMS duplication event, not a duplicate document — somebody copied each object and then edited one. Neither edition can be ranked: both state the same cycle and schedule, neither carries a revision stamp, and **the City links both** (its own document listing links ten plain names *and* `copy_of_DMDStormDrainageSummary.pdf`). All 29 copy records are held as one package, ordered most-divergent first; the two Council Neighborhood Set-Aside sheets (0.0103 and 0.0729) are where to start.
- **Fourth undiscovered file of the run.** `DMDCIPFacParkingScope.pdf` returns **HTTP 200, 52,980 bytes**, a genuine PDF, and is linked from the City's own listing — but it is **absent from the inventory**; only its `copy_of` edition was crawled. Its counterpart `SeniorAffairsScope.pdf` was probed the same way and correctly **404s**, so that department genuinely has only one edition and its copy is recommended for addition. Add `DMDCIPFacParkingScope.pdf` as a candidate; the edition comparison cannot be completed without it.
- The remaining flat level of `municipaldevelopment/documents` is triaged and fully terminal. `project-state/discovery/flat-bond-and-standard-forms-cluster-research-2026-09-12.json` recommends 6 `approved for addition`, 38 `requires human review` and 1 `excluded` across all 45 uncovered candidates, with **no duplicates and no supersession**.
- **The brief asked whether the lowercase and CamelCase bond sheets are copies of each other. They are not, and nothing else in the set is a copy either.** All **41 within-family pairs** were measured with a real `difflib` sequence ratio plus a string-equality test: the **highest ratio in the whole set is 0.6155** (`AffordableHousingBonds.pdf` against `affordable-housing-bonds.pdf`), the lowest is **0.0157** (`street-bonds-1.pdf` against `street-bonds.pdf` — two filenames differing by one character), and **not one pair is string-equal**. Their figures differ department by department: affordable housing appears at $2,500,000, $3,400,000, $4,300,000 and $5,000,000 across four files; public transportation vehicle replacement at $3,790,500, $4,000,000 and $4,500,000. **They are sheets from several different bond programmes filed side by side under near-identical names.** The naming convention carries no information about content.
- **Two dated 2015 bond records the archive does not hold.** `summary.pdf` is headed "Summary of **2015** G.O. Bond Program"; `2015-g-o-summary.pdf` is the decade schedule over the columns **2015 2017 2019 2021 2023**. With the 2013 set from the previous slice the archive now has candidates for 2003, 2007, 2009, 2011, 2013, 2015 and 2017. These two are also the **key to the 37 undated sheets** held for review: matching each sheet’s figures against these tables is a reading task against a document this artifact recommends, not a research question. Every review row carries a `distinguishing_content` field holding its lead project and dollar figure so the matching needs no refetching.
- **A caution on that matching.** A single lead figure is not always enough. Three of the four storm sewer sheets carry **$2,000,000** on the NPDES Stormwater Quality MS4 line, yet their pairwise ratios are 0.1612, 0.1940 and 0.2036 — the rest of each table differs. Match on the full line set, not the first entry.
- **The standard-form family is now six instruments, and the archive held none of them.** The procurement slice recommended on-call engineering and on-call landscape architectural; this slice adds the **generic blank template** (`boilerplate-for-engineers-or-architects-template.doc`, legacy OLE2, whose heading reads "CITY OF ALBUQUERQUE ____________________ SERVICES AGREEMENT" with the profession itself left blank), **architectural**, **landscape architectural for a named project**, and the **HUD federally assisted** form. They are six instruments, not six copies: against the on-call engineering form, architectural returns a real ratio of **0.1919** and the HUD form **0.0256**. The archive publishes the Rules and Regulations Governing Compensation for these consultants and, until now, nothing about what they sign.
- **The coverage-is-not-sameness lesson confirmed a second time, in a second slice.** The two landscape forms return token coverage **0.9946** in both directions and a real sequence ratio of **0.4745**. Across all fifteen pairs of the six forms coverage runs **0.7658–0.9946** with eleven pairs above 0.95, while real ratios run **0.0256–0.6825** and not one pair is string-equal — so on coverage alone every one of these six would have read as a duplicate of the others. This is the same failure shape as the Animal Welfare scope pair (coverage 1.0000 both directions, real ratio 0.2739) recorded one slice earlier.
- **A ballot question with no election year on it.** `affordable-housing-bonds-2.pdf` is an Election Bond Question: "Shall the City of Albuquerque issue **$10,150,000** of its general obligation bonds in support of the Workforce Housing Act…", citing **F/S(3) O-06-9**. Its body references 2022 and 2024 and bond elections fall in odd years, which points at 2023 **without establishing it**. Held for review — publishing a ballot question under the wrong election would misstate what voters were asked. The archive holds almost no ballot questions; the 2007 set was recommended in `cip-documents-cluster-research-2026-09-12.json` for exactly that reason.
- **A third edition, where the previous artifact recorded two.** `copy2_of_FamilyCommServicesSummary.pdf` carries the same 2013–2021 column set as the plain and `copy_of` editions held for review in `bond-2013-department-set-cluster-research-2026-09-12.json`. That artifact’s `the_two_editions` package has **three** answers for this department, not two.
- **The numbered series really is incomplete.** It runs 1, 2, 5, 6, 8, 9, 10, 11. Numbers **3, 4, 7 and 12** were probed directly, six plausible names each (parks and recreation, public transportation, street, library, energy, community facilities) — **24 probes, none HTTP 200**. The gaps are real, not a crawl failure. This is the first probe sweep in the run to come back entirely empty, which is itself worth recording: the four earlier undiscovered files were found the same way.
- **One exclusion, later withdrawn — see the correction bullet below.** `albuquerque-m-helioscope_shading_1870012_summary.pdf` is a 3.4 MB Folsom Labs **HelioScope** photovoltaic shading report credited to a named individual, with loss percentages for shading, reflection, inverters, wiring, mismatch, soiling and temperature — and **no identification of the site it models** anywhere in its extracted text. It reached a bond slice only because its filename ends in `_summary.pdf`. It was excluded as unattributable on that basis, and that was wrong. **It did not survive slice 6.** Rendering the five sibling reports, then rendering this one, showed its site printed in large type on page 1: *Museum of Albuquerque (COA#2), 2000 Mountain Rd. NW*. The exclusion was withdrawn, the record removed from that artifact and approved in `facility-environmental-compliance-cluster-research-2026-09-12.json`. See the correction bullet below.
- The facility environmental compliance set is triaged and fully terminal. `project-state/discovery/facility-environmental-compliance-cluster-research-2026-09-12.json` recommends **26 `approved for addition`**, 1 `requires human review` and 1 `excluded` across 28 candidates — the 27 claimed plus one corrected out of the previous artifact.
- **A CORRECTION TO A SAVED ARTIFACT, and the rule it breaks was already written down twice.** `flat-bond-and-standard-forms-cluster-research-2026-09-12.json` excluded `albuquerque-m-helioscope_shading_1870012_summary.pdf` as unattributable "because searches of its extracted text return nothing". **The site is printed in large type at the top of page 1**: *Museum of Albuquerque (COA#2), 2000 Mountain Rd. NW*. Its title block is simply not in the text layer — `pdftotext` returns **1,561 bytes for three pages**, header and footer only. The record is removed from that artifact (now **44** records, no `excluded` rows) and approved here. **The absence of an expected string in extracted text is evidence about the extraction, not about the document. Render before excluding anything for what it does not say.** It was caught only because the sibling series was claimed as the next slice; nothing in the previous slice would have surfaced it.
- **Five files that complete a record the archive already holds.** `water-year-2015-report-032516.pdf` (`src-c312994aa69f9bd5`) is **validated and R2-archived** — and it is a 127,553-byte cover that *lists five attachments and holds none of them*. Two of those attachments are already archived separately; the other five were sitting unread in `pending review`, along with the **signed certification statement** — the only file carrying the signature (Robert J. Perry, Chief Administrative Officer, 3/30/16), the addressee (EPA Region 6, Water Enforcement Branch) and the penalty-of-law language. Containment was measured, not assumed: token coverage of each candidate inside the parent runs **0.1276 to 0.6852**; none is inside it. **A compliance submission whose attachments are missing is not the submission.**
- **Fifteen City stormwater pollution prevention plans, and the archive held none.** All marked **FINAL PLAN**, all **May 2021**, under the EPA Multi-Sector General Permit, across four departments: Street Maintenance (5 sites), Solid Waste Management (7), ABQ RIDE (2 versions) and Purchasing (1). The archive publishes the MS4 permit, the annual reports and the stormwater management program — every one of which describes facility plans — and not one facility plan.
- **Three plans, one address.** 5501 Pino St NE holds Street Maintenance in Building F, Clean City Division in Building D, and Warehouse and Surplus Auction in Building G, each with its own plan under its own department. Do not collapse them. Separately, **two filenames misspell their own site** — `swmd-cero-colorado-landfill` for Cerro Colorado, `swmd-don-reservior-convenience-center` for Don Reservoir. Title from the document, as the Energy Council record established.
- **The similarity trap, third slice running, and this time the score was 0.9527.** The two ABQ RIDE files share a cover naming both facilities and compare at a line-level ratio of **0.9527** with coverage 0.9457/0.9638. A character-level comparison, left running in the background because it does not terminate quickly on 105 pairs of 200-page documents, later put the same pair at **0.9811** with token coverage 0.9891/0.9912. They are **two facility-specific versions of one plan**: the Daytona file carries Figure 1A and a 2021 non-stormwater discharge re-evaluation for Daytona, the Yale file carries Figure 1B and the Yale memo. Settled by reading the diff opcodes — the largest differing block is 51 lines against 4 and it is the site drawing set — not by trusting the ratio. **Both retained; neither duplicates nor supersedes the other.** Note also that character-level comparison does not terminate in reasonable time on 105 pairs of 200-page documents; line-level comparison does.
- **Six solar design records for five City buildings, 813.1 kW DC designed.** Prepared for the City by Rio Grande Renewables and exported from Folsom Labs HelioScope: Albuquerque Museum (246.3 kW), Botanical Gardens (203.0), Main Library (201.9), Old APD Building (86.3), Griegos Health and Social Service Center (75.6). The archive publishes the City’s climate plans and greenhouse-gas inventories and has **no record of what solar was actually installed on City roofs**. **Caution carried on every row: these are modelled outputs marked "For Construction", never metered generation.** The Main Library file is an incomplete export — inverter nameplate **0**, load ratio blank, annual production table absent, and its own title records it as a *2017 backup* — and is the slice’s single `requires human review`.
- **THE LARGEST UNDISCOVERED SET OF THE RUN: 37 City MS4 documents, all verified, none in the inventory.** The certification statement prints the City webpage its data would be posted to. Fetching that page and reconciling every link against the inventory returned **61 links, 23 held, 38 absent** — 37 of them City files. **All 37 fetched individually: every one HTTP 200 with PDF leading bytes.** They include the **entire 2014 MS4 Annual Report package** (a main body plus 27 lettered attachments in their own directory), the **FY19, FY20 and FY21 annual reports** and an FY21 draft, the fiscal 2017 report, the 2016 signed compiled report, EPA’s 2015 approval letter, and **the MS4 permit itself**. The verified URL list is in the artifact under `an_undiscovered_set`. Three need care when adding: one `http` link on a host that refuses port 80, and two hrefs with unencoded spaces that must be percent-encoded before the request will form.
- **How that set was found is the transferable part.** The first four undiscovered files of the run came from probing names the City itself uses. This one came from **reading a City page that a candidate document pointed at**. The archive’s stormwater compliance series has a hole running 2014 through 2021 that no amount of triage over the existing inventory would ever have revealed. Worth repeating wherever a candidate names a City page.
- One exclusion, and it is clean: `nm-nrcs-agronomy-technical-note-revised-universal-soil-loss-equation.pdf` is **USDA-NRCS New Mexico Agronomy Technical Note 28, October 1999**, replacing the Universal Soil Loss Equation with its revised form. The City posts it because its stormwater work relies on the method, but USDA-NRCS is the authoritative publisher and the City’s copy carries no City authorship, adoption or amendment. If the method is worth a reference, cite it to USDA-NRCS rather than archiving the City’s copy as though the City issued it.
- The project-specific engineering and procurement records are triaged and fully terminal. `project-state/discovery/project-procurement-records-cluster-research-2026-09-12.json` recommends 18 `approved for addition`, 1 `duplicate`, 1 `requires human review` and 9 `excluded` across all 29 uncovered candidates.
- **The brief asked whether procurement papers for a single project belong in a public archive. The answer is not as a class, and the line is not between big and small projects.** What is kept states something — scope, budget, schedule, condition, or a City position. The **questions-and-answers files are the surprise here**: they are where the City puts its figures on the record. The Children’s Fantasy Garden Q&A gives a **$5 million total project budget, $1 million of soft costs, $4 million of maximum allowable construction cost and $750,000 estimated compensation**; the Double Eagle II Q&A records that **City projects are not LEED Certified**; the 7232.00 Q&A confirms a firm holding any City on-call architectural agreement is barred from proposing. What is not kept records only that something happened.
- **Nine exclusions, in two groups, and neither is "because it is procurement."** All three requests for proposals are recommended. Excluded are **four meeting rosters** — three of them lists of named private individuals with their working email addresses — and **four site walk-through notices**, single pages saying where to stand on a morning in 2022. Republishing a roster of private individuals’ contact details has a real cost and no archival return; that is why the rosters are excluded as a group rather than judged one at a time.
- **A NEW METHOD, and it is the one to keep: render and hash.** `cfg-survey-markup_2021-11-03-6353-93.pdf` and `..._percent-grade-extents_amp-003-6353-93.pdf` are **741,873 and 740,221 bytes with different SHA-256** — byte hashing says different. Normalized text is **string-equal, real ratio 1.0000**. That still would not settle it: this is a one-page aerial markup whose content is almost entirely one raster image and whose text layer is only the annotation list beside it, so two sheets could share every annotation and carry different drawings. **Rendering both pages produced byte-identical PNGs — the same 5,090,954 bytes with the same SHA-256.** True duplicate. **When two candidates differ in bytes but not in text and their substance is graphical, render both at the same scale and hash the images.**
- **Rendering now cuts both ways in consecutive slices.** In slice 6 it overturned an exclusion by finding a site name that was not in the text layer; here it proved a duplicate that hashing called different and text comparison could not prove. It is the only comparison in this run that answers the question a reader actually asks.
- **A fifth and sixth filename wrong, and a new category: numbers that exist only in a filename.** `7373-99-informational-sheet.pdf` is headed **Project No: 7303.99**. `mf-3604-10-20-25.pdf` is a request for proposals for **citywide on-call traffic engineering, Project No. 183.02** — its filename records neither. Separately, **three approved files print no project number at all**, so their numbers are recorded under `project_attribution` with the basis written out, never as `city_project_number`. The Yale cost estimate’s subject is beyond doubt (headed with its facility, addressed to the ABQ RIDE director) but its number comes from other documents; the two Children’s Fantasy Garden files **never write the garden’s name** — one abbreviates it to CFG, the other names only a Mushroom Tunnel and a Labyrinth.
- **The validation harness caught that itself, twice in one slice.** It checks that every project number claimed on a row occurs in that file’s extracted text: that failed on four rows. A first draft of the correction then claimed all three "name their subject unmistakably" — and the check that every asserted evidence term occurs in the text failed again, because two of them do not. Each row now carries `evidence_terms_present_in_the_document`, and the harness verifies every term.
- Substantive finds worth naming: a **complete condition record for the ABQ BioPark Children’s Fantasy Garden** — a 28-point accessibility slope survey (readings 0.3% to 15.8%, two stretches annotated *inaccessible for ADA*), a deficiency list pricing low, medium and high cost remedies, the renovation scope, and the budget; a **2015 conceptual construction cost estimate for the ABQ RIDE Yale maintenance facility** pricing three fleet options, which reaches the inventory under the filename `cost-estimate.pdf` and identifies neither its project nor its subject in that name; the **Selection Advisory Committee process presentation** recording the December 2022 rules update and the rise in the Expedited Design Services limit from $25,000 to $150,000; and a 2018 RFP for **Asia at the ABQ BioPark Zoo** at an approximate construction cost of **$15 million**.
- Two project numbers are preserved in the artifact that would otherwise have been lost with their files: **6370.93 Architectural Consultants for Albuquerque Museum Education Center** and **7011.95 Brillante Explora Early Learning Center**. Each appears in this slice only in an excluded walk-through notice, so the number and title are recorded on the row rather than discarded with the document.
- The permits, forms and regulations group is triaged and fully terminal. `project-state/discovery/permits-forms-regulations-cluster-research-2026-09-12.json` recommends 25 `approved for addition`, 2 `superseded`, 2 `duplicate`, 3 `requires human review` and 2 `excluded` across all 34 uncovered candidates.
- **THREE FILENAMES WITH THE WRONG YEAR, on forms residents actually use.** `gvp-application-2025.pdf` is headed **Green Vehicle Registration 2026**. `on-street-parking-application-2025.pdf` is headed **2026 APPLICATION FOR ON-STREET PARKING**. Taking either filename at face value would publish the current parking and green-vehicle forms a year stale and tell residents the wrong registration year. The brief’s instruction to date from the face rather than the filename changed three records. **Eight filenames in this tree have now contradicted their own documents.**
- **Two supersessions, found two different ways.** The **2008** Regulation Governing the Award and Rejection of Bids/Offers and Debarment of Contractors is superseded by the **1/22/21** edition — and that was invisible until the file was rendered: it has **36 bytes of extractable text for 36 pages**, and its cover reads *Effective JULY 25, 2008*. The **2024** Green Vehicle Registration form is superseded by the 2026 edition at sequence ratio **0.9994**, with a line-level diff returning **exactly one differing block: the year**. Both superseded records are retained — a superseded rule is the only account of what the rule used to say.
- **A caveat carried on the bids regulation.** The 2021 edition states **no effective date on its face**; its date is a page footer (1/22/21 on every page, 40 pages against the 2008 edition’s 36). The supersession rests on the identical title plus the later footer date, which is sound, but if the City can confirm an adoption date for the 2021 edition that should be recorded instead of the footer.
- **A supersession that the measurement refused.** `excavationbarricadeform.pdf` requires **fourteen** days’ notice; `row-permit-application-updated-2024.pdf` requires **ten**, has a longer title and is dated 2024. It looked like the same form in a later edition with a shortened notice period. **Real sequence ratio 0.2268, coverage 0.4560/0.7634 — not the same form.** They are two different permits from the same section, and both are recommended. The notice-period difference, which looked like the strongest evidence for the hypothesis, is simply a difference between two permits.
- **Hashing finally found something — once.** `mf-3334.docx` and `mf-3334-1.docx` are the same **20,309 bytes with the same SHA-256**: the only byte-identical pair in eight slices of this tree, and the only relationship in any of them that plain hashing would have found unaided. The other duplicate is a **format pair** — `excavationbarricadeform.doc` and `.pdf`, token coverage 1.0000 both directions, real ratio 0.9868, and they *cannot* be text-equal because the two extractions come from different readers over different containers (antiword over legacy OLE2, pdftotext over PDF). **Across eight slices, hashing found one relationship; text comparison, containment, date mapping and rendering found every other one.**
- **Substantive finds: the capital programme’s own instructions.** The **2027–2036 Decade Plan for Capital Improvements** instruction book and the **2023 G.O. bond programme** instruction booklet — the archive holds a 2007 decade plan and nothing later — plus the project request form instructions for **operating and maintenance expense analysis**, which require a project’s running costs to be identified from FY 2028 through FY 2032. Together they show how a project gets into the capital programme and what it must say about what it will cost to run.
- **Four promulgated City regulations, recommended as regulations rather than as paperwork.** The public-works bids and debarment rule; the **June 2025 fiber right-of-way excavation and barricading rules**; the rule governing requests for the City to serve as **fiscal agent of a state capital outlay project**, effective 1/30/2023; and the **October 2022 residential parking rule**. Three of the four carry a promulgation trail on their face — two DocuSign envelopes and an inter-office memorandum from the department director to the Chief Administrative Officer — and that trail is what separates them from the forms underneath them.
- **Three held for judgment rather than decided alone.** (1) The **fiber rulemaking correspondence** is genuinely two things: the public comment record of a City rulemaking, which is ordinarily public, *and* 74,000 characters of emails from named residents about work outside their own homes. This lane excludes rosters of private individuals’ contact details on its own judgment; it should not decide on its own judgment to publish residents’ complaints in full. (2) A **NOVOAGLOBAL, Inc. maintenance manual for the City’s automated speed enforcement equipment** — vendor-authored, which on this lane’s own precedent means exclude, but unlike the USDA and NMAC documents it does not appear to be published anywhere else, and it documents how the City’s speed cameras are maintained. (3) A blank worksheet whose entire shared string table is two words, *Source* and *Amount*, with no City identification on it at all.
- One clean exclusion on the established third-party rule: `cid-license-classifications-and-scopes.pdf` is **NMAC Title 14, Chapter 6, Part 6** — codified New Mexico law issued by the Construction Industries Division, reissued 3/10/2022. Cite it to NMAC rather than archiving the City’s copy. Same treatment as the USDA-NRCS technical note in slice 6.
- **THE `municipaldevelopment/documents` TREE IS COMPLETE.** Nine slices, **384 records**, every one decided. The last slice, `project-state/discovery/municipaldevelopment-flat-remainder-cluster-research-2026-09-13.json`, recommends 52 `approved for addition`, 1 `superseded`, 3 `duplicate`, 4 `requires human review` and 5 `excluded` across all 65 remaining candidates. Zero records remain uncovered anywhere under that tree.
- **RENDER AND HASH NOW CUTS BOTH WAYS, AND THIS SLICE IS THE PROOF.** Nine construction moratorium maps score up to **0.9980** real sequence ratio against each other, with token coverage to 0.9944 — on text alone most read as copies. Their extracted text is **street labels**: they are maps of the same city, so they carry almost the same labels whatever is drawn on top, and the moratorium itself (shaded areas, coloured streets) is not text at all. Rendered to PNG at the same scale and hashed: **nine maps, nine distinct SHA-256 values, not one pair the same drawing.** One slice earlier the same technique *proved* a duplicate that text could not; here it *refuses* six that text appeared to show. **Apply render-and-hash to any graphical candidate set before accepting or refusing a duplicate on text similarity.**
- **Rendering also produced the titles.** `holiday-shopping-map.pdf` carries *2018 HOLIDAY SHOPPING Construction Moratorium, November 21st – January 1st* in its title block; `2018-moratorium.pdf` turns out to be a **combined sheet showing all three 2018 moratoria at once**. Three maps state no year anywhere and are held for review — publishing one under the wrong year would tell a contractor the wrong streets were closed.
- **THREE PIECES OF ENACTED LAW THAT NO TEXT SEARCH COULD HAVE FOUND.** All scanned, all found by rendering the ten files whose text layer was empty: the **Storm Water Quality Ordinance, Council Bill C/S O-16-16, Enactment No. O-2016-016**, sponsored by Trudy E. Jones, creating Article 11 of Chapter 6 ROA 1994 (12 pages, **12 bytes** of extractable text); the **ordinance governing the naming and changing of names of streets, Council Bill O-198, Enactment No. 99-1983**, Fifth Council, sponsored by Nadyne C. Bicknell and Thomas W. Hoover (5 pages, **5 bytes**); and the Mayor’s **Vision Zero executive order**, recording 69 people killed on Albuquerque roadways in 2018 and citing Complete Streets Ordinance O-14-27 (2 pages, **2 bytes**). Both ordinances carry their **enactment number handwritten on the face** — exactly the evidence the west-central lane had to reconstruct from later legislation when its adopting resolutions came back blank.
- **A SECOND CITY FACILITY SOLAR SERIES: twelve more documents, 1,967.3 kW DC designed.** Eleven buildings — Barelas Senior Center, Cherry Hills Library, Fire Stations 5, 11 and 18, the Daytona transit facility, Cerro Colorado landfill, Los Altos swimming pool, the Pino Avenue City Yards, the **Sunport (609.8 kW, 1.026 GWh modelled — the only array measured in gigawatt hours)** and the Foothills area command. Prepared by **Solluna Solar** (Zach Johnson) and **OE Solar** — neither appears in the first series, which was Rio Grande Renewables. **With slice 6 the series now runs to eighteen documents across sixteen sites and three consultants.** Every single one had to be rendered: the site name, address and capacity are printed at the top of page 1 in text that is **not in the PDF text layer**. That is the exact failure that produced a wrong exclusion two slices ago, and the reason all twelve were rendered rather than sampled.
- **Two more Albuquerque Energy Council minutes** — 18 August 2021 (chair Alex Montano) and 16 March 2022 (chair Mayane Barudin) — for a body the agenda and minutes slice recorded as having **no presence on cabq.gov at all**. The March 2022 meeting is later than any record that artifact held. The ten agendas it preserved under the missing-minutes exception still stand; this adds to the series rather than displacing it. `august-18-min.pdf` carries no year in its filename; the document gives **Wednesday, August 18th 2021** on its face.
- **A ninth filename contradicting its own document**, plus a new category. `127-02-additions.docx` is headed *Project No: 137.02*. And **three records carry a year that exists only in a filename** — the Paseo del Norte moratorium map (filename says 2013, document says nothing) and the two undated event maps beside it. **A year in a filename is an attribution, not a fact**, and all three are held for review on that ground alone.
- **An extract, and a cross-slice near-miss worth keeping.** `pages-40-44-from-g-o-book-smm-1.pdf` sits entirely inside the 2025 G.O. bond instruction book (token coverage **1.0000** in, 0.1028 out) and is recommended `duplicate`. But the *same five pages* appear again as `o-m-instructions-2027.pdf`, approved in slice 8 — and those two are **not** the same: coverage 0.8722/0.9856, **real sequence ratio 0.4528**, not string-equal. The 2027 instructions are a revision with the fiscal years moved on. Keep both; only the extract is redundant.
- Two `/view` wrapper URLs resolved: the bicycle-lane poster twins return **110,513 bytes of HTML** with the suffix intact and the identical poster bytes without it. Both recommended `duplicate` of their plain twins, following the precedent already in the inventory. A third, the Mountain Road crossing `/view`, is **excluded** — its plain twin is already excluded and the same document is already held validated and R2-archived under the name `gabac-mountain-road-bike-blvd-crossing.pdf`.
- One supersession with its reasoning exposed: the two District 8 coalition street-maintenance presentations differ by **exactly one line block** in 166 lines — a revenue bullet rewritten from "$42 appropriated recently by City Council" to a complete sentence. **Neither document carries a date on its face.** The ordering rests on the successor’s fuller, corrected wording, with the filename date as corroboration only — because nine filenames in this tree have now been wrong, the filename alone would not have been enough.
- **A WHOLE LANE WITH NO DOCUMENTS IN IT.** `project-state/discovery/councilor-district-3-and-6-cluster-research-2026-09-13.json` reviews all 70 uncovered candidates under district-3 and district-6 and recommends **70 `excluded`, 0 `approved`**. Every one was fetched: **68 return HTML, 2 return a councillor portrait JPEG. Not one is a PDF, a Word file or a spreadsheet.** They are live Council web pages — biographies, news releases, event notices, project pages — plus **23 Plone `resolveuid` aliases** that redirect to those same pages, nine of them to pages that are themselves separate candidates in the same slice. A rendered web page has no original to archive, so the standing R2 rule cannot be satisfied for any of them.
- **AND IT GENERALISES, WHICH MATTERS FOR THE SIZE OF THE REMAINING BACKLOG.** All **239** uncovered candidates under `council/find-your-councilor` are extensionless URLs and **76 are `resolveuid` aliases**. Beyond the 70 decided here, a **random sample of 30** was drawn from the remaining 169 and fetched: **29 HTML, 1 JPEG**. **100 of the 239 have now been fetched and measured by leading bytes and not one is a static document.** Codex should fetch the remaining 139 and remove every HTML responder from the document candidate pool rather than queuing them for triage. This was the largest single block of uncovered candidates left in the inventory; treating it as a document backlog **overstates the remaining archival work by roughly a sixth of the whole**.
- **A page cannot be archived, but the files it links to can — and this lane proves the rule pays even when the lane is empty.** All 68 HTML bodies were parsed for links to static files: **5 found, 1 already held, 4 absent from the inventory**, all four verified HTTP 200 with PDF leading bytes. The best of them is the **Albuquerque City Council Cruising Task Force Findings and Recommendations, April 2, 2018, produced per Resolution R-17-250** — 9,756,353 bytes, linked from the district-3 cruising initiative page, and nowhere in the inventory. A Council task force report produced under a named resolution is exactly what this archive is for. **When a candidate turns out to be a page, harvest its links before discarding it.** That is now the second slice where reading a City page a candidate pointed at found records the crawl missed — the stormwater slice produced 37 the same way.
- One of the four, `2026-8-15-theory-of-change-final-pdf.pdf`, **also appeared** in the 37 undiscovered files harvested from the City’s MS4 permit page in `facility-environmental-compliance-cluster-research-2026-09-12.json`. Two unrelated City pages link it and the inventory has it from neither.
- **No row in that artifact carries a `canonical_id`, deliberately.** Nine alias records point at pages that are also candidates, and two are byte-identical to the pages they alias — but a `duplicate` recommendation names a canonical, and **a canonical has to be a record worth keeping**. None of these is. The relationships are recorded on the rows as `aliases_a_candidate_in_this_slice` and `byte_identical_to` instead.
- The MRCOG DocumentCenter set is triaged and fully terminal. `project-state/discovery/mrcog-documentcenter-cluster-research-2026-09-13.json` recommends **81 `approved for addition`, 23 `duplicate`, 12 `requires human review` and 3 `excluded`** across all **119** uncovered candidates. (The lane was claimed as 116; the tree actually held 119.)
- **THE BRIEF I WROTE FOR THIS LANE WAS WRONG, AND THE INVENTORY SAYS SO.** I claimed MRCOG material should be tested against the third-party rule that excluded the USDA-NRCS soil-loss note, the NMAC licensing rule and the PNM tariff. **The archive already holds 117 validated MRCOG records, most with an `r2_url`, published across ten content pages** — demographics, maps, dashboards, design references, operations data, safety and crash data, speed management, studies, bike maps and zoning. MRCOG is the region’s metropolitan planning organisation, not a third party. **The rule was never about who else publishes a document; it is about whether the document is a record of governance here.** Nothing in this lane is excluded on third-party grounds.
- **TWO PLANS HELD TWO DIFFERENT WAYS, AND THE RIGHT ANSWER IS OPPOSITE IN EACH CASE.** Both metropolitan transportation plans here are published in parts. **Connections 2040**: the archive holds the *full* 348-page document (`src-d651356cdad0e349`, 59,514,796 bytes, validated with an `r2_url`) plus appendices A–E, G and I. The nine chapters, the Executive Summary and the Cover/ToC found here are **bound into it** — all eleven recommended `duplicate`, avoiding **60,998,778 bytes** of redundant archival. **The 2030 MTP**: the archive holds **one** element, the Pedestrian Element, at `DocumentCenter/View/2262`. The plan is published as a table of contents plus elements 1–14 at **View/2254–2268**; this lane supplies **all fourteen others** — recommended for addition.
- **TEST CONTAINMENT WITH A CONTIGUOUS RUN, NOT A TOKEN SET.** Coverage alone could not separate the two cases. The eleven bound-in parts score coverage **1.0000** (Executive Summary 0.9990) **and** three of three sixty-word contiguous runs present verbatim in the parent. The four Connections 2040 appendices **F, H, J, K** score coverage **0.8447–0.9622 — and not one contiguous fifty-word run is present**. They are not bound in, they are recommended for addition, and with the seven already held they **complete the appendix set A–K**. On coverage alone Appendix K (0.9622) would have looked more “inside” than it is.
- **A THIRD WAY A DOCUMENT HIDES FROM TEXT SEARCH: an enciphered display font.** All fourteen 2030 MTP elements carry a running head set in a display font whose text layer is **shifted one character position** — it extracts as `.FUSPQPMJUBO5SBOTQPSUBUJPO1MBO`, each letter one higher than the letter shown, decoding to *Metropolitan Transportation Plan*. Measured exactly: **14 of 14 carry the shifted running head, 3 of 14 also state the plan’s name readably in body prose, 11 do not state it readably anywhere.** A text search for the plan’s name would miss eleven of its own elements. The run has now met three variants: **no text layer at all** (the scanned ordinances), **a thin text layer that omits the title block** (the solar reports), and **a complete text layer whose headings are systematically wrong** (these).
- **My first draft of that finding overstated it and the validation harness caught it.** I had written that the phrase never appears in the extracted text of any of these files. It appears in three. The harness now checks the claim against a recount of all fourteen and fails if the stated counts do not match. **State the count, not the impression.**
- **Hashing found twelve relationships here; containment found eleven more that hashing could not reach.** The twelve: eleven **URL-shape twins** (the publisher serves every document at both `/View/<id>` and `/View/<id>/<Slug>`, byte-identical — the slugged form is canonical because it names the document) and one **genuine cross-id duplicate** (`Project-Feasibility-Form-PFF-DOCX` at both `/View/6498` and `/View/6504`, one 59,280-byte file under two document records). The eleven containment duplicates are different files with different bytes; **no hash comparison could ever have found them**, because the relationship is that one is printed inside the other.
- **Two inventory titles contradicted by the bytes or by their own twin.** `2014 CMP Corridor Rankings Map (PDF)` is a **JPEG** — leading bytes `ffd8ffe0`, and the publisher’s own slug says JPG. And `/View/2062` is registered as *2014 CMP Strategies Matrix* while its byte-identical twin `/View/2062/2014-CMP-Strategies-Matrix-PDF` is registered as **2016** — same bytes, two different years claimed. The 2014 reading is recommended because it is what the publisher’s slug says; flagged for integration.
- Twelve records held for judgment as one package: **comprehensive and growth plans for other jurisdictions** — Belen, Moriarty, Bernalillo, Edgewood, Peralta, Bosque Farms, Corrales and Valencia County — hosted by MRCOG because the region publishes its members’ plans. The archive’s existing MRCOG holdings are regional analyses and regional plans, **not other towns’ comprehensive plans**. Whether the scope extends to them is a question about what this archive is for, not about the documents.
- Only three exclusions, and none on third-party grounds: three `DocumentCenter/Index/` **folder listing pages**. They sit under a DocumentCenter path but return HTML — the fetched bytes begin with blank lines then `<!DOCTYPE html>`, which is why the leading-bytes check reads `0d0a0d0a` rather than a document signature.
- The parks and recreation lane is triaged and fully terminal. `project-state/discovery/parks-recreation-cluster-research-2026-09-13.json` recommends **6 `approved for addition`, 2 `duplicate`, 34 `requires human review` and 55 `excluded`** across all 97 uncovered candidates.
- **THE ARCHIVE CONTRADICTS ITSELF ON A CLASS OF 34 RECORDS, AND THIS LANE WILL NOT PICK A SIDE.** `BianchettiPark.pdf` (`src-00d4de3fc40a3556`) is **excluded** with the recorded reason *“one-page Prescription Trails walking-guide/map for Bianchetti Park, not a durable City plan, study, ordinance, or project record.”* `ManzanoMesaMulti-CulturalCtr.pdf` (`src-08682b53cc068b7b`) is **validated, described and published** on `content/public-works/parks-recreation.md`. **They are the same kind of document** — one-page Prescription Trails guides for a single park, same programme, same path — and **34 more are pending here**. All 34 are held as **one package and one decision**, with the conflict quoted from the inventory on every row. Approving them would have been the larger-looking result and the wrong one.
- **And the record that sets the publishing precedent is itself not site-ready.** Manzano Mesa carries **no `r2_url`**; its `validation_status` reads *“R2 archival follow-up pending explicit approval”*. So the archive is publishing a document it has not archived, and excluding its twin. Whichever way the class is decided, that needs settling too.
- **THE FIRST CROSS-INVENTORY BYTE COLLISION OF THE RUN.** `final-albuquerque-comprehensive-on-street-bicycle-plan` is **byte-identical** to `src-0f60535452ac9154`, the 2000 Albuquerque Comprehensive On-Street Bicycle Plan, already **validated with an `r2_url`** — same **8,054,496 bytes**, same SHA-256. The two inventory records carry **different URL strings for one object**, so no URL group-by could ever have found it; only comparing the fetched checksum against the *archived record’s own checksum* did. **Every slice before this one reported zero cross-inventory collisions** — true each time, but a weaker claim than it sounded, since the comparison can only reach the 1,612 inventory records that carry a checksum at all. **Worth a sweep: compare every checksummed pending record against every checksummed archived record, not only within slices.**
- **Two files that are not what their names say, and an extension that lies.** `recreation/bike/documents/bike-map.pdf` is **byte-identical to `2009bikemap.pdf`** — the City serves its **2009** bike map under a name carrying no year; publishing that name would present a seventeen-year-old map as current, so the **dated** name is canonical. `waiver-of-liability-flag-football.pdf` **is not a waiver**: it is the *Additions to Original Roster* form, byte-identical to `additions-to-original-roster.pdf` — the flag football waiver the filename promises **is not at that address**. And `parentscodeofconduct2016.doc` has leading bytes `25504446`: it is a PDF. **Ten filenames across this run now contradict the document or the bytes behind them.**
- **Three byte-identical pairs are recorded on their rows rather than as `duplicate` recommendations, for one reason: a canonical has to be a record worth keeping.** The roster/waiver pair are *both* excluded. The two prescription twins sit inside the package whose scope is the open question — naming one of them canonical would quietly decide the very thing the artifact refuses to decide. This is the third lane in which that principle has applied.
- **Missing-minutes exception applied, with the review recorded inline.** The **Metropolitan Parks and Recreation Advisory Board agenda of 4 May 2021** is the only record of that board in the inventory apart from a press notice. Three candidate official page URLs → **all 404**; the City’s own site search for the exact phrase plus *minutes* → **“No results”** with no link to the board at all; **no MPRAB minutes anywhere in the inventory**; and the agenda carries **zero** cancellation, postponement or no-quorum language. The exception applies — approved, labelled **“Agenda (approved minutes not located)”**, never to be presented as minutes. **Caution on the row: it prints a Zoom meeting ID and passcode. Stale, but redact rather than republish a working-looking credential.**
- 55 exclusions, and none of them a judgment call: **38 live web pages** (leading bytes `3c21444f`) and **17 items of recreation programme paperwork** — season schedules, registration forms, waivers, rosters and codes of conduct from 2014–2017. Both classes were already settled in this same tree: 74 prescription-trails records are excluded in the inventory as *“navigation, pagination, contact, or generic interface text,”* and the non-durable reasoning is the archive’s own.
- The NMDOT file host is triaged and fully terminal. `project-state/discovery/nmdot-file-host-cluster-research-2026-09-13.json` recommends **84 `approved for addition` and 3 `duplicate`** across all 87 uncovered candidates — nothing excluded, nothing held for review. Its approved footprint is the largest of any lane in this run.
- **The brief’s two questions, answered from evidence.** *Whose files are these?* NMDOT’s: across the 87 files’ extracted text, **NMDOT appears 256 times** and *New Mexico Department of Transportation* 76; Santa Fe Trails 11, Mid-Region 8, Rio Metro 4, ABQ RIDE once. *Is the endpoint authoritative or a mirror?* **Authoritative** — it is where `dot.nm.gov`’s own pages link their files, and the archive’s existing records from it carry `parent_url` values on dot.nm.gov, one of them the department’s Research and Climate Bureau page.
- **AND I WROTE THAT BRIEF ON A WRONG PREMISE — FOR THE SECOND LANE RUNNING.** I framed this lane around a possible third-party exclusion. The inventory had already decided: **42 records from this exact endpoint are validated** (41 with an `r2_url`), filed under agency values *NMDOT*, *New Mexico Department of Transportation* and *Other authoritative source*, and the archive holds **62 validated NMDOT records** overall. The MRCOG lane before it was the same mistake with 117. **Check the inventory for the archive’s existing decisions about a source before framing a lane around it.** It changed no disposition either time — what was wrong was the question I set myself — but it cost two lanes their framing, and the correction was the first thing both artifacts had to say. The next lane’s brief now carries that instruction.
- **A SERIES EXTENDED BACKWARDS BY A DECADE.** The archive holds **six** years of FTA Section 5310/5311 transit budget awards, **FY2022–FY2027**, every one validated with an `r2_url`. This lane supplies **ten more, FY2012–FY2021**, including the CARES Act year — an unbroken **sixteen-year run**. These list, year by year, which transit providers in New Mexico got federal money and how much. Six years show the current position; sixteen show how the money moved.
- **A complete seven-part statewide truck parking study**, published as numbered deliverables from project management plan through recommendation and implementation plan. Five of the seven are **OOXML** and must be archived in their original format; the final report alone is **55,752,016 bytes**.
- **A file host is not a publisher.** Three documents on this endpoint are **not NMDOT’s**: the **MRCOG** Coordinated Public Transit – Human Services Transportation Plan 2024, and two federal publications — the **FHWA** Vulnerability Assessment and Adaptation Framework (3rd ed.) and *Model Performance Measures for State Traffic Records Systems*. Reading provenance off the domain would have attributed all three to NMDOT. Their rows carry the real `publisher` and record NMDOT as `hosted_by`. They are recommended, not excluded — the archive has already validated an FHWA record from this same endpoint.
- **Seven inventory titles that are not the document.** The worst: a **17,401,443-byte** record titled *“Related Links”* — it is the **NMDOT Transit and Rail Division 2023 Transit Guide**; *Related Links* was the navigation text of the page the crawler found it on. Also *“Event (1).Pdf”* (an Event/Comment Form), and three Park-and-Ride route letters titled after routes their files do not cover. Every row carries a **`served_filename`** field beside the title taken from the document, so the mismatch stays visible. **Seventeen records across this run now carry a title, filename or extension that contradicts the document or the bytes behind it.**
- A near-miss worth recording: the **Notice of Non-Discrimination** appears twice in English and twice in Spanish and looks like the duplicate case the Title VI forms actually are. It is not — **206,129 against 109,311 bytes** for the English pair, **246,573 against 104,486** for the Spanish. Different files, all four retained. The three genuine duplicates are the Title VI complaint form in English, the same in Spanish, and the ADA modification request in Spanish, each registered twice under two candidate ids.
- **Remaining work, recounted 2026-09-13:** 2255 records are `pending review` in the inventory; **1375 are now rows in a saved artifact**; **880 remain uncovered**. (The earlier count in this file predates the last eight lanes.)
- The council-projects and planning-forms lane is triaged and fully terminal. `project-state/discovery/council-projects-planning-forms-cluster-research-2026-09-13.json` recommends **14 `approved for addition`, 2 `duplicate` and 53 `excluded`** across all 69 uncovered candidates.
- **The brief worked, and it is the first lane in three that needed no correction.** Reading the archive’s existing dispositions at both paths *before fetching anything* settled the whole lane in advance: `council/projects` holds **14 validated live-link records** titled *“Official source: …”* and **14 excluded**, twelve of them as *navigation, pagination, contact, or generic interface text*; `planning/online-forms` holds **2 validated with `r2_url`** (the General Planning Fee Schedule and the Drainage Pond Slope Stabilization requirements) and **2 excluded**, one a blank plat-extension checklist. **This archive keeps planning standards and excludes blank process forms; it treats Council project pages as live links, not documents.** Both rules applied cleanly to all 69.
- **THE CROSS-INVENTORY SWEEP PAID AGAIN — TWO MORE ALREADY-ARCHIVED RECORDS.** Every fetched checksum was compared against all **1,020** checksummed validated/archived inventory records. Two candidates are **byte-identical** to records already held with `r2_url`s: the **Original Albuquerque Complete Streets Ordinance** (126,155 B) and the **Albuquerque Complete Streets Legislation Packet** (5,086,786 B). Both are reached through **Plone `resolveuid` aliases** under `council/projects/completed-projects/2015/complete-streets`, while the archived records sit under `council/documents/councilor-district-2-documents` — **no URL, filename or title comparison could ever have matched them. Only the bytes did.** That is three such collisions in two consecutive lanes; the sweep is now written into the next lane’s brief.
- **A STANDING BLOCKER RESOLVED RATHER THAN TRIPPED.** The lane turned up a **Council Bill F/S O-14-27** file whose `ENACTMENT NO.` line is blank underscores — exactly the shape the isolated-legislative-file blocker exists for. It resolves cleanly: the archive **already holds the enacted package** (Original Complete Streets Ordinance, Legislation Packet, and an Ordinance Update sourced from `o-64enacted-6.pdf`, all validated with `r2_url`s), and the candidate is **byte-identical to the first of them**. It was never an isolated file — it is the held file reached by another route. **And the date came from another lane:** the Vision Zero executive order approved in the flat-remainder artifact records that the Council enacted O-14-27 **in 2015**. That is the enactment-number method working across artifacts.
- **Seven organisational letters of support for the Complete Streets Ordinance** — AIA Albuquerque, APA New Mexico, the NM Complete Streets Leadership Team, NM ASLA, the NM Healthier Weight Council, the Downtown Albuquerque Millennial Group and the Greater Albuquerque Chamber of Commerce — plus the **Comprehensive Plan map of the Central and Established Urban areas** (AGIS, 12/5/2014) the ordinance was framed against. **Three of the seven letters have no usable text layer and had to be rendered**; without that they were three unattributable scans.
- **Why these letters are recommended when the fiber correspondence was not.** The permits-and-regulations lane held the fiber rulemaking correspondence for human review because it compiles **named private residents’** complaints about work outside their own homes. These are **organisations** writing officially to Council on their own letterhead, signed by an officer in that capacity. **The distinction is not the subject; it is whose privacy is at stake.**
- **Six planning standards hiding among the blank forms.** Sorting `planning/online-forms` by filename would have excluded all six with the checklists: the **Zone Change Policy adopted as Enactment 270-1980** (Appendix B, still published among the application materials), the **Albuquerque Site and Building Design Considerations** — whose filename calls it a *submittal form* and which is actually design guidance developed by a convened team of architects and planners — the two **Wireless Telecommunications Facility** requirements documents, and the two **EPC application requirements** documents. The other 25 really are blank checklists and are excluded on the archive’s own recorded reasoning.
- The 28 Council web pages are excluded as not-static-documents, consistent with the councillor lane — **but that is not the whole story, and the artifact says so.** The archive keeps 14 such pages as `Official source:` live-link records. Several of these 28 are substantive project pages that may belong in that pattern; the candidate list is handed to Codex under `a_note_on_the_web_pages` rather than decided here, because **creating a live-link record is a different act from archiving a document**, and this lane’s remit is documents.
- The NTMP and planning-documents lane is triaged and fully terminal. `project-state/discovery/ntmp-planning-documents-cluster-research-2026-09-13.json` recommends **38 `approved for addition`, 10 `duplicate`, 2 `requires human review` and 11 `excluded`** across all 61 uncovered candidates. (Claimed as 57; the two paths held 61.)
- **THIRTEEN ADOPTED PLANS WERE SITTING IN A DIRECTORY OF APPLICATION FORMS**, reaching back to the **Planned Communities Criteria: Policy Element, February 1991**. Also: the **Barelas Sector Development Plan, Adopted April 2008** (49,626,419 B, the largest file in the lane); the **Bosque Action Plan, Rio Grande Valley State Park, Final Plan, January 1993**; the **Major Public Open Space Facility Plan, January 1999**; the **Clayton Heights Metropolitan Redevelopment Area Plan (2010)**; the joint City–County **Electric System Transmission and Generation Facility Plan 2010–2020**; the **H-1 Historic Old Town Zone Design Guidelines** as amended through 9 April 1998; the **Unser Boulevard Overlay Zone complete legislation**; the **Barelas Neighborhood Commercial Area Revitalization Plan**; and the **Trails & Bikeways Facility Plan, adopted July 1993** — which is the **predecessor of a series the archive already holds** (a 2015 appendix and the 2024 Bikeway and Trail Facilities Plan). **Five of these have no usable text layer and were readable only by rendering.**
- **AN ENACTED PACKAGE THE ARCHIVE DOES NOT HAVE.** The archive holds the **Volcano Trails Sector Development Plan** (8,903,701 B, r2) and its **adoption resolution** as *separate* objects. This lane found `VolcanoTrailsSDPFINALenacted2011.pdf` — **8,976,542 B, the plan and its adopting legislation in one instrument**. Measured: token coverage of the held plan inside it is **1.0000** with **two of three** contiguous 60-word runs present verbatim, and it carries **221 further tokens** that are the vocabulary of adoption (*adopt, adopting, authorized, advisory*, enactment-style numbers, NMSA years). The standing blocker on isolated legislative files exists precisely because a plan without its adopting instrument is incomplete — here is the complete one.
- **THE CROSS-INVENTORY SWEEP FOUND TWO MORE — FIVE NOW ACROSS THREE CONSECUTIVE LANES.** `ABQcomprehensiveonstreetbicycleplan.pdf` and `VolcanoTrailsSDPFINAL.pdf` are byte-identical to records already validated with `r2_url`s. **The 2000 Comprehensive On-Street Bicycle Plan has now appeared as three inventory records at three different paths for one 8,054,496-byte file** — once in the parks lane, once here, and once already archived. **Run the sweep before writing any lane.**
- **The City’s complete traffic-calming toolbox**, one sheet per measure: chicanes, diagonal diverters, forced turn islands, high-visibility crosswalks, lane narrowing with a centre island, lateral shifts, median barriers, medians, neckdowns and bulbouts, one- and two-lane chokers, one-way couplet conversions, parking strategies, partial closures, permanent radar speed signs, radar speed trailers, raised intersections, speed humps, speed kidneys, speed tables, and education and community involvement — plus the program document. The archive holds **83 validated speed studies** from this program and a live link *to* the tools, and not the tools themselves. **Eight sheets are registered twice** at one URL and are recommended duplicate.
- **A fetch timed out mid-download and the measurement was verified before anything was recorded.** A 15,918,465-byte plan stopped at **15,130,560 bytes** on one attempt; curl’s own retry completed it, and an **independent refetch returned the identical SHA-256**. A partial download that reached the log unnoticed would have put a wrong size and a wrong checksum into an artifact.
- **Two held for judgment, both stated precisely rather than guessed.** (1) A **Map #3 Trails Map** whose legend names the Bosque Action Plan, numbered sheet 19, same `0193` date stamp — almost certainly a page of the plan, but **both files are wholly image-only (zero extractable tokens each)** so token containment cannot be measured, and **render-and-hash at PDF page 19 does not match because the printed sheet number is offset from the PDF page number** (PDF p19 of the 105-page plan is an Introduction divider). Settling it needs a page-by-page render comparison — mechanical, not research. (2) **El Rancho Atrisco Phase III**, whose cover reads *Prepared for WESTLAND DEVELOPMENT CO. INC., For Submission TO THE CITY OF ALBUQUERQUE ENVIRONMENTAL PLANNING COMMISSION* — a developer’s submission, not on its face an adopted plan.
- Eleven exclusions, none of them a judgment call: four live web pages and seven items of **live-process permit paperwork** (flow charts, cancellation and stop-work requests, a contact sheet). Both classes were already settled at these exact paths by the archive’s own recorded reasoning. One of them, `stop-work-request-for-homeowners-dm.docx`, has leading bytes `25504446` — **it is a PDF**.
- **Running total, 2026-09-13:** 1505 of 2255 `pending review` records are now rows in a saved artifact; **750 remain uncovered**.
- The Rio Metro, sustainability and economic-development lane is triaged and fully terminal. `project-state/discovery/riometro-sustainability-econdev-cluster-research-2026-09-13.json` recommends **43 `approved for addition`, 13 `duplicate`, 1 `superseded` and 10 `excluded`** across all 67 uncovered candidates.
- **THE CHECKSUM SWEEP’S LARGEST YIELD YET: TEN ALREADY-ARCHIVED RECORDS, ALL FROM RIO METRO.** The Rail Runner system map, the rail and bus connections guide (**twice**), the project development history, the Paseo del Norte high-capacity transit study, two operations-and-maintenance facility documents, the double track study, the consolidation study **final** report and the facility fact sheet — every one byte-identical to a record already validated with an `r2_url`. **The document centre serves each file at both a bare `/View/<id>` and a slugged URL; the archive holds one shape and the inventory registered the other**, so the connections guide would have been archived a third time. **Running total: 15 already-archived records caught across four consecutive lanes** — before the parks lane introduced the sweep, every lane reported zero.
- **AN AGENDA IS NOT A NOTICE, AND THE EXCEPTION IS NARROW ON PURPOSE.** Thirteen Economic Development Action (EDAct) Council records: **four agendas and nine notices of meeting**. A recorded exhaustive official-source review — three candidate page URLs all **404**, City site search for the body plus *minutes* returns a **no-results** page, **no EDAct minutes anywhere in the inventory** — means the missing-minutes exception applies. But it applies **only to the agendas**: the notices open *NOTICE OF MEETING, EDAct COUNCIL*, a date and a time range, and stop. **The exception exists to preserve the only official trace of what a meeting considered; a notice records only that a meeting was called.** Applying it to everything meeting-shaped would have added nine records that say nothing. One of the four agendas is a **draft** and its row says so.
- **A draft and its final, both kept.** The **Rio Metro / ABQ RIDE Consolidation Study Draft Report, October 2025** (25,042,434 B) against the **final report** the archive already holds with an `r2_url` (25,114,690 B). The draft names its own stage on page 1. Recommended `superseded` and **retained** — it is the only record of what the study said before the final.
- **The Albuquerque Climate Change Task Force working record for October 2020** — minutes, two agendas, four presentations and a one-pager — plus the **Food and Agriculture Action Plan**, the **Optimized Municipal Electric Vehicle Charging Analysis (2019)**, a greenhouse-gas inventory, the **Spanish** Climate Action Plan Implementation Report 2025, and a climate action survey. The archive holds the Climate Action Plan and its implementation report; **this is the working record behind them**.
- One clean third-party exclusion: an **IRS Form 1040 Schedule SE**, posted by the City as a *sample document* for an economic relief grant application. Federal form, federal publisher — same treatment as the USDA-NRCS note, the NMAC licensing rule and the PNM tariff. If the grant programme needs a record, archive the City’s own application guidance instead.
- Three internal re-registrations, resolved on the same rule each time: a task-force minute registered twice (one with a `-1` suffix), the Food and Agriculture Action Plan twice (one with a `copy_of_` prefix), and the Zozobra train schedule twice (one under a bare numeric id). **A `copy_of_` prefix, a `-1` suffix and a bare numeric id are artefacts of the publishing system, not names of documents** — the plainer name is canonical.
- **Running total, 2026-09-13:** 1572 of 2255 `pending review` records are now rows in a saved artifact; **683 remain uncovered**.
- The transit, open-space, NMDOT-modal and sustainability-transportation lane is triaged and fully terminal. `project-state/discovery/transit-openspace-nmdot-sustainability-cluster-research-2026-09-13.json` recommends **14 `approved for addition`, 3 `duplicate` and 41 `excluded`** across all 58 uncovered candidates.
- **A GAP IN MY OWN METHOD, FOUND AND CLOSED.** The checksum sweep had been comparing fetched checksums only against **archived inventory records** — the 1,020 that carry a checksum. It was **not** comparing against records *this run had already decided*, whose checksums exist only inside the saved artifacts (they are still `pending review` in the inventory and carry no checksum there). Two candidates in this lane are **byte-identical to records I had already recommended for addition in earlier artifacts**: the **Optimized Municipal Electric Vehicle Charging Analysis** (approved in the Rio Metro/sustainability lane) and the **Vision Zero executive order** (approved in the flat-remainder lane). Both reach this lane through `resolveuid` aliases whose URLs share nothing with the earlier records’. **No wrong fact — both earlier approvals stand — but Codex would have been told to archive one file twice under two candidate ids, which is exactly what the sweep exists to prevent.** The sweep now runs against **both** sets (1,020 archived records **and** 1,659 artifact rows), and the next lane’s brief says so. **A saved artifact is shared state as much as the inventory is.**
- **HTTP 200 IS NOT EVIDENCE THAT A FILE EXISTS.** `transit/documents/copy2_of_Minutes11.15.11.doc` returns **HTTP 200, no redirect, and zero bytes**. A link check that records status codes would pass it; only measuring the bytes catches it, and the leading-bytes check has nothing to read. Its name is the shape of the three Para-Transit Advisory Board minutes recommended in this same artifact — **if the 11 November 2011 minutes exist, they are not at that address.** This is the second failure of its kind in the run, after the three economic-forum URLs that returned 200 while serving 4,926 bytes of homepage HTML.
- **Three sets of Para-Transit Advisory Board minutes** (6 May 2009, 9 September 2009, 9 February 2010) as legacy OLE2 documents. **Minutes, not agendas** — no exception needed. And **six documents covering the Montaño Rail Runner station** end to end: a draft environmental assessment, a traffic assessment, the MRCOG framework plan of June 2010, an NMDOT certification of 11 August 2010, and two sets of construction plans. **The 46-sheet plan set is stamped `65% SUBMITTAL`** — design drawings at a submittal stage, not as-built, and its row says so.
- A **query-string twin**: an NMDOT motorcycle-safety page registered twice, once plain and once with `?programInfo=overview` appended, byte-identical. A query string is not a different document.
- **A latent risk in the coverage gate, checked and cleared.** Every generator’s coverage gate reads saved artifacts with `encoding='utf-8'` inside a `try/except`, so a **UTF-8 BOM makes the file silently skipped**. There are **26 BOM-prefixed files** in `project-state/discovery/` — crawl logs, R2 archive plans and validation reports. I checked: they carry **zero** decision rows between them, so nothing has ever been missed. **But if a future decision artifact is ever written with a BOM, the gate will not see it.** Read discovery artifacts with `utf-8-sig`.
- **Running total, 2026-09-13:** 1630 of 2255 `pending review` records are now rows in a saved artifact; **625 remain uncovered** — of which **169 are the `find-your-councilor` remainder** that `councilor-district-3-and-6-cluster-research-2026-09-13.json` already showed to be almost certainly all web pages (100 of 239 fetched, not one a document). Excluding those, roughly **456 genuine document candidates remain**.
- The Council questions-and-answers, LGCC and news lane is triaged and fully terminal. `project-state/discovery/council-qa-lgcc-news-cluster-research-2026-09-13.json` recommends **17 `approved for addition`, 7 `requires human review` and 16 `excluded`** across all 40 uncovered candidates. Nothing had been decided at any of the three paths before this lane, and the two-set sweep returned **zero** from both sets — the first lane since the sweep was introduced to find nothing. Worth recording that it is cheap and sometimes finds nothing.
- **THE HARDEST CALL OF THE RUN, AND I DID NOT MAKE IT.** Seven City **Code Enforcement Notices and Orders with Appeal**, each naming the owner of a specific Albuquerque property and finding its units substandard under the Uniform Housing Code (Enactment Ordinance 25-2018, codified 14-3-1-1 et seq ROA 1994). **The City itself published them on a public Council page** in answer to councillor questions about motel enforcement — a strong argument that they are publishable. But the owners are **not all companies**: six are corporations or LLCs (1213 INC, AA & S INC, HOTEL GP ILIFFE ABQ LLC, RELIANCE ABQ LLC, ROSHYOGI LLC, ROCKHILL CAPITAL PARTNERS LLC) and **one, for 2700 4th St NW, is addressed to three named natural persons**. This lane has excluded rosters of private individuals’ details on its own judgment elsewhere; when that principle collides with a record the City itself made public, **the scope question belongs to a person, not to this lane**. All seven are **one package and one decision**, and every row carries the owner, whether it is a company or a person, the date and the case file number.
- **The documents about the same campaign that are recommended without hesitation:** the **Comprehensive Motel Enforcement History Report, June 2025** and the Planning Department director’s response on motel closure operations. **They describe the enforcement campaign; they do not name the owner of a particular building.** That is the line.
- **Ten Local Government Coordinating Commission agendas**, approved under the missing-minutes exception — **with a limitation I have recorded rather than glossed**. City side: both official City pages return HTTP 200 and carry **not one link containing the word *minutes***; the City’s own site search for the exact phrase plus *minutes* returns a **no-results** page; the inventory holds none; and **none of the ten agendas shows cancellation, postponement or no-quorum language**. But the LGCC is a **joint City–County body and the County side could not be checked: `www.bernco.gov` returned HTTP 403**. The exception applies on the evidence available; **if minutes exist anywhere, the County’s site is where to look.** I will not call a review “exhaustive” when a source was blocked.
- **The questions-and-answers page turns out to be an oversight record, not a publicity one.** It holds the Administration’s numbered answers on whether a statutory committee was lawfully constituted (*“Is it the Administration’s position that it does not have to establish the Committee in accordance with the law?” — “No.”*), the **FY25 non-recurring items** list, a **National Guard deployment breakdown**, the motel enforcement history, boundary map exhibits, and a **contract and fee ledger for the North Domingo Baca aquatic centre** naming Bradbury Stamm Construction against a contractor fee line and WSP USA against an engineering fee line.
- **Two container/title traps on that page.** The record titled *NDB 12 IDOH* is a **spreadsheet**, not the Word document its title implies — leading bytes `504b0304` with `xl/` parts. And **every file on the page is served under an opaque 32-character identifier** (`3a1ae030b21146e7a55cec9a727ae5b7` and the like), so the inventory title is the only name there is. Every row in this artifact therefore carries `inventory_title` alongside the title read from the document.
- **A validator caught me over-generalising again.** I wrote that all seven enforcement notices are image-only scans read by rendering. **One has a usable text layer** — and reading it supplied the owner and case number I had recorded as *“not checked page by page”*. The harness now checks each row’s stated measurement against what the file actually is, in both directions, and verifies that the owner named on a row appears in the text it was read from.
- **Running total, 2026-09-13:** 1670 of 2255 `pending review` records are now rows in a saved artifact; **585 remain uncovered**, of which **169 are the `find-your-councilor` web-page remainder**. Roughly **416 genuine document candidates left.**
- The final sweep lane is triaged and fully terminal. `project-state/discovery/final-sweep-cluster-research-2026-09-13.json` recommends **29 `approved for addition`, 42 `duplicate` and 345 `excluded`** across all **416** remaining uncovered candidates outside the `find-your-councilor` tree. **This exhausts the document-bearing pool.** Eighty-nine per cent of what was left is web pages; **38 records are documents**, and the harness passes at zero failures.
- **A TIMEOUT WROTE A PLAUSIBLE LIE INTO THE LOG.** The bulk fetch ran with `--max-time 90`. The **Albuquerque Sustainable Airport Master Plan is 280,024,902 bytes** and did not finish in ninety seconds; curl wrote the **171,450,880 bytes** it had, and the loop recorded that length with **a correct SHA-256 of the truncated file** against **HTTP 200**. Nothing about the row looked wrong — a large plan, a plausible size, a valid checksum. Caught by refetching without a timeout; **two independent complete fetches agree** on 280,024,902 bytes and the hash. I then tested **every PDF in the lane for `%%EOF` in its last 2 KB — all 38 are complete**, so this was the only one. **Verify the length of anything large against a second complete fetch, and test PDFs for their end marker before recording a checksum.**
- **The substance that was hiding in the remainder.** Two editions of the **Integrated Development Ordinance** (the **2019 Archive Draft**, 22,684,755 bytes, and the **2022 NARO Update Effective Draft**, 35,898,335 bytes — both name their own draft status in their filenames and both rows say so); the **Police Oversight Ordinance** (11,359,271 bytes); the **Planning Department General Fee Schedule 2018**; the **Sustainable Airport Master Plan**; the six-item **50 Mile Activity Loop** set; three progress and perception surveys; **three NMDOT local safety plans** for Española, Truth or Consequences and Madrid (published by NMDOT for other New Mexico communities — each row says so); three public-art brochures; and three Esperanza bike-repair sheets.
- **`R-150Enacted.pdf` is an enacted Council resolution, and only rendering could say so.** It has **no usable text layer**. Page 1 is a City Council interoffice memorandum to Mayor Timothy M. Keller from the Director of Council Services transmitting **Bill No. R-19-150**, *Adopting Interim Procedures For The Development Review Board Until The First Annual Update To The Integrated Development Ordinance Has Been Completed (Jones)*, with **ENACTMENT NO. R-2019-035** written on its face, **passed 20 May 2019 by 9 FOR AND 0 AGAINST**. The enactment number, the bill number and the vote are all *only* in the image.
- **ONE FILE, TWO NAMES, AND ONE OF THEM IS WRONG — NOW RESOLVED.** `documents.cabq.gov/planning/BizReg` serves a single **493,169-byte** file as both *How to Obtain NM **Business** Registration Certificate.pdf* and *How to Obtain NM **Tax** Registration Certificate.pdf*, byte-identical, both opening on *HOW TO Obtain a Registration Certificate Online*. A filename comparison cannot decide which is right; **the document can.** Its steps are: log into the **Taxpayer Access Point at tap.state.nm.us**, open the Letters panel, *Request a Current Registration Certificate Letter*. TAP belongs to the **Taxation and Revenue Department**, so it is the **tax** registration certificate and **the Business-named link is mislabelled.** The Tax record is approved, the Business record is a `duplicate` carrying `a_mislabelled_link`, and the artifact asks that the City be told rather than that both be republished.
- **Another title the document overturned.** The candidate registered as *albuquerque-yearly-survey-**2023**.pdf* is byte-identical to the archive’s **2024** results. The file is a **Pinion Research memorandum dated April 16, 2024**. **The archived title is right and the candidate’s is wrong**; the row carries the correction.
- **The two-set sweep found 42 relationships here, none of them visible from a URL or a title:** **3** already archived, **10** already decided in this run (each row carrying `decided_in`), and **29** cases of one object at two addresses — NMDOT serving every page under both `/infrastructure/…` and `/planning-research-multimodal-and-safety/infrastructure/…`, MRCOG and Rio Metro under two numeric ids. **Run total across the six lanes since the sweep was introduced: 18 already-archived and 12 already-decided records caught.** Every lane before it truthfully reported zero — because nothing was looking.
- **Five distinct byte signatures in this lane all mean HTML** (`3c21444f` 270, `0d0a3c21` 89, `3c68746d` 8, `3c21646f` 3, `0d0a0d0a` 1). **A container check that recognised only the first would have mis-sorted 101 records.** Leading bytes have to be read as a family, not matched against one constant.
- **Two more ways a link fails without failing.** A Legistar legislation URL for R-11-211 returns **HTTP 200 and nineteen bytes reading `Invalid parameters!`**; and `mrcog-nm.gov/536/Annual-Report` returns **302 to a host whose TLS certificate has expired**, so nothing could be retrieved and that row carries **no checksum at all**. Both are recorded as terminal.
- **Running total, 2026-09-13:** 2086 of 2255 `pending review` records are now rows in a saved artifact; **169 remain uncovered — and every one of them is in the `find-your-councilor` tree.** The document-bearing pool is exhausted. I have claimed that last block as a lane and will confirm its contents by measurement rather than leave it on inference.
- **The `find-your-councilor` block is closed, and closing it by measurement rather than by inference was the right call.** `project-state/discovery/find-your-councilor-cluster-research-2026-09-14.json` recommends **1 `approved for addition` and 168 `excluded`** across all **169** remaining records. The harness passes at zero failures.
- **THE INFERENCE WOULD HAVE LOST A DOCUMENT.** `councilor-district-3-and-6-cluster-research-2026-09-13.json` fetched **100 of that tree’s 239 records and found every single one to be a web page**, and on that basis the remaining 169 were recommended to Codex as a **bulk HTML confirmation**. Fetching all 169 shows the inference was *nearly* right: **164 are HTML — and five are not.** Four are councillor portrait photographs (all exactly 400×500, measured from their SOF markers). **The fifth is a real document**: a **29-page, 3,774,703-byte Lee Engineering study**, *2023 Council District 7 Project — Sandia HS Area Safety and Traffic Calming Study*, for **Councillor Tammy Fiebelkorn**. **A unanimous sample of 100 says nothing certain about the 169 that were not sampled.** The check cost one fetch loop; skipping it would have excluded a document permanently, because a class-wide exclusion is the kind of decision nobody revisits.
- **Nothing about that document was visible from its address.** It sits behind a `resolveuid` alias four levels deep under `district-7/district-7-projects/traffic-street-improvements/...`, and **its inventory title is the 32-character identifier itself**. Only the leading bytes separated it from the 164 pages around it. Its **text layer yields little but the schedule and the section numbers**, so the pages were rendered: page 27 is an On-Street Parking slide pairing a study-area map (Comanche Rd, Louisiana Blvd, Candelaria) with a community-provided photograph.
- **It is the meeting presentation, not the study.** The deck ends on a *Thank You / Questions* slide, and **its own schedule lists “Final Report July 2023” as still to come**. That final report is **not in the inventory under any path** — logged as an undiscovered document and carried into the next lane. The row is labelled so it can never be published as the study’s record.
- **Seven byte collisions, zero `duplicate` rows — deliberately.** The sweep found four pages identical to rows in yesterday’s final-sweep artifact (*Report Weed and Litter Issues* and *Contact all Councilors*, each reached through two different councillors’ districts) and a third pair internal to this lane. **Every canonical they would point at is itself an excluded web page**, and a `duplicate` naming a canonical that is not worth keeping is the opposite of what the status means. Each row is excluded on its own merits and carries `byte_identical_to` as a recorded fact instead. **Same principle as the three earlier cases in this run.**
- **A figure the harness caught me overstating, again.** I first wrote that the sweep found *“three more pairs internal to this lane”* on top of the four already-decided rows. **Two of those three pairs *are* the already-decided ones** — counting them separately would have double-counted. The artifact now states it exactly: **three pairs, six records involved, five rows carrying a byte identity**, and the harness recounts all three figures from the log and fails if any drifts.
- **THE PENDING-REVIEW POOL IS EXHAUSTED. 2255 of 2255 records are now rows in a saved decision artifact; **zero remain uncovered.** Sixteen dated research artifacts, every one validated at zero failures, none of them having written to `master-inventory.json`, `checkpoint.json`, `r2-inventory.json`, site content or R2.
- **What is left for research is what the run found but the inventory does not hold**, and I have claimed it as the next lane: the **37 MS4 stormwater documents** (all verified HTTP 200 PDFs), the **4 files found on councillor pages** including the **April 2018 Cruising Task Force report**, and now the **Sandia High School study’s July 2023 final report**. These have no inventory ids, so that artifact will have to say so plainly rather than pretend to be a triage of existing candidates.
- The undiscovered-documents lane is complete. `project-state/discovery/undiscovered-documents-research-2026-09-14.json` gathers **every undiscovered file reported anywhere in this run** — 49 reports of 48 distinct files from seven lanes — refetches and remeasures all of them, and recommends **44 as additions to the inventory and 4 not to add**. All 47 PDFs are complete to their end markers. The harness passes at zero failures.
- **THIS ARTIFACT IS NOT A TRIAGE, AND IT SAYS SO IN ITS FIRST KEY.** Every other artifact in this run decides the status of records that already exist. **None of these 48 files has an inventory id**, so every row carries `inventory_id: null`, the field is called `recommendation` rather than `recommended_status`, and the values are *add to the inventory as a new candidate* / *do not add*. **`Update-Candidate.ps1` cannot act on a single row here** — there is no candidate to act on. They must be created first, after which the ordinary archival gate applies to them.
- **THE STORMWATER HOLE IS THE BIGGEST SINGLE GAP THIS RUN FOUND: 516,974,923 bytes.** The City’s federal stormwater compliance record from 2014 to 2021, none of it in the inventory — the **2014 MS4 Annual Report in full** (main body plus **27 lettered attachments**), the annual reports for **fiscal 2017, 2019, 2020 and 2021** plus a 2021 draft and a signed compiled 2016 report, **EPA’s letter of 17 December 2015 authorising the City’s permit coverage**, and **the MS4 general permit itself**. The three fiscal-year reports alone are 138,084,248, 133,787,450 and 122,657,409 bytes — four fifths of the footprint.
- **TWO EARLIER FINDINGS OF MINE WERE WRONG, AND THE SAME MISTAKE MADE BOTH.** `bond-2013-department-set-cluster-research-2026-09-12.json` reported `DMDCIPFacParkingScope.pdf` as absent from the inventory and recommended adding it. **It is inventory record `src-047e8956baad212d`, status validated**, at the same URL with the same checksum. And `Sirolli.pdf`, which the economic-forum lane reported as missing from the City’s *collection listing*, **is an inventory record too** — `src-719d61a1047aeb31`, already excluded. **Probing a server tells you a file exists; only searching the inventory tells you the archive lacks it.** Two of seven reports conflated the two. Both were caught twice over, by checksum and by URL.
- **A URL SWEEP IS NOT REDUNDANT WITH A CHECKSUM SWEEP.** For proposed *additions* the sweep must run against **every** inventory record, not only the 1,020 archived ones — and **only 1,460 of the inventory’s records carry a checksum at all**, so a file can sit in the inventory and be invisible to a checksum comparison. This lane therefore swept 1,460 checksums **and** 6,073 inventory URLs. Here the two agreed; they need not have. **It also unblocks an open row:** the bond lane held `src-dc2bbf43c5bb13f7` for human review pending whether the original scope sheet existed. It exists and is validated.
- **HOW MANY PAGES YOU LOOK AT DECIDES WHAT YOU CONCLUDE ABOUT A TEXT LAYER.** The fiscal 2017, 2019 and 2020 reports defeated the renderer at 122–138 MB. They did not need it: **their first six pages are images and the text from page 7 on names the reporting period exactly.** I first recorded them from a two-page extraction window as having no text at all, and the harness caught the mismatch between the window my evidence quoted and the window it checked. **A file whose opening pages yield nothing is not a file without a text layer.**
- **A draft and a final proved identical by render-and-hash.** The fiscal 2021 annual report exists twice, 134,830 bytes apart, neither with a text layer on page 1. **Rendering both produced byte-identical page images — the same 450,584-byte PNG with the same SHA-256 — and both files have 382 pages.** Recorded as a supersession: archive the final, keep the draft only if the archive keeps drafts.
- **A filename that promises results and delivers none.** 2014 MS4 Attachment A.1.b is called **Survey Card Results**. It is two pages and **both of them are the blank survey card**. There are no results in it. Its row is titled for the file, not the filename.
- **THE SANDIA FINAL REPORT: FOUND, AND NOT RETRIEVABLE.** The District 7 project page carries **two different links** — *Meeting PowerPoint Presentation* (the deck approved yesterday) and *Sandia High School Safety and Traffic Calming Study*, which points at **`https://sfftp.cabq.gov/f/7e1d61fbc4c19830`**, a City file-transfer host. **The page itself confirms yesterday’s reading was right.** But the share URL returns **1,344 bytes of JavaScript application shell** and the host’s REST API answers **401** without credentials. There are no bytes to measure, so **it has no row** — a row asserting a document with no size, checksum or container would be asserting something unverified. **I did not attempt to work around the authentication.** It needs browser retrieval, and the City site should be swept for other `sfftp.cabq.gov` links: any document published only that way is invisible to discovery.
- **Two recommendations are carried forward, not re-derived, and the artifact says which.** `Part2.pdf` (already 144 of the 276 pages of `src-a5475f489b373f20`) and `Figure17.pdf` (already inside the validated R2 archive at printed page 35). I confirmed both files are still served at the recorded size and checksum, and rendered Figure 17 to confirm it is the Central Avenue conceptual streetscape design — **but I did not re-run either containment measurement, and no row should be read as a second independent confirmation.**
- **Two files have no page on the site to sit on** — the ABQ Area Resources Quick Guide and the APD resource card, both social-services material, and there is no health or human-services page. Their rows propose **no canonical page** and say why. **Placement, or a decision that they are out of scope, belongs to a person.**
- **Next lane claimed, and it is the one with the best evidence behind it.** Harvesting document links from City pages before discarding them produced **37 files from the stormwater permit page and 4 from a councillor lane that contained no documents at all**. This run has excluded **roughly 900 HTML pages** and harvested almost none of them. **The HTML for about 535 of those pages is already on disk**, so most of the lane costs no refetch.
- **THE HARVEST IS THE MOST PRODUCTIVE THING THIS RUN HAS DONE.** Parsing **535 stored HTML pages** — pages this run had already excluded — for document links yielded **610 distinct links**, of which **254 are already inventory records and 355 are not**. All 355 were fetched: **every one returns HTTP 200 and real document bytes** (336 PDF, 16 OOXML, 2 legacy OLE2), **1,326,543,013 bytes in total**, and not one PDF is truncated. **The crawl that built this inventory missed 355 documents that its own pages link to.**
- First slice complete: `project-state/discovery/harvested-city-documents-research-2026-09-14.json` covers the **69 files served from `www.cabq.gov`** — **28 to add, 41 not**. Harness passes at zero failures. Like the undiscovered-documents artifact, **this is not a triage**: every row carries `inventory_id: null` and a `recommendation`, and **`Update-Candidate.ps1` cannot act on any of it**.
- **THE METHOD CORRECTED ONE OF MY OWN DECISIONS.** The harvest turned up a link to `sfftp.cabq.gov/f/licenses.txt`, which can only come from the City’s file-transfer application — and **the page it came from was one of the final-sweep lane’s own candidates**. `src-2f89e1bc040e1d33` is titled **“Final McDuffie-Twin Parks Traffic Calming Study”** and I had **excluded it as a live web page** because its 1,344 bytes are HTML. The bytes are HTML; **a document is behind them**. `final-sweep-cluster-research-2026-09-13.json` is corrected — the row is now `requires human review` for browser retrieval, and the artifact carries an `a_correction_to_this_artifact` block. The companion row, an ArcGIS Map Journal app, stays excluded but now says what it actually is. **A container check tells you what the bytes are; I let it answer whether a document exists.** Of 371 HTML responses in that lane, **exactly two were under 4,000 bytes and both were application shells** — size is the cheap tell that leading bytes cannot give you.
- **A regeneration hazard, found and fixed.** Re-running the final-sweep generator after two later lanes existed made its sweep pick up those later artifacts, and its rows started pointing at rows that point back at them. **A lane can only be informed by what existed when it ran.** Both the generator and its harness now skip any discovery artifact dated after the artifact being built, and the artifact states that it does.
- **What was sitting in plain sight on `www.cabq.gov`:** the **Third Amended Court Approved Settlement Agreement in *United States v. City of Albuquerque*** (the federal police-reform CASA, approved 2 June 2023, filed as Document 988-2 in case 1:14-cv-01025); the **Downtown 2050 Redevelopment Plan, May 2025**; a **complete recent series of six City Clerk public-records reports** (FY2025 annual through FY2026 Q2/Q3); the **Minimum Wage Ordinance as amended** with its enforcement rule and 2026 rate notice; the **Lodgers’ Tax ordinance**; three **Citizens’ Independent Salary Commission** reports; and the **Parks and Recreation Design Guidelines**. None of it in the inventory.
- **36 liquor hearing notices, all excluded, as one reasoned class.** Statutory notices that the City’s Liquor Hearing Officer will hear licence applications — they **announce hearings and do not record them**, the same distinction on which the EDAct lane excluded nine notices of meeting. Their dates run **August 2025 to October 2026**, so most are notices of hearings that have not happened. **The applicants are companies and trading names, so this is a record-type judgment, not a privacy one** — unlike the code enforcement notices I referred to a person. The archival value in liquor licensing is the hearing officer’s *decisions*, and **those are not published at this path**.
- **Four of those notices are Word documents, and their extensions are honest.** The series is mostly PDF; four URLs end `.docx` and the files are OOXML with `word/` parts. Worth stating plainly **because this run has met several cases where the extension lied** — here it did not, and the container was still read from the file rather than assumed.
- **Two accessibility artefacts of the same document.** The City publishes its organisation chart twice: a **922,752-byte graphic with no text layer at all**, and a **227,594-byte accessible version**. Keep both; cite the accessible one. Nothing in the graphic is searchable or screen-readable.
- **Two agendas kept under the missing-minutes rule, with unequal evidence behind them.** The **LGCC agenda of 20 August 2026** rests on the review recorded in `council-qa-lgcc-news-cluster-research-2026-09-13.json` — **which could not check the County side, because `bernco.gov` returned 403**. The **Citizens’ Independent Salary Commission agenda of 25 November 2025 rests on no review at all**; one must be run before it is kept. Both limitations are on the rows.
- **Two more lanes of this harvest remain and are listed above:** the **77 `documents.cabq.gov` planning files** (claimed), and the **173 real property flyers plus 36 third-party-hosted files** (pending). **The flyers are a scope question, not a research one:** auto-generated listing sheets off a live GIS, no date of issue, status “AVAILABLE” and an appraisal that changes — whether the archive keeps a snapshot of live data is for a person to decide, as one package.
- The `documents.cabq.gov` planning slice is complete. `project-state/discovery/harvested-planning-documents-research-2026-09-14.json` recommends **all 77 as additions**, **859,560,362 bytes**, nothing colliding with anything by checksum or by URL. Harness passes at zero failures. It covers the **Mesa del Sol series end to end** (20 files: the Level A plan as amended, the Level B plan, its **142,963,930-byte technical appendices**, the wireless appendix, six notifications of decision, the development agreement and eight Council instruments), **23 business and industrial park plans**, and the DRB/EPC rules, forms and schedules.
- **FIVE ENACTMENT NUMBERS EXIST ONLY AS HANDWRITING IN AN IMAGE.** Council instruments here appear twice: the bill as introduced, with **ENACTMENT NO. left blank**, and an enacted copy with the number **written on its face by hand**. Every one of those enacted copies is a **scan with no text layer on any page**. Read by rendering: **R-23-158 → R-2023-067**, **R-23-98 → R-2023-014**, **R-23-196 → R-2024-001**, **C/S R-12-118 → R-2012-107**, **C/S O-16-22 → O-2016-019**. **A text-based pipeline would index all five without ever recording what they enacted.** Keep both copies: the bill package carries the attachments (R-23-158 is **170,328,730 bytes**), the enacted copy carries the number and the signature.
- **AND ONE OF THEM CHANGED SPONSOR BETWEEN INTRODUCTION AND ENACTMENT.** R-23-196 was introduced *SPONSORED BY: Pat Davis, by request* — extracted from its text layer — and the enacted copy, R-2024-001, reads *SPONSORED BY: Renée Grout, by request*, read from the render. **That is the clearest possible argument against treating an enacted copy as a duplicate of its bill.**
- **AN ORDINANCE THAT BECAME LAW WITHOUT THE MAYOR’S SIGNATURE.** The small cells right-of-way ordinance, **Enactment O-2018-026**, is transmitted with a City Clerk memorandum of 4 December 2018 certifying that Bill F/S O-18-27 passed on 5 November 2018, that **Mayor Keller neither signed it within the ten days allowed nor exercised his veto**, and that under **City Charter Article XI, Section 3** it is therefore in full effect without his approval or signature. **The certificate is the only thing in the file that says how the ordinance became law** — archive the ordinance without it and that fact is gone.
- **A SECOND FONT-SHIFT CIPHER, AND A DIFFERENT SHIFT.** Two **current** Design Review Committee submittal requirement sheets extract as `3ULYDWHO\)XQGHG'HYHORSHU3URMHFWV`. A uniform **+29** character shift decodes it to *PrivatelyFundedDeveloperProjects / DesignReviewCommitteeDRC SubmittalRequirements / EffectiveDate July*. The MRCOG 2030 MTP elements were shifted by **one**. **Two different shifts in one run: test gibberish for a uniform offset before calling a file unreadable.** Neither file is an image — the text is there, systematically wrong.
- **THE `MasterDevelopmentPlans` DIRECTORY MOSTLY DOES NOT CONTAIN MASTER DEVELOPMENT PLANS.** `AlbuquerqueWestMP.pdf` is a *Site Development Plan for Subdivision*; `FountainHillsMP.pdf` a *Site Plan for Subdivision*; `LaderaIndustrialParkMP.pdf` is titled *Ladera **Business** Park*; `PaseoGolfCourseMP.pdf` is a *Site Plan for Subdivision and Building Permit*; and **`RenaissanceMasterPlan.pdf` is a private covenant document** — a Master Declaration and Rules and Regulations, stamped *PLEASE RETURN TO BOARD SECRETARY*. **Title every one from its own title block.**
- **28 of the 77 have no extractable text on any page** — not merely on page 1; `pdftotext` over the first forty pages of each returns nothing. They are wholly scanned signed plan sheets, blueprints and photocopied resolutions, and **all 28 were rendered and read**. The oldest is **Paradise Hills, a blueprint dated MAY 1963**, revised through 1985, approved by the Albuquerque EPC in January 1975 **and the Bernalillo County Commission in February 1975** — a joint City–County approval.
- **Two date conflicts recorded rather than smoothed over.** The Atrisco Business Park amendment sheet gives EPC approval as **both September 13, 2018 (handwritten stamp) and September 13, 2019 (printed block)** on the same page, and its filename says 2018. And the Mesa del Sol Level A cover **misprints its own amendment year as “20234”**. Both rows quote what is there and say the conflict is unresolved.
- **Two files have no readable page count.** Their page trees sit inside compressed object streams, so the count cannot be taken from the file structure the way it can for the other 71 PDFs. Those rows assert **no page count** and carry `page_count_not_readable` saying why, rather than guessing.
- **The last harvest lane is claimed:** the **173 real property flyers** and the **36 third-party-hosted files**. Running total for the harvest so far: **69 + 77 = 146 files decided, 105 recommended as additions**, against a harvest of 355.
- **THE LINK HARVEST IS COMPLETE.** `project-state/discovery/harvested-remainder-research-2026-09-14.json` closes it: **209 rows** — **33 to add, 3 not, and 173 put to a person**. Across the three harvest lanes, **all 355 undiscovered documents are decided**: 69 + 77 + 209. Harness passes at zero failures. **535 pages parsed → 610 document links → 254 already inventory records, 1 already decided, 355 absent.**
- **173 REAL PROPERTY FLYERS: I AM NOT DECIDING THESE.** One page per parcel, generated from the Real Property Division’s GIS, all 173 linked from `www.cabq.gov/available-city-properties`. **Not one states when it was generated**, every one begins **AVAILABLE**, and **165 of the 173 say “Not yet appraised”** — both facts change as parcels are appraised and sold, so a flyer archived today goes silently wrong at some unknown date. **And yet they are real City PDFs about publicly owned land.** The question is whether this archive keeps a **dated snapshot of a live dataset** — if yes, take all 173 on one date and label the date; if no, the object worth capturing is the underlying list. **That is a scope decision, not a measurement.** Fourth class referred to a person this run, after the Prescription Trails guides, the code enforcement notices, and this. Every row carries the parcel identifier, property, acreage, zoning and appraisal status read from the file, so the class can be judged without opening one.
- **FIVE OF THE FLYERS ARE NOT IN ALBUQUERQUE.** The City owns and is offering land in **Farmington** — Parcel 1A (42.5 ac), Parcel 2 (50 ac), Parcel 3 (40 ac), Choke Cherry (2.5 ac) and **Hood Mesa at 340 acres, the largest parcel in the set** — published on the same page as the city parcels. Whatever is decided about the class, these five are a different question.
- **AND THEY CAUGHT MY OWN PARSER LYING.** Those same five flyers are **laid out differently: none states a zoning code**, where all 168 Albuquerque flyers do. My first parse read the token after the word *Zoning* and recorded **“$60,000”, “$1,500,000” and “Not” as zoning codes** — values that really are in the text, just not that field. The harness caught it. The parser now reports a zoning **only when the value is shaped like a zoning code** and marks the rest `zoning_not_stated_on_this_flyer`. **A field parser that assumes one layout silently mislabels the records using another: check the shape of what you captured, not just that you captured something.**
- **Two flyers share a parcel identifier.** Farmington Parcel 1A and Farmington Choke Cherry both carry UPC `2074175049476`. **Not a duplicate** — different bytes, different acreage, different property. **172 parcel identifiers cover 173 flyers**, so if these are ever added the parcel identifier cannot be the key. All 173 checksums are distinct; that was measured, not assumed.
- **The most useful row in the lane is one I excluded.** `sfftp.cabq.gov/f/licenses.txt` is the file-share application’s reference to its own licence file — not a document, and the URL returns the app shell. But **it could only have come from a file-share page, and that page was one of the final-sweep lane’s own candidates.** That is how the wrongly excluded **McDuffie-Twin Parks Traffic Calming Study** was found. **A harvested link that is not worth keeping can still tell you something about the page it came from.**
- **Where the line falls on third-party publications.** Excluded: the **Access Board’s** public rights-of-way accessibility guidelines and a **Federal Register** page range — federal publications of general applicability, same treatment as the FCC rules, the USDA-NRCS note, the NMAC rule, the PNM tariff and the IRS form. Kept: the **FHWA–NMDOT stewardship and oversight agreement**, because it is a federal instrument **specific to New Mexico**. **Generic publication of another authority → out; instrument about New Mexico → in, with attribution.**
- **Substantive additions here:** the **NMDOT NPDES stormwater manual** (23.2 MB), its **Infrastructure Design Directives** (29.3 MB), the 2024 **Erosion & Sediment Control** and **GSI Maintenance** field guides, the **MRCOG Albuquerque Metropolitan Planning Boundaries map** (August 2023), four **ABQ RIDE route timetables**, and two NMDOT histories — *Route 66 and Native Americans* and *The Road to Science*, the latter by named authors Laurel Wallace and Janet Spivey.
- **Eleven of the 36 third-party files are not PDFs**, and no extension was trusted: **two legacy OLE2 binary Word documents** (read with `antiword`), **a 73-slide PowerPoint** (read from `ppt/slides`), **four spreadsheets** and **three DOCX** (read from `xl/sharedStrings.xml` and `word/document.xml`). Three files announce their own provisional status — the DBE goal methodology **begins with the word DRAFT**, the programmatic agreement is a draft for agency review, and the DBE program manual’s filename says it is being revised. Three crash-data files are **sample data documenting the schema, not real crash records**, and are labelled so.
- **Next lane claimed, and it is the highest-value work left.** **220 excluded HTML rows across eleven earlier artifacts have never been link-harvested** — their scratch files are gone, so they must be re-fetched. The first harvest turned 535 pages into 355 undiscovered documents. **There is no reason to expect these 220 to be barren.**
- **RE-FETCHING THE DISCARDED PAGES PAID OFF IMMEDIATELY.** The 220 excluded HTML rows with no surviving scratch copy were fetched again; **219 returned 200**, and parsing them gave **159 document links — 48 already inventory records, 5 already decided in the first harvest, and 106 absent from the inventory**. All 106 were fetched: **105 returned 200, 103 of them real PDFs, 1,029,025,522 bytes, not one truncated.**
- First slice complete: `project-state/discovery/second-harvest-council-research-2026-09-14.json` — **56 records, 50 to add, 6 not**. Harness passes at zero failures.
- **THE 2022 REDISTRICTING COMMITTEE RECORD IS HERE IN FULL, AND IT SETTLES SOMETHING THIS RUN HAS STRUGGLED WITH ALL ALONG.** Eight agendas, **eight sets of minutes**, eleven district maps with statistics tables, the Report and Recommendations, the communications plan, the consultant’s rules briefing and three analytical briefings (compactness, population movement, social vulnerability), the members’ concept ratings, and **two Council floor amendments each proposing a different citizen map**. **This run has had to invoke the missing-minutes exception four times — MPRAB, EDAct, LGCC, the salary commission. This body needs it for nothing: agendas and minutes are published meeting by meeting, eight pairs of them.** Seven sets are approved; the 29 June 2022 set is published as **DRAFT Minutes** and its row says so.
- **The scale is unlike anything else recommended in this run.** The **Report and Recommendations is 242,483,055 bytes** — the largest file measured anywhere in this run except the 280 MB airport master plan. The **eleven district maps run 41,975,777 to 72,579,402 bytes, 656,466,507 bytes together**, and are drawn maps with almost no extractable text: **the five citizen maps yield nothing but the repeated words “City Councilors”**. Footprint for the slice: **993,940,052 bytes**.
- **A CONTENT SUBSTITUTION CONFIRMED FROM A SECOND DIRECTION.** `economic-forum-cluster-research-2026-09-12.json` found three economic forum URLs serving **4,926 bytes of City homepage HTML** instead of their documents. This lane reached two of the same documents by a **different URL form** — the Plone `/@@download/file/` path rather than the plain one — and got **the same 4,926 bytes, byte-identical**. **That rules out an addressing artefact: the documents are gone from the server.**
- **Two harness catches worth recording.** It rejected my claim that a compactness table “opens: % of Population in a Different District Compared with Current Plan” — **the extraction actually interleaves two column headings** (“% of Population in a **Compactness** Different District Compared with Current Plan **Reock Polsby-Popper**”). I had quoted the *reading* order, not the extraction order. The row now quotes what the extractor gives and explains the interleaving. The harness now also **checks every quoted opening against the file it is quoted from**, normalised, for all 50 rows.
- **Also added:** the **Regional Baseball Complex** set (March–April 2016 phase 1 site plan, building elevations and softball conversion plan — and **the elevations name a different architect from the site plan**, so neither supersedes the other), the **Safe Outdoor Spaces A-12 option 3 map** from the 2021 IDO annual update, the **Golf Division 2026 aerification schedule**, the **Balloon Fiesta Park event application**, and the **New Mexico DWI Report 2023** prepared for NMDOT.
- **Eighth exclusion on the same reasoning.** The **NM Attorney General’s Open Meetings Act compliance guide**, a **state Senate bill** amending the Child Helmet Safety Act, and a **federal air traffic control careers brochure** are all publications of general applicability belonging to the bodies that issued them — joining the FCC rules, the USDA-NRCS note, the NMAC rule, the PNM tariff, an IRS form, the Access Board guidelines and a Federal Register extract. **What stays in: an instrument specific to New Mexico**, like the DWI report.
- **A broken link recorded rather than dropped:** a City committee page still links a UNM traffic safety document that returns **HTTP 404**.
- The second harvest is complete. `project-state/discovery/behavioral-health-task-force-research-2026-09-14.json` covers the **50 City/County Behavioral Health Task Force files** of 2014–15: **42 to add, 7 not, 1 put to a person**. Harness passes at zero failures. Across both lanes the second harvest’s **106 files are all decided** (56 + 50).
- **THE ONE FILE I WILL NOT DECIDE, AND IT IS THE MOST SENSITIVE THING THIS RUN HAS TOUCHED.** A parent’s dated first-person account of a **26-year-old private individual’s** psychiatric crises, police contacts, incarceration and hospital treatment — 7,982 characters of specific detail — submitted to the task force through a family support organisation and **published by the City on its own page**. The anonymisation is thin: he is “PT” and “my son”, but the dates, the county jail and the named hospital would identify him to anyone who knows him. **That the City published it is an argument, not an answer** — whether an archive should re-publish and index a private person’s psychiatric and criminal history is a different question. **The artifact does not quote the narrative, name anyone in it, or reproduce its detail.** Fifth class referred to a person this run. Its companion — a *caregiver’s perspective* presentation that is general, structural and names nobody — is recommended without hesitation. **That is the line.**
- **A RULE FOR THE HALF OF THIS SET SOMEBODY ELSE WROTE.** *A document is kept if the task force produced it, commissioned it, or was given it as an exhibit — because that is what a body’s record consists of. It is not added if it is a standing publication of another authority that the task force merely gathered as background.* **Kept:** the commissioned UNM *Landscape of Behavioral Health* and gaps reports, the City’s own **2004 Crisis Triage Planning report** (the earlier attempt at the same problem, ten years on), an out-of-state crisis programme presentation, a comparable provider’s budgets, the MDC *Fast Track* presentation. **Not added:** a statewide services directory, a state purchasing collaborative’s own meeting packet, a 2011 state task force report, an LFC programme evaluation and that committee’s budget press release. **The artifact names its own borderline case** — a VA construction cost guide annotated “Example I” and used to cost a facility, which is an exhibit and another authority’s publication at once — so the call can be reversed without re-reading the file.
- **And one excluded on a different ground entirely:** a printout of an **Albuquerque Journal opinion column**. Two City Councillors wrote it, but **the newspaper published it and holds the rights**; a printed copy of a copyrighted column is not the archive’s to redistribute. Cite it at source.
- **A third position on the minutes question.** The MPRAB, EDAct, LGCC and salary commission needed the missing-minutes exception. The **2022 Redistricting Committee** needed none — agendas and minutes, eight pairs. **This body is in between:** it published **meeting notes for five of its eight meetings** and calls them *notes*, not minutes. **18 and 25 September and 2 October 2014 have agendas and no notes.** Whether notes were taken and not published, this page does not say, and no exhaustive search has been run. **So no exception is claimed:** the agendas are recommended as agendas, on their own merits.
- **Running totals for the harvest method.** Two harvests, **754 pages parsed → 769 document links → 461 absent from the inventory**, every one of them fetched and measured, **2.36 GB**, not one truncated PDF. Five artifacts decided them. **The method also corrected an earlier artifact of mine** (the McDuffie-Twin Parks study, wrongly excluded as a web page).
- **Next lane claimed, and the seam is far from worked out.** Both harvests parsed only pages this run had *excluded*. **The inventory holds 2,586 page-shaped records that have never been parsed at all** — 882 excluded, 751 pending review, **508 validated**, and 1,401 of them on `www.cabq.gov`. On the yield so far that is a large number of undiscovered City documents still sitting behind pages the archive already knows about.
- **BOTH FILE-SHARE STUDIES ARE IN.** The repository owner retrieved them through a browser after I reported I could not. `project-state/discovery/file-share-retrieval-research-2026-09-14.json` measures both; harness passes at zero failures. **Neither matches anything the archive holds or anything this run has already decided.**
- **`src-2f89e1bc040e1d33` is answered and is now `approved for addition`.** It is the **McDuffie/Twin Parks Neighborhood Traffic Calming Study, final report dated 7 May 2024**, prepared for the City by Wilson & Company — **11,940,327 bytes, 321 pages**, complete to its end marker, with sections for two public meetings and their post-meeting comments. **The record I had first excluded as a live web page is a 321-page final engineering study.**
- **And the Sandia final report exists after all.** **Sandia High School Area Traffic Calming and Safety Study, Final Report with appendices — January 2024**, 10,493,498 bytes, 157 pages, carrying a New Mexico PE seal. Study area bounded by Louisiana Blvd, Wyoming Blvd, Comanche Rd and Phoenix Ave, explicitly excluding the bounding arterials. **Not in the inventory under any path**; recommended as a new candidate.
- **A CORRECTION THE RETRIEVAL FORCED.** I recorded from the presentation’s own schedule slide that the final report was due **July 2023**. It is dated **January 2024** — six months later. **A schedule inside a document tells you what was intended on the day it was written; it is not evidence of when anything happened, and a row must not carry it as though it were.** `find-your-councilor-cluster-research-2026-09-14.json` is corrected on both the row and the block.
- **THE PROVENANCE CHAIN HERE IS WEAKER THAN EVERY OTHER ROW IN THIS RUN, AND THE ARTIFACT SAYS SO.** Every other measurement was taken from bytes I fetched myself, so URL and bytes were verified in one act. **These were not.** I can attest the files are what they say they are and that nothing in the archive matches them; **I cannot independently attest they came from the URLs on these rows.** What would close the gap: a second retrieval by anyone, reproducing these checksums.
- **AND A WARNING FOR THE VERIFICATION PASS: these two records can never be verified by re-fetch.** Both `authoritative_url`s are share pages returning **1,344 bytes of application shell** to any fetch. The size and checksum on the rows are the *document’s*. **Mark them as not re-fetchable, or the next verification sweep will flag two good records as failures.** The final-sweep row deliberately keeps the 1,344-byte shell measurement and carries a pointer rather than a copy of the decision.
- **What this does not resolve:** any City document published only through `sfftp.cabq.gov` is invisible to a crawler in exactly this way. **Two surfaced only because two pages happened to link them** — and one was found only by harvesting a licence-file link out of the application shell. **A sweep of the City site for every `sfftp.cabq.gov` link has not been done**, and the 2,586 unparsed page-shaped records are where it would start.
- **THE FILE-SHARE SWEEP IS DONE, AND IT BEGAN BY FINDING A HOLE IN MY OWN METHOD.** `project-state/discovery/file-share-sweep-research-2026-09-14.json`: **2,168 pages parsed → 21 distinct `sfftp.cabq.gov` share links.** 2 were already retrieved, **3 the owner retrieved and this lane recommends**, 16 the owner reviewed and did not take. Harness passes at zero failures.
- **BOTH LINK HARVESTS SILENTLY DISCARDED EVERY ONE OF THESE.** They filtered candidate links **by file extension** — pdf, doc, docx, xls, xlsx, ppt, pptx, csv, rtf, txt. A share URL is `/f/39395977f639746c` and **has no extension**, so all 21 were dropped. **Exactly one sfftp string survived the harvest: `licenses.txt`**, the application’s own licence file, purely because it ends in `.txt` — and that one accident is what exposed the wrongly excluded McDuffie study, and then this. **The harvest lanes reported 461 undiscovered documents and implied that was what those pages held; they also linked at least 21 file-share documents the harvest never reported.** Sweep for known document hosts **by hostname**, not only by filename shape.
- **Three retrieved and recommended, 15,659,378 bytes**, all new by checksum and by URL: the **Comprehensive Plan Main Street Corridor / East Central briefing** (20-slide deck, 6 May 2026, Council Services), the **Laurelwood Median and Parkway conceptual landscape and irrigation study** (DMD, April 2025, Ladera Dr NW to Hanover Rd), and a **briefing on the 2025 Drinking Water Consumer Confidence Report**.
- **AND THAT THIRD ONE IS NOT WHAT ITS LINK SAYS IT IS.** The City page labels it *“2025 Drinking Water Consumer Confidence Report”*. **It is a 13-page briefing *about* that report, not the report.** Catalogue it under the link text and the archive would assert it holds a federal compliance document **it does not hold**. Worse, its filename begins `D8-ABCWUA` — and **the document’s own text never names ABCWUA or the City anywhere.** The attribution rests on the filename alone, and the row says so.
- **A FINDING THAT REACHES BACK OVER FOUR EARLIER LANES.** One of the 21 links is the **African American Advisory Board agenda for 4 August 2026** — a City board publishing its agenda through the file share. **This run invoked the missing-minutes exception four times** (MPRAB, EDAct, LGCC, the salary commission), each after searching official pages and finding nothing. **A document published this way looks like a web page to every check those reviews ran.** I am not claiming that is what happened in those cases — I am recording that **my searches could not have distinguished it**, and that **any future missing-minutes or exhaustive-source claim must check for `sfftp.cabq.gov` links before concluding nothing was published.**
- **The owner made the scope call on the other 16, and it is recorded rather than reasoned away.** Food-assistance flyers published by a county food bank, photo galleries of a tree-lighting and a Touch-a-Truck event, a festival notice. **All 16 are listed in the artifact with their link text and linking page, so the decision can be revisited without running the sweep again.**
- **Coverage limit, stated plainly:** the 2,168 pages are only pages that appear in `master-inventory.json`. **Any City page the original crawl never recorded could link more, and this sweep cannot see it. 21 links is a floor, not a total.**
- **THE OWNER REVIEWED THE THREE RETRIEVED FILES AND REVERSED ONE OF MY RECOMMENDATIONS.** The **drinking water briefing is rejected** — a low-value slide deck. `file-share-sweep-research-2026-09-14.json` now carries it in `do_not_add`, and the harness re-checks at zero failures. **Verified before changing anything: it was never added to master-inventory.json by checksum or URL, never in r2-inventory.json, never in site content.** It existed only as a recommendation in a research artifact — which is exactly why a research lane writes recommendations and not records.
- **What the archive actually wants instead is recorded as a wanted document:** the **2025 Drinking Water Consumer Confidence Report itself**. It is **not in master-inventory.json under any URL**, and it **will not be found on a City path** — its publisher is **ABCWUA**, a separate authority. Queue discovery against ABCWUA, not against cabq.gov.
- **And the distinction this lane drew is what made that decision possible.** The City page labels that file the *“2025 Drinking Water Consumer Confidence Report”*. Had it been catalogued under its **link text** rather than what the document says it is, **the archive would have believed it already held the report it is still missing**, and the owner could not have asked for it.
- **THE EAST CENTRAL MAIN STREET DECK IS CONFIRMED FOR ARCHIVING AND LINKING, AND THE REASON SETTLES A PRINCIPLE.** In the owner’s words: it **shows how these initiatives develop and are presented to the public by city employees**. So: **a briefing deck is not automatically lesser than the document it describes.** It is kept when it is itself **the record of a public process** — who presented what, to whom, on what date — and not kept when it is only a **summary of a document the archive should hold directly**. The Main Street deck is the first kind; the water deck was the second. The Main Street row now says it is kept **for** being a briefing, not in spite of it.
- **Where that bears on earlier lanes, without my changing them.** Several rows in this run carry a caution that a deck *is not the document it describes* — the **Sandia meeting presentation**, the **DRB transition briefing**, the **redistricting consultant briefings**. Those cautions remain correct as statements of *what the files are*. **This principle says they are not, by themselves, reasons to exclude.** I have changed none of those rows; the reasoning is recorded so the owner can apply it.
- **THE THIRD HARVEST IS RUNNING AND THE CHECKSUM SWEEP JUST EARNED ITS KEEP.** Re-parsing the **1,337 City pages already fetched for the file-share sweep** (no refetch needed) gave **1,625 document links**; 636 were inventory records by URL, leaving **975 that looked new**. Then the checksum sweep ran: **73 are byte-identical to inventory records, 542 to rows this run had already decided**, and 16 are copies of each other. **Only 358 are genuinely new.** A URL-based reconciliation would have proposed 975 additions, **nearly two thirds of them files the archive already holds, served at addresses it had not seen.**
- First slice complete: `project-state/discovery/urban-design-planning-research-2026-09-14.json` — the **86 `planning/UDD` files, 1,715,982,194 bytes**, the largest footprint of any lane in this run. Harness passes at zero failures. It is **the City’s comprehensive planning record**: both generations of the Comprehensive Plan and the **54 sector and area plans the IDO repealed**.
- **BOTH COMP PLAN GENERATIONS, AND EVERY PART PROVEN TO BE INSIDE ITS WHOLE.** The 2017 edition (**113,805,107 bytes**) and the edition **effective October 2025** (**294,090,168 bytes — the largest file measured anywhere in this run**). All 14 of the 2017 chapters plus its appendices, and all 12 published 2025 chapters plus its appendices, score **token coverage 1.0000 with 3 of 3 sixty-word contiguous runs present verbatim**. 28 containment tests, all unanimous.
- **AND BOTH THE WHOLE AND ITS PARTS ARE RECOMMENDED — a deliberate departure, recorded as one.** Proving containment is what let the run drop 60,998,778 bytes of redundant Connections 2040 chapters. Here the parents are **113 MB and 294 MB**, and a 294-megabyte file is not a usable citation target for a page about urban design. **The chapters are the practical unit; the compiled plan is the authoritative one.** Every chapter row carries the containment measurement, so if the archive would rather hold only the compiled plans, **the chapter rows can be dropped and nothing else changes**.
- **THREE OF THE MOST-CITED CHAPTERS OF THE CURRENT PLAN HAVE NO SEPARATE FILE.** The 2025 plan has fourteen chapters; twelve are published individually. **Chapter 6 Transportation, Chapter 9 Housing and Chapter 10 Parks & Open Space are not.** All three are inside the compiled plan — its contents names them and carries their text — but there is no citable chapter file for any of them. **Look for them before treating the set as complete.**
- **A REPEAL STAMP THAT HIDES THE DOCUMENT UNDERNEATH IT.** The repeal was verified from the documents, not the filenames: **53 of the 54 carry a text-layer stamp**, 52 naming **R-17-213** and 8 also **O-17-49**. But for **20 of them that stamp is the only extractable text** — **exactly 144 characters, byte-identical across all 20**. It is a text overlay on a scan. **A keyword search of this archive would match those plans only on the word REPEALED**: search for Sawmill, or Old Town, or University Neighborhoods, and the plan for that neighbourhood never comes back. They need OCR before they are findable.
- **Two that do not fit, and both rows say so.** The **West Side Strategic Plan** carries the word REPEALED but **names no instrument**. The **Southwest Area Plan** yields **no extractable text at all** — not even a stamp — so its repeal rests on its filename and nothing else. **Neither is counted among the verified.**
- **THE HARNESS CAUGHT ME TWICE ON THE SAME CLAIM.** First: checking only pages one and two found the R-17-213 / O-17-49 pair in just 8 of 50, which would have supported “42 plans carry no repeal marking” — the narrow check was measuring **my extraction window, not the documents**. Second: the generator **treated missing data as a measured zero**, so the four area plans, which had never been measured, were silently counted among the stamp-only files — 22 where the verified figure was 18. Measuring them properly gave 20 **and turned up the two exceptions above**. **Absent data must never read as a measured zero.**
- **FIRE SLICE COMPLETE:** `project-state/discovery/fire-monthly-reports-research-2026-09-15.json` — **81 Albuquerque Fire Rescue Monthly Informational Reports, 573,027,770 bytes**, zero failures on the first harness run. **A complete monthly series, February 2018 to October 2024, one report per month, no gaps inside the span.** Every report’s period was read from its own first page and compared with its filename — no disagreement in 80 of 80; the one image-only report was read from its rendered cover.
- **The archive held the page and not the reports.** All 81 are linked from `fire/copy_of_reports`, which is itself **validated inventory record `src-6260059ea0d144b3`**. The same page links nine fire annual reports (2014–2022), all already held. **The annual series was collected; the monthly one never was.**
- **Two issues of April 2020.** The page links `mir-april-2020-1.pdf`; probing the name without `-1` returned a different, unlinked file. Both 18 pages, 12 identical by position — but the earlier issue heads page 1 **“MARCH CALLS” under an April 2020 title**, which the linked issue corrects to “APRIL CALLS”. Linked issue is the record; the earlier one is in `do_not_add` as superseded, fully measured, reversible.
- **The 2018 two-month titles are not overlapping coverage.** Seven reports are titled February–March through August–September. Counting month names, each is dominated by its **first**-named month, and the single-month September 2018 report has the same shape. Recorded as consistent with monthly coverage — an inference from counts, not page-by-page — and every row keeps the span as published.
- **Thirteen variant filenames probed; only April 2020 has an earlier version.** The `lr` copies are the **only** published form of those months (the full-resolution names 404), and each row says so. **March 2023 states on its own first page that unit response and alarm reason data are omitted** due to technical issues. **Nothing after October 2024** is on the page, and November/December 2024 return 404 — whether the series stopped or moved is unknown.
